{
  "schemaVersion": "1.0.0",
  "readme": "",
  "modules": [
    {
      "kind": "javascript-module",
      "path": "src/elementals/accordion/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "exclusiveOpen",
          "parameters": [
            {
              "name": "states",
              "description": "`open` of each panel, in document order.",
              "type": {
                "text": "boolean[]"
              }
            }
          ],
          "description": "Which panels of an exclusive group are left open, given how they were authored: the\nfirst one with `open` on it, and no other.\n\nThe decision has to be made before the shared `name` goes on. Naming several\nalready-open panels leaves it to the browser which of them survives, and a group that\nopens on a different panel depending on the engine is not a group anybody authored.\nChoosing here makes it the first, everywhere.",
          "return": {
            "type": {
              "text": "boolean[]"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<accordion-elemental>` custom element.\n\nA thin coordinator over native `<details>`/`<summary>` rather than a\nreimplementation of the APG Accordion pattern. Native already provides the\ndisclosure semantics, Enter/Space activation, screen-reader announcement and\nfind-in-page expansion. This element only adds what native leaves out:\n\n- `exclusive`: assigns a shared `name` so only one panel stays open\n- arrow/Home/End navigation between headers (APG accordion, optional but recommended)\n- opening the panel that contains the current URL fragment\n- an `accordion-toggle` event on the group, since `toggle` does not bubble\n- a height animation on open and close\n\nLight DOM, no shadow root, so every part stays stylable by the page author.",
          "name": "AccordionElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<time>"
              },
              "description": "How long a panel takes to slide. The element reads it back out of the computed styles, so `0s` toggles instantly.",
              "name": "--accordion-elemental-duration",
              "default": "250ms"
            },
            {
              "type": {
                "text": "<easing-function>"
              },
              "description": "How the slide moves.",
              "name": "--accordion-elemental-easing",
              "default": "ease"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Rule between the panels.",
              "name": "--accordion-elemental-border-color",
              "default": "color-mix(in srgb, currentcolor 15%, transparent)"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Corner radius, on the grouped look.",
              "name": "--accordion-elemental-radius",
              "default": "0.5rem"
            }
          ],
          "slots": [
            {
              "description": "The `<details>` panels, each with its own `<summary>`.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "panels",
              "description": "Direct-child panels only, so a nested accordion is not swallowed.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "headers",
              "description": "The `<summary>` of each panel, in document order.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "wrapPanels",
              "description": "Wrap each panel body in two divs, because a height transition needs one box to\nmeasure and clip and `<details>` hands you a bare run of siblings. Idempotent,\nso moving the group in the DOM does not nest a second wrapper.\n\nTwo rather than one because the box being animated cannot be padded: block\npadding is a floor the height cannot get under, since `box-sizing: border-box`\nrenders `height: 0` as the padding, and the panel would slide shut down to it\nand then cut. The outer box is the library's and stays inert; the inner one is\nwhere a stylesheet puts the panel's inset, on one box rather than spread over\nwhichever children it thought to name.\n\nponytail: `::details-content` is the wrapper the platform already has, but\nanimating it from 0 to `auto` also needs `interpolate-size`, which is not\neverywhere yet. Drop the outer div for the pseudo-element once it is."
            },
            {
              "kind": "method",
              "name": "wrapperOf",
              "parameters": [
                {
                  "name": "panel"
                }
              ],
              "return": {
                "type": {
                  "text": "HTMLElement|null"
                }
              }
            },
            {
              "kind": "method",
              "name": "applyExclusive",
              "description": "Give every panel the same `name`, which is what makes native `<details>`\nmutually exclusive."
            },
            {
              "kind": "method",
              "name": "openPanel",
              "parameters": [
                {
                  "name": "panel"
                }
              ],
              "description": "Open a panel and slide its body down. The panel opens first, since the body\nis `display: none` until it does and an unrendered box has no height."
            },
            {
              "kind": "method",
              "name": "closePanel",
              "parameters": [
                {
                  "name": "panel"
                }
              ],
              "description": "Slide a panel's body up, and only then actually close it - `<details>` sets\nits contents to `display: none` on close, which would cut the animation off\nat frame one."
            },
            {
              "kind": "method",
              "name": "restoreName",
              "parameters": [
                {
                  "name": "panel"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "Take over the toggle so the close can outlive the click. Enter and Space on a\n`<summary>` dispatch a click too, so this covers the keyboard as well."
            },
            {
              "kind": "method",
              "name": "onKeyDown",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onToggle",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onHashChange"
            },
            {
              "kind": "method",
              "name": "openFromHash",
              "description": "Open the panel containing the element the URL fragment points at, so a link\nto a single question lands on it opened. Instant rather than animated: a deep\nlink should arrive at the content, not at a panel still on its way open."
            }
          ],
          "events": [
            {
              "name": "accordion-toggle",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.panel` is the `<details>` that toggled, `detail.open` its new state. On the group, because `toggle` does not bubble."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "boolean"
              },
              "description": "Only one panel open at a time.",
              "name": "exclusive",
              "default": "false"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The shared `name` `exclusive` assigns. Generated if unset.",
              "name": "name"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "accordion-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "nextIndex",
          "declaration": {
            "name": "nextIndex",
            "module": "src/elementals/accordion/index.js"
          }
        },
        {
          "kind": "js",
          "name": "exclusiveOpen",
          "declaration": {
            "name": "exclusiveOpen",
            "module": "src/elementals/accordion/index.js"
          }
        },
        {
          "kind": "js",
          "name": "AccordionElemental",
          "declaration": {
            "name": "AccordionElemental",
            "module": "src/elementals/accordion/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/carousel/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "rotationInterval",
          "parameters": [
            {
              "name": "value",
              "description": "The attribute, as authored.",
              "type": {
                "text": "string|null"
              }
            },
            {
              "name": "fallback",
              "default": "5000",
              "description": "Milliseconds to use when it says nothing usable.",
              "optional": true,
              "type": {
                "text": "number"
              }
            }
          ],
          "description": "How long between slides, out of the `interval` attribute.\n\nAnything that is not a positive number of milliseconds is the default rather than an\nerror: a typo in one attribute is not a reason for a carousel to stand still. The floor\nis the part worth writing down - under a second is a strobe, and it is also shorter than\nthe smooth scroll it would be interrupting, so the carousel would never finish arriving\nanywhere.",
          "return": {
            "type": {
              "text": "number"
            }
          }
        },
        {
          "kind": "function",
          "name": "stepSlide",
          "parameters": [
            {
              "name": "current",
              "description": "Index the carousel is on.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "delta",
              "description": "`1` for next, `-1` for previous.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "count",
              "description": "How many slides there are.",
              "type": {
                "text": "number"
              }
            }
          ],
          "description": "Which slide a press of previous or next asks for.\n\nIt stops at the ends rather than wrapping, because the buttons say so: at the end of the\nrow the next button is `aria-disabled` and dimmed, and a control that looks spent and then\njumps you back to the first slide is a control that lied. The rotation is the other\nbehaviour and keeps it - see `advance()`, which is the only thing here that wraps.",
          "return": {
            "type": {
              "text": "number"
            }
          }
        },
        {
          "kind": "function",
          "name": "swipeStep",
          "parameters": [
            {
              "name": "direction",
              "description": "`left` or `right`, as the gesture reported it.",
              "type": {
                "text": "string"
              }
            },
            {
              "name": "rtl",
              "description": "Whether the carousel's direction is right to left.",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "Which way a finger that has just come up was asking to go: `1` for next, `-1` for previous.\n\nOnly `fade` ever asks. A scrolled row is a real scroll container and swipes natively, and\na second answer laid over the browser's would fight it; stacked slides have nothing to\nscroll, so the gesture is read for them or the mode does not have one.\n\nThe one part of that gesture book-of-spells cannot answer, because it reads the screen and\nnot the writing direction: where the carousel runs right to left the next slide is the one\nto the *right*, the same way round as the arrows under it.",
          "return": {
            "type": {
              "text": "number"
            }
          }
        },
        {
          "kind": "function",
          "name": "scrollEdges",
          "parameters": [
            {
              "name": "offset",
              "description": "`scrollLeft`.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "visible",
              "description": "`clientWidth`.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "total",
              "description": "`scrollWidth`.",
              "type": {
                "text": "number"
              }
            }
          ],
          "description": "Whether a scroller is as far back, or as far on, as it goes.\n\nThe scroller's own answer and not arithmetic on the index, which is the only one that\nholds when more than one slide is on screen: with three of five showing, the row is at its\nend while the current slide is the third, and an index that counted to the last slide\nwould leave two presses doing nothing. A row short enough to fit is at both ends at once,\nand that is the honest reading - there is nowhere to go either way.\n\n`abs` on the offset because a right-to-left scroller counts down from zero, and a pixel of\nslack at each end because a fractional layout - a percentage width, a zoomed page - lands\na hair short of the number the arithmetic wants.",
          "return": {
            "type": {
              "text": "{start: boolean, end: boolean}"
            }
          }
        },
        {
          "kind": "function",
          "name": "startInset",
          "parameters": [
            {
              "name": "styles",
              "description": "The scroller's computed style.",
              "type": {
                "text": "CSSStyleDeclaration|object"
              }
            },
            {
              "name": "rtl",
              "description": "Whether the scroller's direction is right to left.",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "How far short of the row's own edge a slide has to stop: its `scroll-padding` on the start\nside.\n\nA row that bleeds past the text it sits under puts the column's inset back as padding, and\nmoves the snap point with it - which is the only way the first card lines up with the\nheading while the row still runs to the edge of the page. Scrolling a slide flush to the\nrow's own edge there lands the same distance *past* its snap point, and a mandatory snap\nthen carries on to the next slide: one press, two slides, and it reads as the element\nlosing count.\n\nPhysical rather than logical, because the number is about to be added to `scrollLeft`: the\nstart side is the left one, or the right one where the row runs the other way.",
          "return": {
            "type": {
              "text": "number"
            }
          }
        },
        {
          "kind": "function",
          "name": "swapHeight",
          "parameters": [
            {
              "name": "from",
              "description": "The height the stack had before the current marker moved.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "to",
              "description": "The height it has now.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "reduced",
              "description": "Whether the reader asked for less movement.",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "Whether a swap in `fade` has a height worth travelling between.\n\nBoth refusals are the same bug approached from opposite sides: a pinned height comes back\noff when the transition it was written for ends, so a pin with no transition behind it is\none nothing ever takes off - and from then on the stack answers a resize, a font arriving\nor a picture that finally loaded with a pixel count taken before any of them happened.\nReduced motion is one road there, because the stylesheet drops the transition under it;\ntwo slides that measure the same is the other, because a transition between one value and\nthe same value never starts and so never ends.",
          "return": {
            "type": {
              "text": "boolean"
            }
          }
        },
        {
          "kind": "function",
          "name": "rotationHeld",
          "parameters": [
            {
              "name": "hovering",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "focused",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "Whether the rotation stays held: while the pointer or the keyboard is in the carousel.\n\nTwo facts rather than one flag, because either can end while the other holds - and the\nAPG asks that rotation not resume until hover *and* focus have both left. One flag is\nthe version that breaks: a mouse crossing a carousel a keyboard reader is inside leaves\nagain, and its leaving restarts the slides under someone mid-caption.",
          "return": {
            "type": {
              "text": "boolean"
            }
          }
        },
        {
          "kind": "function",
          "name": "slideName",
          "parameters": [
            {
              "name": "index",
              "description": "Which slide, from zero.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "count",
              "description": "How many slides there are.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "template",
              "description": "`position-text`, with `{n}` and `{total}` in it.",
              "optional": true,
              "type": {
                "text": "string|null"
              }
            }
          ],
          "description": "The name a slide gets when the markup gave it none: its place in the set.\n\n`1 of 10` is English, and it is the name a screen reader reads out for every slide in the\ncarousel - so leaving it as the only string on this element a page cannot reach is\nleaving the most-read one. `position-text` is the whole template rather than the word\nbetween the numbers, because \"of\" between them is English's shape too: Japanese counts\nthe other way round, `10 中の 3`.\n\nAn empty attribute falls back rather than being honoured. It would otherwise write\n`aria-label=\"\"`, and a slide with no name at all is worse than a slide named in the\nwrong language.",
          "return": {
            "type": {
              "text": "string"
            }
          }
        },
        {
          "kind": "function",
          "name": "markerName",
          "parameters": [
            {
              "name": "word",
              "description": "`slide-text`, with or without `{n}` in it.",
              "type": {
                "text": "string"
              }
            },
            {
              "name": "index",
              "description": "Which slide, from zero.",
              "type": {
                "text": "number"
              }
            }
          ],
          "description": "A picker button's accessible name: `slide-text` with the slide's number in it.\n\n`Slide 3` is English's order, and putting the number after the word is a decision this\nelement has no business making for a language it cannot read. Hungarian writes `3. dia`,\nJapanese `3枚目`. A `slide-text` holding `{n}` says where the number goes and the element\nonly fills it in; one that does not gets the number appended, which is what every\n`slide-text` written before this did.",
          "return": {
            "type": {
              "text": "string"
            }
          }
        },
        {
          "kind": "function",
          "name": "roleDescription",
          "parameters": [
            {
              "name": "raw",
              "description": "The attribute, as authored.",
              "type": {
                "text": "string|null"
              }
            },
            {
              "name": "fallback",
              "description": "The English word to use when it said nothing usable.",
              "type": {
                "text": "string"
              }
            }
          ],
          "description": "The word a screen reader says in place of the role: `carousel` here, `slide` on each of\nthem.\n\n`aria-roledescription` is author-localized by definition - it overrides the name assistive\ntechnology has for a role, in whatever language that technology had it in, so \"the value\nshould be translated when a page is localized\"\n([MDN](https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Reference/Attributes/aria-roledescription)).\nEnglish baked into the element is a Serbian page whose slides announce themselves in\nEnglish, and there is nothing the page can do about it.\n\nWhitespace is refused rather than written, which the same page asks for: the value must be\n\"not empty and contains more than just whitespace characters\". Honouring `\" \"` would\noverride the role announcement with nothing at all, so a reader stops hearing \"group\" and\nhears nothing in its place - worse than the English it replaced.",
          "return": {
            "type": {
              "text": "string"
            }
          }
        },
        {
          "kind": "function",
          "name": "currentSlide",
          "parameters": [
            {
              "name": "starts",
              "description": "Each slide's start edge, relative to the scroller's own.",
              "type": {
                "text": "number[]"
              }
            },
            {
              "name": "inset",
              "description": "The row's `scroll-padding` on the start side.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "fallback",
              "description": "The index to keep when there is nothing to measure.",
              "type": {
                "text": "number"
              }
            }
          ],
          "description": "Which slide the carousel is on: the first one that has not gone past the row's start edge.\n\nMeasured against the *snap* edge rather than the scroller's box, which is the only version\nthat survives a row with `scroll-padding` on it. A shelf that bleeds past its text keeps the\ncolumn's inset as padding, and the slide before the current one sits in that padding, still\ntwo thirds on screen - so \"the earliest slide more than half visible\" answers with the slide\nyou just left, the index never advances, and the next button stops doing anything after one\npress. This asks the question the snap points already answer.\n\nA pixel of slack because a fractional layout lands a hair either side of the edge, and the\nlast slide as the fallback for a row scrolled past every start - which is where a row whose\nfinal slides are narrower than the viewport ends up.",
          "return": {
            "type": {
              "text": "number"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<carousel-elemental>` custom element.\n\nA row of slides you scroll through, per the\n[APG Carousel pattern](https://www.w3.org/WAI/ARIA/apg/patterns/carousel/) - previous and\nnext, a picker with one button per slide, and, when it is asked for, automatic rotation\nwith the control that stops it.\n\n**The scroll container is the state.** There is no transform engine, no clones and no\nindex attribute to keep in step with where the row actually is: the slides sit in a\nscroll-snapping scroller, moving means setting `scrollLeft`, and which slide is current is\nwhatever an `IntersectionObserver` says is on screen. That is what buys the\nresponsiveness for nothing - resize the window, change how many slides fit in CSS, or put\nthe whole thing in a container query, and there is no listener to fire and no measurement\nto redo, because nothing was measured in the first place.\n\nLight DOM, no shadow root. Nothing you wrote is moved or wrapped; the element adds the\nroles to your list and its items, and appends the controls - which is also why the\ncontrols are its to write rather than yours. A previous button authored in the markup is\na button that does nothing until the script lands, and this element's promise is the\nother way round: with no script the slides are a scroll-snapping row you swipe, drag the\nscrollbar of, or reach with the keyboard, every slide in the page and in reading order.\n\n**No live region while it scrolls, and that is deliberate.** The APG's example flips\n`aria-live` between `off` and `polite` because there one slide exists at a time and a\nreader who cannot see the swap would otherwise never hear about it. Scrolling, every slide\nis in the DOM, in the accessibility tree and in reading order the whole time, so there is\nnothing to announce and nothing hidden to miss - and no `aria-hidden` on the slides off\nscreen either, which is the bug that puts a focusable link inside a hidden subtree.\n`fade` is the mode where one slide really is all there is, and there the live region comes\nback, off while rotating and polite when it is not, exactly as the pattern asks.\n\n**One gesture, and only in `fade`.** A scrolling row swipes because it is a scroll\ncontainer, and a second answer laid over the browser's would fight it. A stack is not one,\nso there the swipe is read here - touch and pen, never the mouse, which has the buttons and\nthe keyboard and would pay for a drag with the page's text selection and link clicks.\n\nponytail: one axis, horizontal. A vertical carousel is the same code with the block\nproperties, and nothing has asked for one; the refusal is on the page rather than an\n`orientation` attribute nobody sets.",
          "name": "CarouselElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length-percentage>"
              },
              "description": "How wide one slide is. This is how many slides fit: `50%` for two, `33.333%` for three, or any expression - it is the flex basis.",
              "name": "--carousel-elemental-slide-size",
              "default": "100%"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between the slides. Nothing in `fade`, where they are stacked.",
              "name": "--carousel-elemental-gap",
              "default": "0px"
            },
            {
              "type": {
                "text": "<time>"
              },
              "description": "How long the cross-fade takes in `fade`, and how long the box takes to travel between two slides' heights. Zero under reduced motion, whatever this says.",
              "name": "--carousel-elemental-fade",
              "default": "400ms"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between the controls under the row.",
              "name": "--carousel-elemental-controls-gap",
              "default": "0.5rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Diameter of a picker button.",
              "name": "--carousel-elemental-marker-size",
              "default": "1.75rem"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Text and border of the controls.",
              "name": "--carousel-elemental-control",
              "default": "currentcolor"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Border of a control that is not the current slide's.",
              "name": "--carousel-elemental-border",
              "default": "color-mix(in srgb, currentcolor 20%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Control background under the pointer.",
              "name": "--carousel-elemental-hover",
              "default": "color-mix(in srgb, currentcolor 10%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Fill of the picker button for the slide on screen.",
              "name": "--carousel-elemental-current",
              "default": "currentcolor"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Fill behind the rotation control - the one control drawn over a slide rather than over the page, which is why it is opaque and does not follow `currentcolor`.",
              "name": "--carousel-elemental-chip",
              "default": "Canvas"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The rotation control's foreground under the pointer: its icon, and the countdown ring with it.",
              "name": "--carousel-elemental-rotate-hover-color",
              "default": "CanvasText"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "How thick the rotation control's countdown ring is. A hairline is a countdown nobody reads at a glance.",
              "name": "--carousel-elemental-ring",
              "default": "3px"
            },
            {
              "type": {
                "text": "<length-percentage>"
              },
              "description": "Corner radius of the controls.",
              "name": "--carousel-elemental-radius",
              "default": "999px"
            }
          ],
          "slots": [
            {
              "description": "One `<ul>`, `<ol>` or `<menu>` of `<li>` slides.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "scroller",
              "description": "The scroller: the first list in the element. A carousel inside a slide keeps its own.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "slides",
              "description": "The slides, in order. What the list holds, so a list inside a slide is not one.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "markers",
              "description": "The picker buttons, in slide order.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "fade",
              "description": "Cross-fade in place rather than scroll a row.\n\nThe one mode where the scroller is not the state: stacked slides have nothing to scroll,\nso the index is what this element holds and the stylesheet reads through\n`data-carousel-current`. Everything above it - the controls, the picker, the rotation,\nthe events - is the same code either way."
            },
            {
              "kind": "field",
              "name": "autoplay",
              "description": "Rotate on a timer. Reflected, so `[autoplay]` is a styling hook too."
            },
            {
              "kind": "field",
              "name": "interval",
              "description": "Milliseconds between slides while rotating."
            },
            {
              "kind": "method",
              "name": "strip",
              "description": "Take the pattern back off, leaving the markup the page wrote: a list.\n\nTwo callers, which are the same event approached from opposite sides - a carousel leaving\nthe document, and one whose page has taken its slides away. Everything written comes back\noff in both, because a `role=\"group\"` with `aria-roledescription=\"slide\"` on a row nothing\nis driving is a carousel announced to a screen reader that no longer has controls, and a\nscroller left with a tab stop is a stop onto nothing."
            },
            {
              "kind": "method",
              "name": "wire",
              "description": "Read the markup and put the pattern on it - the roles, the controls, the observer that\nwatches the row - then push the current state onto the controls.\n\nPublic and idempotent, because the slides are the page's to change: add one, remove one,\nreorder them, and this is the one call that says so. Nothing observes the markup on the\nelement's behalf, which would be a `MutationObserver` running on every page that never\ntouches its slides to save this one line on the pages that do."
            },
            {
              "kind": "method",
              "name": "writeControls",
              "description": "Write the controls, or write them again after the slides changed.\n\nThe rotation control is a child of the element rather than of the control bar, and it is\nfirst: the APG asks for it at the head of the tab sequence inside the carousel, so that\na reader who lands in a moving carousel can stop it before reading anything. Putting it\nin the bar under the row and moving it visually would be a tab order that disagrees with\nthe page, which is the trade this project does not make - so it is drawn where it sits,\nover the top corner of the row."
            },
            {
              "kind": "method",
              "name": "control",
              "parameters": [
                {
                  "name": "flag"
                },
                {
                  "name": "label"
                },
                {
                  "name": "id"
                }
              ],
              "description": "One control button: named, typed, and pointed at the row it drives."
            },
            {
              "kind": "method",
              "name": "removeControls",
              "description": "Take the controls back out, so `wire()` and `disconnectedCallback` leave the markup as\nthey found it."
            },
            {
              "kind": "method",
              "name": "labelRotation",
              "description": "The rotation control's name says what pressing it will do, and it has no `aria-pressed`.\nThat is the APG's own answer for this button: a name that changes and a state that does\nnot, rather than both, which would have a screen reader read the two against each other."
            },
            {
              "kind": "method",
              "name": "readIndex",
              "description": "Where the row is now, read off the layout.\n\nCalled from both of the things that can move it, which is not belt and braces: the\nobserver fires when a slide crosses one of its thresholds, and a press that shifts the row\nby less than that - the last step into a clamped end, or any step at all on a row of wide\nslides - crosses nothing and would leave the index behind. A stale index is not a cosmetic\nproblem: the next press is measured from it, so previous appears to work once and then do\nnothing at all, and next jumps several slides at a time."
            },
            {
              "kind": "method",
              "name": "onIntersect",
              "description": "The observer's whole job: notice that the layout changed and re-read it.\n\nA resize, a container query flipping how many slides fit, a webfont landing - none of\nthem fire a scroll event, and this is the callback that would otherwise have to be a\nresize listener. The `scroll-padding` is re-read here rather than on every scroll,\nbecause a media query is the only thing that changes it and this is where layout changes\narrive."
            },
            {
              "kind": "method",
              "name": "onScroll",
              "description": "Scrolled: the edges are the scroller's to report, and they change without the set of\nvisible slides changing."
            },
            {
              "kind": "method",
              "name": "arrived",
              "description": "The programmatic scroll is over: the scroller speaks for itself again."
            },
            {
              "kind": "method",
              "name": "apply",
              "parameters": [
                {
                  "name": "at"
                }
              ],
              "description": "Push the current slide onto the picker and the slides, and tell the page when it moved."
            },
            {
              "kind": "method",
              "name": "applyEdges",
              "description": "Whether there is anywhere left to go, either way - onto the two buttons as `aria-disabled`\nand onto the element as a styling hook.\n\nBoth at once is a row short enough to fit, and both buttons go dim: a carousel with\nnothing to scroll is a list, and two live buttons over a list that cannot move is the\nkind of thing that gets pressed twice and then distrusted."
            },
            {
              "kind": "method",
              "name": "resize",
              "parameters": [
                {
                  "name": "from",
                  "description": "The height the stack had before the current marker moved.",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "Carry the stack's height from the slide that left to the slide that arrived.\n\nA transition needs two numbers and `auto` is neither of them, so the height the box had a\nmoment ago is written back on, read once so the browser takes it as a start, and replaced\nwith the height it is going to. `transitionend` hands the box back to `auto` as soon as it\nlands - which is what leaves a resize, a font arriving or an image that finally loaded to\nthe layout, instead of to a pixel count taken before any of them happened.\n\nMeasured off the scroller rather than off the slide, so whatever padding or `box-sizing`\nthe page gave the list is inside both numbers instead of neither."
            },
            {
              "kind": "method",
              "name": "to",
              "parameters": [
                {
                  "name": "at"
                }
              ],
              "description": "Show a slide: scroll it to the start of the row, or cross-fade to it.\n\nThe delta comes from the two boxes rather than from `offsetLeft`, which is measured\nagainst whichever ancestor happens to be positioned and is a different number for the\nslide and the scroller as soon as a page positions one of them. Assigning `scrollLeft`\nrather than calling `scrollTo` leaves the smoothness to CSS, where the reduced-motion\nquery already lives - see `index.scss`."
            },
            {
              "kind": "method",
              "name": "next",
              "description": "One on, stopping at the end - where the button is dim and says so."
            },
            {
              "kind": "method",
              "name": "previous"
            },
            {
              "kind": "method",
              "name": "advance",
              "description": "One on for the rotation, which is the only thing here that wraps.\n\nA carousel that rotates to its last slide and stops is a carousel that quietly died, so\nthe end goes back to the beginning. The buttons do not, because they are dim there and a\ncontrol that looks spent must not still act."
            },
            {
              "kind": "method",
              "name": "play",
              "parameters": [
                {
                  "name": "pinned"
                }
              ],
              "description": "Start rotating.\n\n`pinned` is what the rotation control sets, and it is the APG's rule that a rotation the\nreader asked for by hand is not stopped again by a stray mouse crossing the row: hover\nand focus are ignored until the same button stops it."
            },
            {
              "kind": "method",
              "name": "pause"
            },
            {
              "kind": "method",
              "name": "applyLive",
              "description": "The live region, and only in `fade`.\n\nStacked, one slide is all there is, so a reader who cannot see the cross-fade is owed the\nannouncement - `polite` when the slides move because somebody pressed something. `off`\nwhile it rotates, which is the half people leave out: a carousel announcing itself every\nfive seconds interrupts whatever else is being read, forever.\n\nScrolling there is no region at all. Every slide is in the tree the whole time, so there\nis nothing to announce that the reader cannot already reach."
            },
            {
              "kind": "method",
              "name": "tick",
              "description": "Start the clock, and say so on the element.\n\n`data-carousel-rotating` is the timer and not `rotating`: the two part company every time\na pointer crosses the row, where the rotation is held but the button still says `Stop`.\nA theme drawing a countdown off `rotating` would be one that keeps counting while nothing\nis moving - so the hook is written where the clock is, out of the same two calls that\nstart and stop it, and cannot say otherwise. `--carousel-elemental-tick` goes with it for\nthe same reason: an animation is only honest at the length of the interval it is drawing,\nand only this knows what that is."
            },
            {
              "kind": "method",
              "name": "clearTimer"
            },
            {
              "kind": "method",
              "name": "onSwipe",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "A finger has come up on a stacked slide, having travelled far enough across to have meant\nit. The ends hold, exactly as they do for the buttons: the arrow is dim there, and a\ngesture that still moved would be the carousel disagreeing with its own controls.\n\nA gesture more down the page than across it is the page scrolling and never arrives here -\n`swipe` reports the axis it travelled furthest along, and only that one."
            },
            {
              "kind": "method",
              "name": "unswipe",
              "description": "Every swipe listener comes back off, and the half-finished gesture with it."
            },
            {
              "kind": "method",
              "name": "unpin",
              "description": "Give the stack its height back, and stop listening for the swap that pinned it. The\ninline height is this element's own writing, so leaving one behind is leaving the list a\nsize the page never asked for."
            },
            {
              "kind": "method",
              "name": "onHeightEnd",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "The end of the height the swap pinned - or the end of any hope of one."
            },
            {
              "kind": "method",
              "name": "suspend",
              "description": "Rotation held while the pointer or the focus is in the carousel - still rotating as far\nas the button's name is concerned, because it will be again on the way out."
            },
            {
              "kind": "method",
              "name": "resume"
            },
            {
              "kind": "method",
              "name": "onHoverIn"
            },
            {
              "kind": "method",
              "name": "onHoverOut"
            },
            {
              "kind": "method",
              "name": "onFocusIn"
            },
            {
              "kind": "method",
              "name": "onFocusOut",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "Focus moving between two controls is focus that never left."
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            }
          ],
          "events": [
            {
              "name": "carousel-change",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.index` is the slide now on screen, `detail.slide` the element itself."
            }
          ],
          "attributes": [
            {
              "name": "autoplay",
              "type": {
                "text": "boolean"
              },
              "description": "Rotate on a timer, and write the control that stops it. Ignored at upgrade when the reader asked for reduced motion - the control is still there to start it.",
              "default": "false"
            },
            {
              "name": "interval",
              "type": {
                "text": "number"
              },
              "description": "Milliseconds between slides while rotating. Under 1000 is treated as 1000.",
              "default": "5000"
            },
            {
              "name": "fade",
              "type": {
                "text": "boolean"
              },
              "description": "Cross-fade one slide at a time in place, instead of scrolling a row. The slides stack, the box takes the height of the slide showing - animated, over the same duration as the fade - and a touch swipe across them moves one slide.",
              "default": "false"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The previous button's accessible name.",
              "name": "prev-text",
              "default": "Previous slide"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The next button's accessible name.",
              "name": "next-text",
              "default": "Next slide"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The rotation control's accessible name while stopped.",
              "name": "play-text",
              "default": "Start slide rotation"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The rotation control's accessible name while rotating.",
              "name": "pause-text",
              "default": "Stop slide rotation"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The word in front of the number on a picker button. `Slide 3`. Holding `{n}` it says where the number goes instead: `{n}. dia`.",
              "name": "slide-text",
              "default": "Slide"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The name a slide gets where the markup gave it none. `{n}` is its number, `{total}` how many there are.",
              "name": "position-text",
              "default": "{n} of {total}"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The word a screen reader says for the element instead of \"group\". Whitespace is refused, since a role announcement overridden with nothing is worse than one in the wrong language.",
              "name": "roledescription-text",
              "default": "carousel"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The same for each slide, instead of \"group\". Whitespace is refused for the same reason.",
              "name": "slide-roledescription-text",
              "default": "slide"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The picker group's accessible name.",
              "name": "picker-text",
              "default": "Choose slide to display"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "carousel-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "rotationInterval",
          "declaration": {
            "name": "rotationInterval",
            "module": "src/elementals/carousel/index.js"
          }
        },
        {
          "kind": "js",
          "name": "stepSlide",
          "declaration": {
            "name": "stepSlide",
            "module": "src/elementals/carousel/index.js"
          }
        },
        {
          "kind": "js",
          "name": "swipeStep",
          "declaration": {
            "name": "swipeStep",
            "module": "src/elementals/carousel/index.js"
          }
        },
        {
          "kind": "js",
          "name": "scrollEdges",
          "declaration": {
            "name": "scrollEdges",
            "module": "src/elementals/carousel/index.js"
          }
        },
        {
          "kind": "js",
          "name": "startInset",
          "declaration": {
            "name": "startInset",
            "module": "src/elementals/carousel/index.js"
          }
        },
        {
          "kind": "js",
          "name": "swapHeight",
          "declaration": {
            "name": "swapHeight",
            "module": "src/elementals/carousel/index.js"
          }
        },
        {
          "kind": "js",
          "name": "rotationHeld",
          "declaration": {
            "name": "rotationHeld",
            "module": "src/elementals/carousel/index.js"
          }
        },
        {
          "kind": "js",
          "name": "slideName",
          "declaration": {
            "name": "slideName",
            "module": "src/elementals/carousel/index.js"
          }
        },
        {
          "kind": "js",
          "name": "markerName",
          "declaration": {
            "name": "markerName",
            "module": "src/elementals/carousel/index.js"
          }
        },
        {
          "kind": "js",
          "name": "roleDescription",
          "declaration": {
            "name": "roleDescription",
            "module": "src/elementals/carousel/index.js"
          }
        },
        {
          "kind": "js",
          "name": "currentSlide",
          "declaration": {
            "name": "currentSlide",
            "module": "src/elementals/carousel/index.js"
          }
        },
        {
          "kind": "js",
          "name": "CarouselElemental",
          "declaration": {
            "name": "CarouselElemental",
            "module": "src/elementals/carousel/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/checkbox-group/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "classify",
          "parameters": [
            {
              "name": "states"
            }
          ],
          "description": "What the parent checkbox has to say about its children: all of them, none of them, or\nsome of them.\n\nAn empty group is `none` rather than a third answer. There is nothing to be mixed\nabout, and the element does not upgrade without children anyway."
        },
        {
          "kind": "function",
          "name": "cycle",
          "parameters": [
            {
              "name": "states",
              "description": "What the children are now.",
              "type": {
                "text": "boolean[]"
              }
            },
            {
              "name": "memory",
              "description": "The combination they were in when the group was last mixed.",
              "type": {
                "text": "boolean[]|null"
              }
            }
          ],
          "description": "Where one press of the parent takes the children:\nmixed → all on → all off → back to the mixed combination they were last in.\n\nThat last step is the [APG's\ncycle](https://www.w3.org/WAI/ARIA/apg/patterns/checkbox/examples/checkbox-mixed/) and\nthe reason to have a tri-state parent at all: two ticks out of twenty are not destroyed\nby one press, they are one more press away from coming back.\n\nThe memory is skipped when there is nothing in it worth stopping at - absent, all on,\nall off, or taken when the group was a different size, which happens when options have\nbeen added or removed since. Restoring a stale combination would put ticks against\nwhatever now sits at those positions, and a cycle whose third step is\nindistinguishable from its second is a step the reader presses through for nothing. So\nthe cycle is three steps while there is something to go back to and two while there is\nnot.",
          "return": {
            "type": {
              "text": "boolean[]"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<checkbox-group-elemental>` custom element.\n\nA \"select all\" checkbox over the checkboxes it stands for, per the\n[APG Checkbox (Mixed-State) pattern](https://www.w3.org/WAI/ARIA/apg/patterns/checkbox/):\nticked when all of them are, empty when none are, and showing the dash when it is some\nof them.\n\nThe dash is the whole reason this exists. It is `HTMLInputElement.indeterminate`, a\nproperty with **no HTML attribute behind it** - no server, template or static page can\nrender a checkbox in that state, so the one line of markup that would have said it has\nto be script, in every project, every time. It is also purely visual: what a checkbox\nsubmits is decided by `checked` alone, indeterminate or not, which is why the parent is\na control the form should not be given a `name` for.\n\nEvery checkbox here is a real `<input type=\"checkbox\">` and stays one, so the label\nassociation, the focus ring, `Space`, `disabled`, submission under each child's own\n`name`, reset and restore are the browser's. A native checkbox with `indeterminate` set\nis already announced as mixed, so there is no `role=\"checkbox\"` and no `aria-checked`\nwritten anywhere - which is where this parts company with the APG's own example, and\ndeliberately: that one builds the parent as a `<div role=\"checkbox\">` to demonstrate the\nrole, and pays for it with everything in this paragraph.\n\nWhat the element writes is two properties on the parent and `data-state` on itself.\nNothing is moved, wrapped, or given an attribute it did not have.\n\n**One level, not a tree.** The pattern is a parent over a flat set of checkboxes, and\nthat is all this is: a group nested inside another is a separate group, keeps its own\ncheckboxes, and neither reads nor is read by the one around it. A tree of them is a\ndifferent control - the cycle alone stops being answerable, since restoring a branch's\nlast mixed combination and its parent's are two different restores of the same boxes.\nWrite the checkboxes flat, one group per parent.\n\nponytail: no `aria-controls` on the parent listing the children. The APG's example has\nit; support for it is thin, and it would mean generating an `id` for every checkbox in\nthe author's markup. Add it if a screen reader turns out to want it.\n\nDegrades by not being offered: with no script the parent would be a checkbox that ticks\nitself and commands nothing. The stylesheet hides it until the element upgrades where it\nis a direct child, and `hidden` in the markup does the same at any depth - a select-all\nin a table header, which is where most of them are, has no selector that can reach it,\nbecause CSS cannot say \"the first checkbox anywhere below me\". Either way what is left is\nthe plain list of checkboxes it was standing in front of, all of them working.",
          "name": "CheckboxGroupElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Box size, both axes. Shared with any `.checkbox-elemental` on the page.",
              "name": "--checkbox-elemental-size",
              "default": "1.15em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Box corners.",
              "name": "--checkbox-elemental-radius",
              "default": "0.25em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Box border.",
              "name": "--checkbox-elemental-border-width",
              "default": "1.5px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Box border, unticked.",
              "name": "--checkbox-elemental-border-color",
              "default": "color-mix(in srgb, currentcolor 45%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Box fill once it is ticked or mixed.",
              "name": "--checkbox-elemental-fill",
              "default": "currentcolor"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The tick and the dash. The page's own background, so re-point it on a card.",
              "name": "--checkbox-elemental-mark",
              "default": "Canvas"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between a box and its label text.",
              "name": "--checkbox-elemental-gap",
              "default": "0.6em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "How far the children sit in from the parent. This one is the group's alone.",
              "name": "--checkbox-group-elemental-indent",
              "default": "1.75em"
            }
          ],
          "slots": [
            {
              "description": "The parent `<input type=\"checkbox\">` in its `<label>`, and the children's checkboxes under it.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "parent",
              "description": "The \"select all\". The first checkbox in the element, in document order, which is where\nit has to be for the reader too - a heading for a list comes before the list.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "checkboxes",
              "description": "The checkboxes the parent stands for: every one below it, minus a nested group's own.\n\nNot `children`, which is `Element`'s own and means every child node that is an\nelement. Shadowing it would leave this element lying to any code that walks the DOM\ngenerically - including the browser's own devtools.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "boxes",
              "description": "Every checkbox this element owns. A nested group keeps its own, parent included."
            },
            {
              "kind": "method",
              "name": "movable",
              "description": "The checkboxes the parent can actually move, which is the set it speaks for.\n\nA disabled one is not in it, and that decides both halves at once. It cannot be\ncounted, because a group holding one disabled and unticked box could never reach \"all\"\n- every press would compute \"some\", set everything it is allowed to, change nothing,\nand the cycle would be stuck on the step it was already on. And it cannot be moved,\nbecause a checkbox the reader could not have clicked is not one the parent gets to\nclick for them. So the parent's tick means \"everything selectable is selected\", which\nis the only reading under which pressing it does what it says."
            },
            {
              "kind": "field",
              "name": "state",
              "description": "`all`, `some` or `none` - the same word the element writes onto itself.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "apply",
              "description": "Read the children and put what they say onto the parent. Public because that is the\none thing no event announces: add or remove a checkbox and this is the call that\ncatches up.\n\nThe memory is taken here rather than at the click, so a combination the reader built\nby hand - ticking two of twenty themselves - is the one that comes back. Any way of\narriving at mixed is the group being mixed."
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "A press of the parent. `click` and not `keydown`, because `Space` on a checkbox *is* a\nclick - there is no keyboard here that the platform has not already written.\n\nThe children are the source of truth, so the cycle is read off them and not off the\nparent, whose `checked` the browser has already flipped and whose `indeterminate` it\nhas already cleared by the time this runs. `apply` puts both back."
            },
            {
              "kind": "method",
              "name": "onChange",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "A child was ticked, so the parent has something new to say."
            },
            {
              "kind": "method",
              "name": "onReset",
              "description": "A form is only put back to its defaults once the `reset` event has been dispatched,\nso the checkboxes are read on the next task rather than in the handler."
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "checkbox-group-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "classify",
          "declaration": {
            "name": "classify",
            "module": "src/elementals/checkbox-group/index.js"
          }
        },
        {
          "kind": "js",
          "name": "cycle",
          "declaration": {
            "name": "cycle",
            "module": "src/elementals/checkbox-group/index.js"
          }
        },
        {
          "kind": "js",
          "name": "CheckboxGroupElemental",
          "declaration": {
            "name": "CheckboxGroupElemental",
            "module": "src/elementals/checkbox-group/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/combobox/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "flipsUp",
          "parameters": [
            {
              "name": "field",
              "description": "Rect of the field, in viewport coordinates.",
              "type": {
                "text": "DOMRect|object"
              }
            },
            {
              "name": "panelHeight",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "viewportHeight",
              "type": {
                "text": "number"
              }
            }
          ],
          "description": "Whether the popup opens upwards: only when it does not fit below and there is more\nroom above.\n\nBelow wins ties and wins when neither side fits, because the list scrolls - \"nowhere\nit fits\" is a choice between two cramped corners rather than a failure, and the corner\nthe reader expects is the one under the field."
        },
        {
          "kind": "function",
          "name": "focusAfterRemoval",
          "parameters": [
            {
              "name": "count"
            },
            {
              "name": "index"
            }
          ],
          "description": "Which chip takes focus after the one at `index` is removed, or `-1` for the input.\n\n`count` is how many there were before the removal, so every index below the last one\nhands focus to whatever slid into its place, and removing the last one has nothing to\nslide - focus goes back to the input rather than to the document body, which is where\na keyboard reader loses the control entirely."
        },
        {
          "kind": "function",
          "name": "removeName",
          "parameters": [
            {
              "name": "verb",
              "description": "`remove-text`, with or without `{label}` in it.",
              "type": {
                "text": "string"
              }
            },
            {
              "name": "label",
              "description": "The option's own text.",
              "type": {
                "text": "string"
              }
            }
          ],
          "description": "A chip remove button's accessible name: `remove-text` with the option it would remove.\n\nVerb then noun is English's order, and a `remove-text` the element concatenates in front\nof the label can only ever produce that one. German puts the verb last - `React\nentfernen` - so a page translating this had a verb and no way to move it. A `remove-text`\nholding `{label}` says where the option goes and the element only fills it in; one that\ndoes not gets the label appended, which is what every `remove-text` written before this\ndid.\n\nThe replacement is a function rather than the label itself, because `String.replace`\nreads `$&` and `$'` in a plain replacement as references back into the match - and the\nlabel is `<option>` text, which is whatever the page's data had in it.",
          "return": {
            "type": {
              "text": "string"
            }
          }
        },
        {
          "kind": "function",
          "name": "offersCustom",
          "parameters": [
            {
              "name": "query",
              "description": "What is in the field.",
              "type": {
                "text": "string"
              }
            },
            {
              "name": "texts",
              "description": "The text of every option already there.",
              "type": {
                "text": "string[]"
              }
            }
          ],
          "description": "Whether what has been typed is a value the list does not already hold - which is the only\nquestion `custom-values` ever asks.\n\nTrimmed and case-folded on both sides, because every answer the other way is a\nnear-duplicate the reader did not mean to make: `react` offered beside `React`, or\n`React ` beside `React`, sitting one row above the real one in every list from then on.\nFolding costs the page the ability to hold two options whose texts differ only in case,\nwhich is a list that was already unreadable.\n\nAn empty query is not a value however much whitespace it holds, and an empty list offers\neverything - which is a tag input with no suggestions behind it, and the reason this is one\nattribute rather than a second element.",
          "return": {
            "type": {
              "text": "boolean"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<combobox-elemental>` custom element.\n\nA `<select>` given a text field to search it with, per the\n[APG Combobox pattern](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) - the\nsearchable, tag-holding select every application ends up needing and the platform\nstill has no answer for.\n\nThe gap is narrow and worth naming, because most of what these libraries do is now\nnative. `appearance: base-select` styles the button, the picker and every `<option>`\nwith ordinary CSS, which is the half people used to reach for select2 to get. What it\ndoes not do is **filter**: there is no typing your way down a list of four hundred\ncities, in any browser, with or without the new styling. That is what this element is,\nand it is why it is not simply a stylesheet.\n\nThe `<select>` stays, and stays the control. It holds the value, submits under its own\n`name`, resets, restores, honours `required` and `disabled`, and goes down with a\n`<fieldset disabled>` - none of which is written here, because a second element\nmirroring the state is a second element that can disagree with it. The field, the\nlistbox and the chips are a view of it; every pick sets `option.selected` and lets the\n`<select>` fire the `change` that was going to be listened for anyway.\n\n`multiple` on the `<select>` is what makes it multi-select. The chips, the\nremove buttons and `Backspace` on an empty field come with it - and the caret goes,\nsince a caret is the mark of a control holding one value out of a list and a field full\nof tags has already said what this one holds. It is the one part of\nthis element with no APG example behind it - the pattern's six are all single-select.\nWhat is written here follows the pattern where it speaks (`aria-multiselectable`,\n`aria-selected` on every option rather than only the chosen one, a listbox that stays\nopen across picks) and is plain buttons where it does not.\n\nLight DOM, no shadow root. This one *does* build markup - a field, a listbox and a\nchip per selection - which the elements wrapping native widgets do not have to. That\nmarkup is the public API and is documented as such.\n\nDegrades honestly: with no script the `<select>` is a `<select>`, which is the whole\ncontrol minus the searching. Nothing is hidden until there is something to hide it\nbehind.\n\nponytail: the option list is built once, at upgrade, and filtered by hiding. Replace\nthe `<option>`s at runtime and call `apply()`; wire a `MutationObserver` if that stops\nbeing rare.",
          "name": "ComboboxElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Corners of the field and the popup.",
              "name": "--combobox-elemental-radius",
              "default": "0.375rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "The one padding unit: inside the field, before the caret, and down the side of every option - and nowhere else, so the field's text and the popup's line up.",
              "name": "--combobox-elemental-inset",
              "default": "0.5rem"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "What the field and the popup are painted on. The page's own background, so re-point it on a card.",
              "name": "--combobox-elemental-surface",
              "default": "Canvas"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The rim around the field and the popup.",
              "name": "--combobox-elemental-border",
              "default": "color-mix(in srgb, currentcolor 30%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The option the cursor is on - where Enter would land, and what the pointer moves.",
              "name": "--combobox-elemental-active",
              "default": "color-mix(in srgb, currentcolor 12%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The options already chosen, which is a different question from where the cursor is.",
              "name": "--combobox-elemental-selected",
              "default": "color-mix(in srgb, currentcolor 5%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Chip fill.",
              "name": "--combobox-elemental-chip",
              "default": "color-mix(in srgb, currentcolor 12%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The field's rim and the message under it once the browser has refused to submit.",
              "name": "--combobox-elemental-invalid",
              "default": "color-mix(in srgb, currentcolor 35%, #e5484d)"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "How tall the popup gets before it scrolls.",
              "name": "--combobox-elemental-max-height",
              "default": "15rem"
            }
          ],
          "slots": [
            {
              "description": "One `<select>`, with the `<option>`s and `<optgroup>`s you would have written anyway.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "select",
              "description": "The control. Direct child, so a `<select>` inside a popover of your own is not\nmistaken for it.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "open",
              "description": "Whether the popup is showing. Reflected, so `[open]` is a styling hook too."
            },
            {
              "kind": "field",
              "name": "multiple",
              "description": "Whether this holds more than one value, which is the `<select>`'s own `multiple`.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "disabled",
              "description": "Disabled by its own attribute or by a `<fieldset disabled>` above it, which\n`:disabled` answers for in one selector.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "placeholder",
              "description": "What the empty field says. A single select usually has the answer in its own markup\nalready - the `<option value=\"\">Choose a fruit</option>` at the top is a placeholder\nthat has been written down as an option since forms had options."
            },
            {
              "kind": "field",
              "name": "emptyText",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "removeText",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "value",
              "description": "The `<select>`'s value, so a single select reads as one string and a multiple one\nas the first of its selections - exactly as the native property does."
            },
            {
              "kind": "field",
              "name": "values",
              "description": "Every selected value, in document order. The one a multiple select has no property\nfor.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "customValues",
              "description": "Whether a value the list does not hold may be typed into it.\n\nOff by default, and it has to be: a `<select>` is a closed set of answers, and an\nelement that quietly widened one would be answering a question the markup did not ask.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "addText",
              "description": "What the popup's add row says, with `{label}` standing in for what was typed.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "build",
              "description": "Build the view: a field holding the chips, the text input and the popup indicator,\nand the listbox under it.\n\nInserted **before** the `<select>` rather than after, because a `<label>` wrapping\nthis element names the first labelable thing inside it, and the `<select>` staying\nfirst would leave the visible field anonymous while the hidden control wore the name."
            },
            {
              "kind": "method",
              "name": "teardown",
              "description": "Put the markup back the way it arrived: the view goes, the `<select>` returns to\nbeing an ordinary, focusable, announced control. An element that is no longer here\nleaves nothing behind that only it knew how to drive."
            },
            {
              "kind": "method",
              "name": "apply",
              "description": "Read the `<select>` again: rebuild the options and redraw everything from them.\nPublic because that is the one thing no event announces - replace the `<option>`s\nfrom script and this is the call that catches up."
            },
            {
              "kind": "method",
              "name": "addOption",
              "parameters": [
                {
                  "name": "option"
                },
                {
                  "name": "parent"
                }
              ]
            },
            {
              "kind": "method",
              "name": "sync",
              "description": "Push the `<select>` onto the view: the chips, the selected states, the field's text\nand whether any of it can be touched.\n\nThe field's text is the selection and nothing else, so a query typed and abandoned -\nby tabbing away, by Escape, by picking something - never survives as a label for a\nvalue it does not name."
            },
            {
              "kind": "method",
              "name": "onInvalid",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "The browser has refused to submit. Its own bubble is dropped and the message kept,\nbecause the bubble would be pointing at the `<select>` - which is transparent,\n`aria-hidden`, and about to take focus away from the field the reader has to fill in.\n\nThe text is the browser's own `validationMessage`, so it arrives already translated\ninto the reader's language and says what the platform would have said."
            },
            {
              "kind": "method",
              "name": "clearError"
            },
            {
              "kind": "method",
              "name": "filter",
              "description": "Hide the options the query does not answer, and the groups that are left holding\nnone of them."
            },
            {
              "kind": "method",
              "name": "navigable",
              "description": "The options an arrow key can reach: on screen, and not disabled. The add row is one of\nthem while it is showing, which is what makes it reachable by keyboard at all."
            },
            {
              "kind": "method",
              "name": "activeIndex",
              "description": "Where the popup's own cursor is - `aria-activedescendant`, read back as an index\ninto the list the arrows walk."
            },
            {
              "kind": "method",
              "name": "setActive",
              "parameters": [
                {
                  "name": "index"
                }
              ],
              "description": "Move the popup's cursor. Focus itself never moves - it stays in the field, which is\nwhat `aria-activedescendant` is for and what lets typing carry on narrowing the list\nwhile an option is \"focused\"."
            },
            {
              "kind": "method",
              "name": "place",
              "description": "Point the popup at whichever side of the field it fits on, and write that on it for\nthe stylesheet to act on - the measuring is the element's, the positioning is CSS's."
            },
            {
              "kind": "method",
              "name": "applyOpen",
              "description": "Show or hide the popup, and put the cursor somewhere sensible inside it."
            },
            {
              "kind": "method",
              "name": "pick",
              "parameters": [
                {
                  "name": "pair"
                }
              ],
              "description": "Choose, or in a multiple select un-choose, one option."
            },
            {
              "kind": "method",
              "name": "addValue",
              "description": "Turn what has been typed into an `<option>` of the `<select>`, and choose it.\n\nA real `<option>`, appended to the control, because the control is what submits - a\nvalue held anywhere else is a value the form does not have. From that moment it is an\noption like the ones the page wrote: it filters, it gets a chip, its remove button is\nnamed the same way, and picking it again toggles it. That is the whole reason this is an\nattribute on the combobox and not a second element - the machinery already existed.\n\nRebuilding the view rather than splicing one row into it: `apply()` is what keeps the\n`<option>` order, the `id`s and the pair list agreeing with the `<select>`, and one\nrebuild on a keystroke the reader made deliberately is cheaper than a second code path\nthat can drift from the first."
            },
            {
              "kind": "method",
              "name": "removeAt",
              "parameters": [
                {
                  "name": "index"
                }
              ],
              "description": "Drop the `index`th selection, and put focus somewhere that still exists."
            },
            {
              "kind": "method",
              "name": "emit",
              "description": "Tell the page, in the events it is already listening for. The `<select>` is the\ncontrol, so `input` and `change` fire on it and in that order, exactly as they do\nwhen a reader uses a native one - which is why this element has no event of its own."
            },
            {
              "kind": "method",
              "name": "onSelectChange"
            },
            {
              "kind": "method",
              "name": "onReset",
              "description": "A form is only put back to its defaults after the `reset` event has been\ndispatched, so the options are read on the next task rather than in the handler."
            },
            {
              "kind": "method",
              "name": "onInput"
            },
            {
              "kind": "method",
              "name": "onMouseDown",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "A pointer press inside the popup would blur the field before the click landed, and a\ncombobox whose field loses focus is one whose popup has just closed. The press is\ncancelled instead; the click that follows still arrives.\n\n`mousedown` and not `pointerdown`, which reads as the modern spelling and breaks\ntouch: cancelling `pointerdown` suppresses the compatibility mouse events, and on iOS\nthe tap's `click` is the last of that same synthesised run - so the press keeps the\nfield's focus and takes the tap with it. The compatibility `mousedown` arrives before\nthe focus change and before `click`, so cancelling that one costs nothing."
            },
            {
              "kind": "method",
              "name": "onPointerOver",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "Pointing at an option moves the popup's cursor onto it, so the mouse and the arrow\nkeys drive the same one thing. Without this the pointer lights up one option while\n`aria-activedescendant` sits on another, and two options look chosen at once - with\nEnter belonging to the one the reader is not pointing at."
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onKeyDown",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onFocusOut",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onDocumentClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            }
          ],
          "attributes": [
            {
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the popup is showing. Reflected, so `[open]` is a styling hook.",
              "default": "false"
            },
            {
              "name": "placeholder",
              "type": {
                "text": "string"
              },
              "description": "The field's placeholder. Single select falls back to the label of the option whose value is empty."
            },
            {
              "name": "empty-text",
              "type": {
                "text": "string"
              },
              "description": "What the popup says when the query matches nothing.",
              "default": "No matches"
            },
            {
              "name": "remove-text",
              "type": {
                "text": "string"
              },
              "description": "The verb in a chip's remove button, in front of the option's label. Holding `{label}` it says where the label goes instead: `{label} entfernen`.",
              "default": "Remove"
            },
            {
              "name": "custom-values",
              "type": {
                "text": "boolean"
              },
              "description": "Let a value the `<select>` does not hold be typed in. The popup offers an add row for anything not already there; taking it appends a real `<option>` and chooses it. With `multiple` and an empty `<select>`, this is a tag input.",
              "default": "false"
            },
            {
              "name": "add-text",
              "type": {
                "text": "string"
              },
              "description": "What the add row says, with `{label}` standing in for what was typed. Same convention as `remove-text`.",
              "default": "Add {label}"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "combobox-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "flipsUp",
          "declaration": {
            "name": "flipsUp",
            "module": "src/elementals/combobox/index.js"
          }
        },
        {
          "kind": "js",
          "name": "focusAfterRemoval",
          "declaration": {
            "name": "focusAfterRemoval",
            "module": "src/elementals/combobox/index.js"
          }
        },
        {
          "kind": "js",
          "name": "removeName",
          "declaration": {
            "name": "removeName",
            "module": "src/elementals/combobox/index.js"
          }
        },
        {
          "kind": "js",
          "name": "offersCustom",
          "declaration": {
            "name": "offersCustom",
            "module": "src/elementals/combobox/index.js"
          }
        },
        {
          "kind": "js",
          "name": "ComboboxElemental",
          "declaration": {
            "name": "ComboboxElemental",
            "module": "src/elementals/combobox/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/copy/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "sourceText",
          "parameters": [
            {
              "name": "target"
            },
            {
              "name": "value"
            }
          ],
          "description": "What goes on the clipboard: the `value` attribute when there is one, otherwise the text\nof the element `for` names.\n\nA field is what is in it - `.value`, not the markup between its tags, which for an\n`<input>` is nothing at all and for a `<textarea>` is only what it loaded with.\n\nThe trimming is one-sided on purpose. A code block written in markdown carries a newline\nbefore its first line and after its last, neither of which is code - and the trailing one\npasted into a terminal runs the command the reader was still reading. Indentation at the\nstart of a line is code, in Python and YAML especially, so nothing there is touched.\n\n`value` is taken exactly as written, empty string included: an author who set it to\nnothing has said there is nothing to copy, and the element reports that as the failure it\nis rather than quietly copying something else instead."
        },
        {
          "kind": "class",
          "description": "`<copy-elemental>` custom element.\n\nA real `<button>` that puts text on the clipboard and says so - out loud as well as on\nscreen.\n\nThere is no APG pattern for this, because there is no widget: it is a button, and a\nbutton is already accessible. What is missing is the half after the click. Every copy\nbutton swaps an icon or shows a tick, and a swapped icon is not an announcement - a\nscreen reader user presses it and is told nothing at all, which is\n[WCAG 2.2 4.1.3 Status Messages](https://www.w3.org/WAI/WCAG22/Understanding/status-changes.html)\nunmet. That gap is this element and the whole of it: the clipboard write, a\n`data-state` for CSS to read, and a live region that says the same thing to the reader\nwho cannot see the icon change.\n\nWhat it does, and nothing more. The button, its label, its focus ring and Enter and\nSpace are the button's - which is the point of using one. There is no tooltip, no\ntimer to configure and no icon: what the button looks like before and after is CSS on\n`[data-state=\"copied\"]`, and the text it announces is two attributes.\n\nLight DOM, no shadow root. One node is added - the live region - because a live region\nhas to be in the document *before* the text lands in it, so it cannot be created at the\nmoment there is something to say.\n\nDegrades honestly: `navigator.clipboard` is absent over plain `http`, and a copy button\nthat cannot copy is a button that lies. The stylesheet keeps it out of reach until the\nelement has upgraded and found the API - so with no script, an insecure page, or nothing\nnamed to copy, there is no button rather than a dead one. Put the text somewhere\nselectable and the reader still has it.\n\nponytail: no `<a href>` special case, no `from=\"el[attr]\"` micro-syntax. Copying\nsomething that is not text on the page is what `value` is for, and one attribute beats a\nselector dialect nobody remembers.",
          "name": "CopyElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "The icon the theme draws, both axes.",
              "name": "--copy-elemental-icon-size",
              "default": "1em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between the icon and the label beside it.",
              "name": "--copy-elemental-gap",
              "default": "0.4em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Above and below the label.",
              "name": "--copy-elemental-padding-block",
              "default": "0.4em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Either side of it.",
              "name": "--copy-elemental-padding-inline",
              "default": "0.6em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Button corners.",
              "name": "--copy-elemental-radius",
              "default": "0.35rem"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "What the button is painted on. The page's own background, so re-point it on a card.",
              "name": "--copy-elemental-surface",
              "default": "Canvas"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The rim around it.",
              "name": "--copy-elemental-border-color",
              "default": "currentcolor at 30%"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Fill under the pointer.",
              "name": "--copy-elemental-hover",
              "default": "currentcolor at 8% over the surface"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Icon, label and rim once it landed.",
              "name": "--copy-elemental-copied-color",
              "default": "currentcolor mixed towards green"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The same three when it did not.",
              "name": "--copy-elemental-error-color",
              "default": "currentcolor mixed towards red"
            },
            {
              "type": {
                "text": "<time>"
              },
              "description": "The cross-fade between those states.",
              "name": "--copy-elemental-duration",
              "default": "150ms"
            }
          ],
          "slots": [
            {
              "description": "The `<button>` that copies, with whatever label or icon you give it.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "button",
              "description": "The `<button>` that copies. Direct child, so a button inside the block being\ncopied - or in a second copy button below - is not mistaken for the trigger.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "target",
              "description": "The element being copied: what `for` names, or nothing. Resolved on every press\nrather than held, so a block that was re-rendered since the last one still copies.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "text",
              "description": "What a press would put on the clipboard, right now.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "value",
              "description": "The literal text to copy, or `null` for whatever `for` names. Reflected, so setting it\nis how a page copies something it computed - a link's `href`, a formatted number."
            },
            {
              "kind": "field",
              "name": "copiedText",
              "description": "What the live region says on success.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "errorText",
              "description": "What it says when there was nothing to copy, or the clipboard refused.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "status",
              "description": "The live region. Added at upgrade, because a live region only announces text that\nlands in one already in the document.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "announce",
              "parameters": [
                {
                  "name": "message"
                }
              ],
              "description": "Say something in the live region.\n\nA live region announces a *change*, so the same message set twice in a row is silent -\nwhich would make every copy after the first one say nothing. Cleared first and set back\nin a later task, so the two writes cannot coalesce into no change at all."
            },
            {
              "kind": "method",
              "name": "feedback",
              "parameters": [
                {
                  "name": "ok"
                },
                {
                  "name": "text"
                }
              ],
              "description": "Show and say how it went, then go quiet again."
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            }
          ],
          "events": [
            {
              "name": "copy-done",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.ok` is whether it landed, `detail.text` what went on the clipboard."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "string"
              },
              "description": "`id` of the element to copy. Also read as `data-for`.",
              "name": "for"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "Literal text to copy, exactly as written. Wins over `for`.",
              "name": "value"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "What the live region announces on success.",
              "name": "copied-text",
              "default": "Copied"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "What it announces when there was nothing to copy, or the clipboard refused.",
              "name": "error-text",
              "default": "Copy failed"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "copy-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "sourceText",
          "declaration": {
            "name": "sourceText",
            "module": "src/elementals/copy/index.js"
          }
        },
        {
          "kind": "js",
          "name": "CopyElemental",
          "declaration": {
            "name": "CopyElemental",
            "module": "src/elementals/copy/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/disclosure/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "disclosureState",
          "parameters": [
            {
              "name": "open",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "The state a disclosure's button and region carry, for a given open state.\n\n`until-found` rather than a bare `hidden` because it is the platform's own answer\nto the oldest problem with hiding content: find-in-page and a link to a fragment\ninside the region still reveal it, and the reveal comes back as `beforematch`.\nBrowsers without it read any value at all as plain hidden, which is the same\ncontent, just not found.\n\n`state` says the same thing as `hidden` and exists because it can be written at a\ndifferent moment: closing cannot set `hidden` until the slide is over, since that is\nwhat stops the region's contents being rendered, so anything keyed off `hidden`\nanimates a whole slide late. `state` flips with the click, which is when a property\ntransitioning alongside the height has to start.",
          "return": {
            "type": {
              "text": "{expanded: string, hidden: string|null, state: string}"
            }
          }
        },
        {
          "kind": "function",
          "name": "slideFrom",
          "parameters": [
            {
              "name": "open",
              "description": "The state being moved to.",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "hidden",
              "description": "Whether the region is hidden right now.",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "height",
              "description": "Its rendered height right now.",
              "type": {
                "text": "number"
              }
            }
          ],
          "description": "The height, in pixels, a slide of the region starts from.\n\nZero only when opening a region that is currently hidden, because that is the one case\nwith no height to read: `hidden=\"until-found\"` sizes the box as if it were empty.\nEverything else is a region already on screen - one being closed, or one caught\nmid-slide and sent back the other way - and its rendered height is the honest start.\nWhich is also why the measurement has to happen *before* unhiding: after, the region\nis at full height and a slide that starts where it ends is not a slide.",
          "return": {
            "type": {
              "text": "number"
            }
          }
        },
        {
          "kind": "function",
          "name": "mediaOpen",
          "parameters": [
            {
              "name": "query",
              "description": "The watched query, or null for an element without one.",
              "type": {
                "text": "MediaQueryList|null"
              }
            }
          ],
          "description": "The open state a `media` query dictates, or `null` when it dictates nothing.\n\nThe null is the load-bearing case rather than a tidy default: every disclosure on a\npage runs this, and one without a `media` attribute has no opinion about its own\nstate at all. Returning `false` for \"no query\" would read as \"closed\", and every\nplain disclosure in the document would slam shut on a breakpoint it never asked\nabout.",
          "return": {
            "type": {
              "text": "boolean|null"
            }
          }
        },
        {
          "kind": "function",
          "name": "mediaMode",
          "parameters": [
            {
              "name": "open",
              "description": "What {@link mediaOpen}said.\n * ",
              "type": {
                "text": "boolean|null"
              }
            }
          ],
          "description": "The name for which side of a `media` query the element is on, for CSS to hold on to.\n\nWhich exists so a stylesheet does not have to repeat the breakpoint. A drawer's layout\nis a media query in every page that writes one by hand, and that query has to agree\nwith the one in the attribute for the panel and its state to describe the same thing -\ntwo declarations of one number, in two languages, that nothing checks. Reflected, the\nelement is the only thing that knows the breakpoint and the CSS keys off the answer,\nwhich is also what lets that CSS be shipped to a project whose breakpoint is different.\n\n`pinned` is the query holding the region open; `free` is the button having it back.\nNull for an element with no query, so nothing is written and nothing can match.",
          "return": {
            "type": {
              "text": "\"pinned\"|\"free\"|null"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<disclosure-elemental>` custom element.\n\nA real `<button>` wired to a region it shows and hides, per the\n[APG Disclosure pattern](https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/).\n\nNative `<details>`/`<summary>` is a disclosure already, and where it fits it wins -\n`<accordion-elemental>` is built on it for exactly that reason. It fits when the\nregion can live *inside* the trigger's element. This element is for when it cannot:\na `<figcaption>`, which HTML requires to be a child of its `<figure>`; a table row;\na grid or flex item whose parent lays it out directly; a panel on the other side of\nthe page from the button that opens it. Wrapping any of those in a `<details>`\nmoves them out of the parent that gives them meaning.\n\nWhat it does, and nothing more: keeps `aria-expanded` on the button and `hidden` on\nthe region in step, points one at the other with `aria-controls`, re-opens on\n`beforematch` so find-in-page still finds what is inside, and slides the region's\nheight on the way in and out.\n\nLight DOM, no shadow root, and nothing is moved or wrapped - the region stays\nexactly where the markup put it, which is the whole point.",
          "name": "DisclosureElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<time>"
              },
              "description": "How long the region takes to slide. Override it on the region, which is where the element reads it back out of the computed styles - `0s` toggles instantly.",
              "name": "--disclosure-elemental-duration",
              "default": "250ms"
            },
            {
              "type": {
                "text": "<easing-function>"
              },
              "description": "How the slide moves. On the region, like the duration.",
              "name": "--disclosure-elemental-easing",
              "default": "ease"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Caret size, on the caret look.",
              "name": "--disclosure-elemental-caret-size",
              "default": "1em"
            }
          ],
          "slots": [
            {
              "description": "The `<button>` that toggles, and - unless `for` points elsewhere - the region right after it.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "button",
              "description": "The `<button>` that toggles the region. Direct child, so a button inside the\nregion - or inside a nested disclosure - is not mistaken for the trigger.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "region",
              "description": "The region the button shows and hides: what `for` names, else the button's\nnext sibling.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "open",
              "description": "Whether the region is showing. Reflected, so `[open]` is a styling hook too."
            },
            {
              "kind": "method",
              "name": "apply",
              "parameters": [
                {
                  "name": "animate",
                  "default": "false",
                  "optional": true,
                  "type": {
                    "text": "boolean"
                  }
                }
              ],
              "description": "Push the current state onto the button and the region, sliding the region's height\non the way if asked to.\n\n`animate` is off by default, because most of what lands here is not a state change\nto animate: the state a page loads with is where the region starts, and one the\nbrowser has already put on screen for find-in-page is already there."
            },
            {
              "kind": "method",
              "name": "watchMedia",
              "description": "Start watching whatever `media` names now, and stop watching whatever it named\nbefore. Both halves matter: the attribute can be rewritten at runtime."
            },
            {
              "kind": "method",
              "name": "onMediaChange",
              "description": "The breakpoint moved, so the state follows it.\n\nInstant, unlike a click. Crossing a breakpoint is the layout being rearranged around\nthe reader - a rotation, a window drag, a zoom - and animating the region through\nthat is animating something nobody asked to happen. It also keeps a resize from\nqueueing a slide per frame."
            },
            {
              "kind": "method",
              "name": "reflectMode",
              "description": "Put the current mode on the element and on the region, or take it off both.\n\nOn the region as well as the element because `for` lets the two live at opposite ends\nof the document, and a panel that has to reach back up to its button through\n`:root:has(…)` for every rule is a stylesheet nobody wants to read. It is one more\nattribute on a box the element is already writing `hidden`, `id` and a class to."
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onBeforeMatch"
            }
          ],
          "events": [
            {
              "name": "disclosure-toggle",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.region` is the element being shown or hidden, `detail.open` its new state."
            }
          ],
          "attributes": [
            {
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the region is showing. Reflected - it tracks the live state.",
              "default": "false"
            },
            {
              "name": "media",
              "type": {
                "text": "string"
              },
              "description": "A media query that owns `open`: held open while it matches, closed when it stops. Unset means the button is the only thing that opens it."
            },
            {
              "type": {
                "text": "string"
              },
              "description": "`id` of the region. Also read as `data-for`. Defaults to the button's next element sibling.",
              "name": "for"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "disclosure-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "disclosureState",
          "declaration": {
            "name": "disclosureState",
            "module": "src/elementals/disclosure/index.js"
          }
        },
        {
          "kind": "js",
          "name": "slideFrom",
          "declaration": {
            "name": "slideFrom",
            "module": "src/elementals/disclosure/index.js"
          }
        },
        {
          "kind": "js",
          "name": "mediaOpen",
          "declaration": {
            "name": "mediaOpen",
            "module": "src/elementals/disclosure/index.js"
          }
        },
        {
          "kind": "js",
          "name": "mediaMode",
          "declaration": {
            "name": "mediaMode",
            "module": "src/elementals/disclosure/index.js"
          }
        },
        {
          "kind": "js",
          "name": "DisclosureElemental",
          "declaration": {
            "name": "DisclosureElemental",
            "module": "src/elementals/disclosure/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/field/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "fieldAction",
          "parameters": [
            {
              "name": "type",
              "description": "The event: `invalid`, `blur`, `input`, `change`, `reset`.",
              "type": {
                "text": "string"
              }
            },
            {
              "name": "valid",
              "description": "What `control.validity.valid` says right now.",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "showing",
              "description": "Whether a message is already on screen.",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "dirty",
              "description": "Whether the reader has put anything in the field.",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "When the field is allowed to speak, and what happens when it does.\n\nThe browser's own rule is one moment and no others: nothing at all until a submit is\nrefused, and then a bubble that vanishes on the next click. The rule every hand-written\nvalidator reaches for instead is the opposite - validate on every keystroke - which tells\na reader their email address is invalid after they have typed `n`.\n\nSo the moments here are three, and the ordering between them is the whole design.\nA refused submit always speaks: that is the reader asking. Leaving a field speaks only if\nthey put something in it, so tabbing through a form does not light it up. And typing\nnever *starts* a complaint - it only keeps one already on screen current, and takes it\ndown the moment the value is right, which is the half that turns a complaint into\nprogress.\n\n`dirty` is whether the reader has put anything in the field, not whether they have been\nin it. A field they typed into and emptied again is back to untouched, which is the\nkinder reading of the same gesture.",
          "return": {
            "type": {
              "text": "\"show\"|\"clear\"|\"ignore\""
            }
          }
        },
        {
          "kind": "class",
          "description": "`<field-elemental>` custom element.\n\nThe label, the control and the message that belongs to it - wrapped so the browser's\nrefusal to submit becomes a sentence on the page instead of a bubble that floats away.\n\nThere is no APG pattern for this, because there is no widget: the control inside is\nalready accessible and already validated. What the platform leaves undone is everything\nafter the refusal. The native bubble cannot be styled, disappears the moment the field\ntakes focus, is shown for the first invalid control and no other, and is not reliably\nannounced. So every form on the web either lives with it or hand-writes a replacement,\nand the replacement is where the accessibility goes: a red paragraph that no `aria`\nattribute ties to the field is a message a screen reader user never meets.\n\nWhat it does, and nothing more: cancels the bubble, puts the browser's own\n`validationMessage` in a paragraph, points the control at it with `aria-describedby`, and\nmarks the control `aria-invalid` while - and only while - there is something wrong. The\nconstraints stay the author's `required`, `type`, `pattern`, `minlength`; the wording\nstays the browser's, already translated into the reader's language. There is no\nvalidation vocabulary to learn here, because there is no validation here.\n\n`aria-describedby` rather than `aria-errormessage`, which is the attribute written for\nexactly this and is still not the one that works. [Adrian Roselli's\ntesting](https://adrianroselli.com/2023/04/exposing-field-errors.html) found the message\nbehind `aria-errormessage` \"generally not exposed when navigating through fields\", while\n`aria-describedby` is \"consistently exposed\" - and [it was still not\nthere](https://cerovac.com/a11y/2024/06/support-for-aria-errormessage-is-getting-better-but-still-not-there-yet/)\nin mid-2024. The day it lands, this is one attribute name to change.\n\nNo live region on the message either, which is the counter-intuitive half. `describedby`\nis announced when focus leaves the field and the message appears, so a live region on top\nof it is the same sentence twice in NVDA and JAWS, and stops VoiceOver reading the\ndescription at all.\n\nLight DOM, no shadow root, nothing moved. One node is added - the message - unless the\npage already rendered one, which is how a server-side error survives: write the paragraph\nwith the message in it and the element adopts it, wires it up and marks the field invalid,\nso the same markup works with the script and without it.\n\nDegrades honestly: with no script the browser's own bubble is still there, doing what it\nhas always done. Nothing is taken away before there is something to replace it with.\n\nponytail: no message vocabulary - no `data-required-message`, no `invalid-message`. The\nplatform already has one call for this, `setCustomValidity()`, and a set of attributes\nshadowing it would be a second place for the same string to live. Its own trap comes with\nit and is the author's to know: a control with a custom validity set stays invalid until\nthe same call is made with an empty string.",
          "name": "FieldElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<color>"
              },
              "description": "The message's colour. The control is not styled at all - `[aria-invalid=\"true\"]` is there for the page's own CSS.",
              "name": "--field-elemental-error-color",
              "default": "currentcolor mixed towards red"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Above the message. Zero, because the page laying the field out has already said how far apart its parts sit - set it to have the theme own that distance instead.",
              "name": "--field-elemental-gap",
              "default": "0"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "The message's own size.",
              "name": "--field-elemental-message-size",
              "default": "0.85em"
            }
          ],
          "slots": [
            {
              "description": "A label, one form control, and optionally the `<p class=\"field-elemental-error\">` a server-rendered error was written into.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "control",
              "description": "The control being validated: the first one in here that a reader could put a value in.\n\nFirst rather than every, because a message belongs to a field and a field holds one\nanswer. A set of radios or checkboxes sharing one question is a `<fieldset>` and a\ndifferent element - wrapping one here would point the message at the first radio and\ndescribe the group by whichever of them the reader happened to land on.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "error",
              "description": "The message. The author's if they rendered one, otherwise the one added at upgrade.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "showing",
              "description": "Whether a message is on screen.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "dirty",
              "description": "Whether the reader has put anything in the field.\n\nA tick box holds its answer in `checked`; its `value` is the string that would be\nsubmitted and is `\"on\"` whether or not it has been ticked, so reading `value` here\nwould call every untouched box filled in and complain at a reader tabbing past it.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "onEvent",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "settle",
              "parameters": [
                {
                  "name": "type"
                }
              ],
              "description": "What the control says now, and what to do about it.\n\n`validity.valid` rather than `checkValidity()`, which is the same answer with an\n`invalid` event fired alongside it - straight back into `onEvent`, where every blur\nwould arrive as a refused submit and light up a field nobody has filled in yet."
            },
            {
              "kind": "method",
              "name": "takeFocus",
              "description": "Put focus on this control if it is the first thing in the form standing in the way.\n\nCancelling `invalid` is what drops the bubble, and it drops the browser's focus with\nit: measured in Chromium and WebKit, a refused submit then leaves focus on the button\n(Chromium) or on `<body>` (WebKit). Either one is a form that says nothing and goes\nnowhere, which is worse than the bubble this element was replacing.\n\nEvery invalid control in the form gets its own `invalid` event, so without the check\nthe last of them would win the race and the reader would land past the field that\nstopped the submit."
            },
            {
              "kind": "method",
              "name": "show",
              "parameters": [
                {
                  "name": "message"
                }
              ],
              "description": "Put the message on screen and tie the control to it."
            },
            {
              "kind": "method",
              "name": "clear",
              "description": "Take it down.\n\nHidden rather than emptied, and unpointed-at rather than pointed at an empty\nparagraph: a description that is there and says nothing is still read out as part of\nthe field, and `aria-invalid` left on a field that is now fine is the field lying about\nitself."
            },
            {
              "kind": "method",
              "name": "announce",
              "parameters": [
                {
                  "name": "valid"
                },
                {
                  "name": "message"
                }
              ]
            }
          ],
          "events": [
            {
              "name": "field-validity",
              "type": {
                "text": "CustomEvent"
              },
              "description": "Whenever the message appears or goes. `detail.valid` is what the control says, `detail.message` the text now on screen, empty when there is none."
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "field-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "fieldAction",
          "declaration": {
            "name": "fieldAction",
            "module": "src/elementals/field/index.js"
          }
        },
        {
          "kind": "js",
          "name": "FieldElemental",
          "declaration": {
            "name": "FieldElemental",
            "module": "src/elementals/field/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/marquee/index.js",
      "declarations": [
        {
          "kind": "variable",
          "name": "DEFAULT_SPEED",
          "type": {
            "text": "number"
          },
          "default": "50",
          "description": "Pixels a second, when `speed` says nothing usable. The figure the ecosystem settled on."
        },
        {
          "kind": "variable",
          "name": "MAX_CLONES",
          "type": {
            "text": "number"
          },
          "default": "20",
          "description": "The most copies of the track this will ever make.\n\nEvery copy is a full subtree in the document - `inert` and out of the accessibility tree,\nbut still nodes to lay out and paint. A track one narrow item wide against a wide screen\nasks for hundreds, and the honest answer there is a visible gap the author can see and fix\nwith more content, rather than a page that quietly grew a thousand elements."
        },
        {
          "kind": "function",
          "name": "cloneCount",
          "parameters": [
            {
              "name": "trackWidth"
            },
            {
              "name": "gapWidth"
            },
            {
              "name": "containerWidth"
            },
            {
              "name": "max",
              "default": "MAX_CLONES"
            }
          ],
          "description": "How many copies of the track it takes to cover the container without a gap.\n\nThe strip repeats every `track + gap`, which is also how far one lap travels. A lap ends\nwith the original translated that far out of frame, and what the reader sees at that moment\nis whatever is behind it - so the strip minus one repeat has to still cover the container.\n\n**The gap on the end is the one the strip does not have**, and it is the whole reason this\nis not simply `container / repeat`. A strip of `n + 1` copies carries `n` gaps and none\nafter the last, so it is `(n + 1) x repeat - gap` long, not `(n + 1) x repeat`. Solve\n`(n + 1) x repeat - gap - repeat >= container` and the gap moves to the other side:\n`n >= (container + gap) / repeat`. Count it the shorter way and the strip is up to one gap\nshort of the frame, which is a sliver of empty container in the last moments before the\nlap wraps - visible as a blink, and blamed on the wrap rather than on the arithmetic.\n\n**One is the floor, and zero is not an option while it moves.** A track already wider than\nthe container still needs a copy behind it: without one, the lap ends on empty container\nand the loop reads as a jump. The formula gives that on its own - any positive container\nrounds up to at least one - so there is nothing here enforcing it. Zero is what a track\nwith no width gets, because there is nothing to loop, and what a container with no width\ngets, because a `display: none` panel and a closed `<details>` both measure zero and\ncloning against that number fills the page the moment it opens."
        },
        {
          "kind": "function",
          "name": "cycleDuration",
          "parameters": [
            {
              "name": "distance"
            },
            {
              "name": "speed"
            }
          ],
          "description": "One lap, in seconds: the distance between two copies at the speed asked for. A speed that\nis not a positive number falls back rather than resolving to a `NaN` duration, which is an\nanimation that never runs with the strip parked wherever the clones left it."
        },
        {
          "kind": "class",
          "description": "`<marquee-elemental>` custom element.\n\nA row of content that scrolls forever - a logo strip, a ticker - built out of the list you\nalready wrote.\n\nThere is no APG pattern for this, because there is no widget: nothing here is operated, and\nthe content is the same content it was standing still. What there is instead is an\nobligation, and it is a Level A one.\n[WCAG 2.2 2.2.2 Pause, Stop, Hide](https://www.w3.org/WAI/WCAG22/Understanding/pause-stop-hide.html)\nsays movement that starts on its own, runs longer than five seconds and sits beside other\ncontent has to have a mechanism to stop it - and an infinite loop is longer than five\nseconds. Every marquee in the ecosystem leaves that mechanism to you: the CSS-only recipes\npause on `:hover`, which no keyboard has, and the component libraries hand you a `play`\nprop and a hook to build the button out of. **This writes the button.**\n\nThe second half is the one nobody has at all. A seamless loop is copies of the track, and a\ncopy of a logo strip is a copy of its links: `aria-hidden` keeps them out of the screen\nreader, and does nothing whatever about <kbd>Tab</kbd>, so the keyboard walks into copies of\nthe same links scrolling past under the focus ring. The copies here are `inert` as well as\n`aria-hidden`, and their `id`s are stripped on the way out, because a duplicated `id` is\nthe same bug one layer down.\n\n**The copies are counted, not guessed.** Two of everything is the usual recipe and it is\nright for one screen width: cover a wide monitor with a short track and the loop shows a\nhole. This measures the track against the container and clones until the strip covers it,\nagain whenever a resize changes the answer - one clone when the track already fills the\ncontainer, more when it does not, and none at all while nothing is moving, so a reader on\nreduced motion is not handed copies of the page they never asked to see move.\n\nLight DOM, no shadow root. Nothing you wrote is moved or wrapped - the copies are appended\nafter it and the control after those. With no script it is a list, wrapped or scrolling the\nway any list of things does, every item in the page and in reading order.\n\nponytail: no vertical axis, no `pauseOnClick`, no gradient-mask props. The fade at the\nedges is the theme's, the gap is a custom property, and the axis is one someone can ask\nfor when they have a ticker that needs it.",
          "name": "MarqueeElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between one copy of the track and the next, and between the items inside a `<ul>` or `<ol>` track.",
              "name": "--marquee-elemental-gap",
              "default": "2rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Theme. How far the two edges fade out. `0` is a hard edge.",
              "name": "--marquee-elemental-fade",
              "default": "2rem"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Theme. What the edges fade into and the button is painted on: the page's own background, so re-point it on a card.",
              "name": "--marquee-elemental-surface",
              "default": "Canvas"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Theme. The rim around the button.",
              "name": "--marquee-elemental-border-color",
              "default": "currentcolor at 30%"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Theme. Its fill under the pointer.",
              "name": "--marquee-elemental-hover",
              "default": "currentcolor at 10% over the surface"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Theme. Its icon under the pointer. Setting it tints the fill above with it, which is 10% of whatever the icon is.",
              "name": "--marquee-elemental-hover-color",
              "default": "inherit"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Theme. The button's box.",
              "name": "--marquee-elemental-control-size",
              "default": "2rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Theme. Its corners.",
              "name": "--marquee-elemental-control-radius",
              "default": "50%"
            }
          ],
          "slots": [
            {
              "description": "The content to loop, usually one `<ul>`. Whatever you write is the track, and the track is what gets copied.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "track",
              "type": {
                "text": "array"
              },
              "default": "[]",
              "description": "What the author wrote, captured at upgrade. Held rather than re-read, because after the\nfirst lap the element's own children are mostly copies of it."
            },
            {
              "kind": "field",
              "name": "clones",
              "description": "The copies.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "control",
              "description": "The pause button, when this wrote one.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "speed",
              "description": "Pixels a second."
            },
            {
              "kind": "field",
              "name": "playing",
              "description": "Whether it is moving. Setting it is the same as pressing the button."
            },
            {
              "kind": "method",
              "name": "addControl",
              "description": "Write the pause button.\n\nAppended rather than expected in the markup, for the reason the carousel's controls are:\na button authored by hand is a button that does nothing until the script lands, and a\ndead control is a worse promise than no control.\n\nIts name says what pressing it will do and it carries no `aria-pressed` - the APG's own\nanswer for this button, rather than both, which would have a screen reader read the two\nagainst each other."
            },
            {
              "kind": "method",
              "name": "labelControl"
            },
            {
              "kind": "method",
              "name": "measure",
              "description": "Measure the track against the container, and make the strip cover it.\n\n**Guarded on the width it last saw**, and that guard is load-bearing twice over. Rebuilding\nthe copies restarts the animation from its first frame, so doing it on a resume would put\na jump behind the one gesture that means hold still - and an observer that rebuilt on\nevery callback would be one whose own writes are what it hears next, which is a loop with\nno bottom to it.\n\nThe clones come off before the read, because the number wanted is the width of what the\nauthor wrote and a strip with copies in it is the width of the last answer. It is\nmeasured across the track's own boxes and **not** off the element's `scrollWidth`, which\nis the trap here: `scrollWidth` never reports less than the box it is on, so a short\ntrack in a wide container measures as the container - one copy, and a hole in the loop\nexactly where more copies were needed. Both ends are taken by extent rather than in\norder, because `dir=\"rtl\"` lays the same children out the other way round.\n\nThe gap goes into the distance rather than beside it: a lap has to land the second copy\nexactly where the first one started, which with a gap between them is one track plus one\ngap. It is read off the computed style, so an author's own `--marquee-elemental-gap` is\nin the sum without this having to parse it."
            },
            {
              "kind": "method",
              "name": "applyTiming",
              "description": "How far a lap goes and how long it takes, onto the element for the keyframes to read.\nSplit out of the measuring because `speed` changes the second number and not the first,\nand remeasuring to answer it would rebuild the strip to say the same thing."
            },
            {
              "kind": "method",
              "name": "copyTrack",
              "description": "One copy of the track.\n\n`inert` and `aria-hidden` are two different readers and both are owed an answer: the\nsecond keeps the copies out of the accessibility tree, and the first is the one every\nother marquee is missing - without it <kbd>Tab</kbd> walks into copies of the same links,\nscrolling past under the focus ring. `id`s come off on the way out, because a document\nwith twenty of the same `id` is one where every `aria-labelledby` and every `#anchor`\nresolves to whichever came first."
            },
            {
              "kind": "method",
              "name": "removeClones"
            },
            {
              "kind": "method",
              "name": "play",
              "description": "Start moving, making the copies it takes to do it seamlessly if they are not there yet."
            },
            {
              "kind": "method",
              "name": "pause",
              "description": "Stop.\n\nThe copies stay where they are. Taking them out would reflow the strip back to the\nauthor's one track under a reader who has just asked for the movement to stop, which is\na jump asked for by the one gesture that means \"hold still\"."
            }
          ],
          "events": [
            {
              "name": "marquee-toggle",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.playing` is whether it is moving now."
            }
          ],
          "attributes": [
            {
              "name": "speed",
              "type": {
                "text": "number"
              },
              "description": "Pixels a second. Anything that is not a positive number is the default.",
              "default": "50"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Travel the other way. Flipped again under `dir=\"rtl\"`, where the other way is the default one.",
              "name": "reverse",
              "default": "false"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Do not write the pause button. For a page that provides the mechanism itself - one control over several strips, a site-wide motion switch - through `.play()` and `.pause()`. With nothing else on the page it is 2.2.2 unmet, and yours to meet.",
              "name": "no-controls",
              "default": "false"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The control's accessible name while stopped.",
              "name": "play-text",
              "default": "Start the moving content"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "Its name while moving. `--marquee-elemental-distance` and `--marquee-elemental-duration` are deliberately not tagged below. The element writes them into its own `style` attribute, and an inline declaration beats any stylesheet - so a knob for either would be a control that cannot move anything, and the docs panel is built out of these tags. They are outputs, and the page describes them as such.",
              "name": "pause-text",
              "default": "Stop the moving content"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "marquee-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "DEFAULT_SPEED",
          "declaration": {
            "name": "DEFAULT_SPEED",
            "module": "src/elementals/marquee/index.js"
          }
        },
        {
          "kind": "js",
          "name": "MAX_CLONES",
          "declaration": {
            "name": "MAX_CLONES",
            "module": "src/elementals/marquee/index.js"
          }
        },
        {
          "kind": "js",
          "name": "cloneCount",
          "declaration": {
            "name": "cloneCount",
            "module": "src/elementals/marquee/index.js"
          }
        },
        {
          "kind": "js",
          "name": "cycleDuration",
          "declaration": {
            "name": "cycleDuration",
            "module": "src/elementals/marquee/index.js"
          }
        },
        {
          "kind": "js",
          "name": "MarqueeElemental",
          "declaration": {
            "name": "MarqueeElemental",
            "module": "src/elementals/marquee/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/menu/index.js",
      "declarations": [
        {
          "kind": "class",
          "description": "`<menu-elemental>` custom element.\n\nA button and the nested lists it opens, per the\n[APG Menu Button pattern](https://www.w3.org/WAI/ARIA/apg/patterns/menu-button/) -\nwith one addition the APG has no opinion on, because it is a layout question:\nbelow a breakpoint the whole thing stops being a menu.\n\nThat is the `media` attribute, and it is the point of the element. A flyout is a\ndesktop object: it floats over the page, only one branch of it is open at a time,\narrows move between items because the items are not in the tab order. On a phone\nthe same markup wants to be a stack of nested disclosures inside a drawer - links\nyou tab through, submenus that stay open where you left them, no `role=\"menu\"` at\nall. Two widgets, one set of markup, and the viewport decides which one is on.\n\nWhich also means the ARIA changes with the mode, not just the CSS. `role=\"menu\"`\nis a promise that the arrow keys work and Tab does not, so making it while the\nitems are a plain tabbable list would be a lie told to exactly the readers who\ncannot see the layout that makes it obvious.\n\nponytail: not built on the Popover API. For a dropdown holding *links* it would be\nthe whole answer - Escape, light dismiss, focus return, tab order, all native, no\nscript - and `<disclosure-elemental>` plus `popover` is the better thing to reach\nfor there. A menu has to own focus anyway for the roving tabindex and the\ntype-ahead, so popover would save the small half of the work and cost the inline\nmode, which cannot be in the top layer.\n\nLight DOM, no shadow root, and nothing is moved or wrapped.",
          "name": "MenuElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Corner radius of a flyout list.",
              "name": "--menu-elemental-radius",
              "default": "0.375rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Padding inside a list, and around each item.",
              "name": "--menu-elemental-inset",
              "default": "0.35rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "The caret on an item that opens a submenu.",
              "name": "--menu-elemental-caret-size",
              "default": "0.75em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "The generated hamburger icon.",
              "name": "--menu-elemental-hamburger-size",
              "default": "1.25em"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "What a flyout list is painted on. The page's own background, so re-point it on a card.",
              "name": "--menu-elemental-surface",
              "default": "Canvas"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Item background under the pointer, and while focused.",
              "name": "--menu-elemental-hover",
              "default": "color-mix(in srgb, currentcolor 10%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The rim around a flyout list.",
              "name": "--menu-elemental-border",
              "default": "color-mix(in srgb, currentcolor 20%, transparent)"
            }
          ],
          "slots": [
            {
              "description": "The `<button>` that opens it, and the `<ul>` it opens - submenus nested in their own `<li>`.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "hover",
              "description": "Whether a mouse opens the menu by pointing at it rather than by clicking.\n\nOpt-in, and only ever an addition: click, Enter and the arrow keys are what the\npattern promises, and a menu nobody can open without a steady hand is not one. Off\nfor touch, where there is no hovering to do and the first tap would open and pick\nin one gesture, and off inline, where the branches are stacked in the page and a\npointer crossing the stack would open every one it passed."
            },
            {
              "kind": "field",
              "name": "button",
              "description": "The `<button>` that opens the root list. Direct child, so a submenu's trigger -\nor a nested menu's button - is not mistaken for it.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "menu",
              "description": "The root list.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "menus",
              "description": "Every list in this menu, root first. A nested `<menu-elemental>` keeps its own.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "open",
              "description": "Whether the root list is showing. Reflected, so `[open]` is a styling hook too."
            },
            {
              "kind": "field",
              "name": "inline",
              "description": "Whether this is currently the stack-of-disclosures rather than the flyout: a\n`media` that is not matching right now. No `media` at all means a menu that is\na menu at every width, which is what a menu button is when nothing says otherwise.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "itemsOf",
              "parameters": [
                {
                  "name": "menu"
                }
              ],
              "description": "The items of one list: what its `<li>`s hold, and not what its submenus do."
            },
            {
              "kind": "method",
              "name": "menuOf",
              "parameters": [
                {
                  "name": "item"
                }
              ],
              "description": "The list an item belongs to."
            },
            {
              "kind": "method",
              "name": "submenuOf",
              "parameters": [
                {
                  "name": "item"
                }
              ],
              "description": "The list an item opens, if it opens one."
            },
            {
              "kind": "method",
              "name": "triggerOf",
              "parameters": [
                {
                  "name": "menu"
                }
              ],
              "description": "The item that opens a list. For a submenu that is the item beside it in the same\n`<li>`; for the root list the element's own button, which is the same query one\nlevel up."
            },
            {
              "kind": "method",
              "name": "isOpen",
              "parameters": [
                {
                  "name": "menu"
                }
              ]
            },
            {
              "kind": "method",
              "name": "navigable",
              "parameters": [
                {
                  "name": "menu"
                }
              ],
              "description": "The items the arrows walk from here.\n\nA flyout is walked one list at a time - the submenu is a separate surface and\nLeft/Right are how you cross between them. Inline there are no surfaces: the\nopen submenus are on screen, in the flow, and stopping at the edge of a list the\nreader is looking straight through would be arbitrary."
            },
            {
              "kind": "method",
              "name": "visibleItems",
              "parameters": [
                {
                  "name": "menu"
                }
              ],
              "description": "Every item on screen from `menu` down, in the order they are rendered."
            },
            {
              "kind": "method",
              "name": "watchMedia"
            },
            {
              "kind": "method",
              "name": "onMediaChange"
            },
            {
              "kind": "method",
              "name": "wire",
              "description": "Put the current mode's roles on the markup: a menu with items that are not in the\ntab order, or a set of nested disclosures that are nothing but an ordinary list.\n\n`aria-controls` and `aria-expanded` are the only two that survive the switch,\nbecause they are true of both - a button that shows and hides a thing, and which\nthing it is."
            },
            {
              "kind": "method",
              "name": "apply",
              "description": "Push the root list's state onto it and its button."
            },
            {
              "kind": "method",
              "name": "place",
              "parameters": [
                {
                  "name": "menu"
                }
              ],
              "description": "Point a list at whichever corner it fits in, and write that on it so the stylesheet\ncan put it there - the same trade as `data-mode`, and for the same reason: the\nmeasuring is the element's, the positioning is CSS's.\n\nMeasured from the preferred placement rather than from wherever the last flip left\nit, so a panel does not decide where to go from a position it only has because it\nwent there last time.\n\nThe carets read this back too: a submenu that opened to the left is announced by an\narrow that points left, which no `position-try` fallback can say."
            },
            {
              "kind": "method",
              "name": "placeOpen",
              "description": "Re-place every open list. The viewport moved under them."
            },
            {
              "kind": "method",
              "name": "setSubmenu",
              "parameters": [
                {
                  "name": "trigger"
                },
                {
                  "name": "open"
                }
              ],
              "description": "Show or hide one submenu.\n\nFloating, the branches overlap, so opening one closes its siblings - that is what\nmakes a flyout readable. Inline they are stacked in the flow and closing a branch\nthe reader opened on purpose only loses their place."
            },
            {
              "kind": "method",
              "name": "closeSubmenus",
              "parameters": [
                {
                  "name": "menu"
                }
              ],
              "description": "Close every open branch below a list, deepest first."
            },
            {
              "kind": "method",
              "name": "closeAll"
            },
            {
              "kind": "method",
              "name": "focusItem",
              "parameters": [
                {
                  "name": "menu"
                },
                {
                  "name": "index"
                }
              ],
              "description": "Move focus to one item of a list, counting from the end for a negative index -\n`-1` is the last item, which is where Up on the closed button lands."
            },
            {
              "kind": "method",
              "name": "itemFor",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "The item this event happened on, or null for anything outside this menu."
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onPointerOver",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "Point at it and it opens: the root list from the button, a branch from its own\nitem, and a branch closed again by pointing at any other item of the same list -\nwhich is the sibling rule `setSubmenu` already keeps for clicks.\n\n`pointerover` rather than `pointerenter` because it bubbles, so one listener covers\nevery item; mouse only, because a touch \"hover\" is the tap that was about to pick\nsomething."
            },
            {
              "kind": "method",
              "name": "onPointerLeave",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "The pointer has left the whole element, so the menu closes - after a beat, because\nthe gap between a button and its panel, or between a panel and the one beside it,\nis a place the pointer passes through rather than a place it means to be."
            },
            {
              "kind": "method",
              "name": "onKeyDown",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "closeBranch",
              "parameters": [
                {
                  "name": "menu"
                }
              ],
              "description": "Close the list focus is in and hand focus back to whatever opened it - a submenu\ngoes back to its trigger, the root list back to the button."
            },
            {
              "kind": "method",
              "name": "onFocusOut",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onDocumentClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            }
          ],
          "events": [
            {
              "name": "menu-toggle",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.menu` is the list that opened or closed, `detail.open` which way it went."
            }
          ],
          "attributes": [
            {
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the root list is showing. Reflected, so `[open]` is a styling hook.",
              "default": "false"
            },
            {
              "name": "media",
              "type": {
                "text": "string"
              },
              "description": "The media query the flyout exists in. Outside it, nested disclosures. Unset means a menu at every width."
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "A mouse also opens it by pointing at it. Never on touch, never inline.",
              "name": "hover",
              "default": "false"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "menu-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "MenuElemental",
          "declaration": {
            "name": "MenuElemental",
            "module": "src/elementals/menu/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/modal/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "dismissMode",
          "parameters": [
            {
              "name": "value",
              "type": {
                "text": "string|null"
              }
            }
          ],
          "description": "The closing rule a `closedby` value names, defaulting the way the platform defaults it.\n\n`closerequest` for anything unrecognised rather than nothing at all, because that is what\nan invalid value does to an enumerated attribute in HTML - and because the alternative,\na typo silently producing a dialog that Escape cannot close, is the worst of the three.",
          "return": {
            "type": {
              "text": "\"any\"|\"closerequest\"|\"none\""
            }
          }
        },
        {
          "kind": "function",
          "name": "dismissible",
          "parameters": [
            {
              "name": "mode",
              "type": {
                "text": "\"any\"|\"closerequest\"|\"none\""
              }
            },
            {
              "name": "source",
              "type": {
                "text": "\"escape\"|\"pointer\""
              }
            }
          ],
          "description": "Whether a close request from `source` gets through, under `mode`.",
          "return": {
            "type": {
              "text": "boolean"
            }
          }
        },
        {
          "kind": "function",
          "name": "commandAction",
          "parameters": [
            {
              "name": "command",
              "type": {
                "text": "string|null"
              }
            }
          ],
          "description": "What an invoker's `command` asks of a dialog, or null for one that is not ours.\n\n`request-close` and `close` land in the same place here, and the difference the platform\ndraws between them - one fires `cancel` and can be stopped, the other cannot - is drawn\nagain in ModalElemental#close, which is the only path either of them takes.",
          "return": {
            "type": {
              "text": "\"open\"|\"close\"|null"
            }
          }
        },
        {
          "kind": "function",
          "name": "adoption",
          "parameters": [
            {
              "name": "open",
              "description": "Whether the dialog is open now.",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "modal",
              "description": "Whether it is open *modally* - `:modal`, not just `[open]`.",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "known",
              "description": "Whether this element opened it and has it in the stack already.",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "What to do about a dialog that has just become open, when this element is not the one\nthat opened it - `null` for nothing at all.\n\nBecause `document.getElementById('…')` gives an author the `<dialog>` and not the element\naround it, so `showModal()` on it is the call they will reach for. Left alone, that is a\nmodal that never gets `data-state`, which the stylesheet draws at `opacity: 0` - a\nbackdrop over the page with nothing on it. Same for a `show()`, minus the backdrop.",
          "return": {
            "type": {
              "text": "\"modal\"|\"inline\"|null"
            }
          }
        },
        {
          "kind": "function",
          "name": "settleLimit",
          "parameters": [
            {
              "name": "endTimes",
              "description": "`getComputedTiming().endTime` of each animation.",
              "type": {
                "text": "number[]"
              }
            }
          ],
          "description": "How long to wait for animations that say they end at `endTimes`, in milliseconds.\n\nThere is a wait at all because `finished` is a promise a browser has to keep, and one that\nnever resolves is a modal that can never be closed - the failure this element exists to\nprevent, arriving through the machinery meant to make it pretty. So the animation's own\ntiming is the wait, and the promise only has to beat it.\n\nA little past the end rather than exactly on it, since a transition resolves `finished` on\nthe frame after its last one. Zero for nothing to wait for, which is what a stylesheet\nthat was never imported looks like from here.",
          "return": {
            "type": {
              "text": "number"
            }
          }
        },
        {
          "kind": "function",
          "name": "outside",
          "parameters": [
            {
              "name": "rect",
              "type": {
                "text": "{left: number, top: number, right: number, bottom: number}"
              }
            },
            {
              "name": "x",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "y",
              "type": {
                "text": "number"
              }
            }
          ],
          "description": "Whether a point is outside a box - the backdrop rather than the dialog.\n\nThe edge counts as inside, since a click landing exactly on the border of a box is a\nclick on the box.",
          "return": {
            "type": {
              "text": "boolean"
            }
          }
        },
        {
          "kind": "function",
          "name": "stopMedia",
          "parameters": [
            {
              "name": "dialog"
            }
          ],
          "description": "Stop whatever the modal was playing, now that it is closed.\n\nA closed dialog is `display: none`, which stops nothing: a video keeps playing and an\nembed keeps talking to a reader who has already dismissed it. `pause()` covers the\nelements; an embedded player is a cross-origin document that cannot be paused from here,\nso its frame is sent to `about:blank` instead - which discards the document, and is why\nreopening a lightbox starts the video at the beginning rather than where it was.\n\n`loading` goes to `eager` for that one navigation, and this is the whole reason the frame\nis parked rather than reloaded in place: a `lazy` frame in a `display: none` dialog is not\nnavigated when its src is set, the load is deferred until it is on screen again, and the\ndocument that was playing lives on unseen and audible - measured with a ticking frame in\nChromium 151, Firefox 153 and WebKit 26.5, all three. `about:blank` alone is not the\nanswer either: Firefox defers a navigation to it the same way. Both attributes are put\nback on the next open, so the markup an author reads is the markup they wrote."
        },
        {
          "kind": "function",
          "name": "restoreMedia",
          "parameters": [
            {
              "name": "dialog"
            }
          ],
          "description": "Point a parked frame back at what it was showing, now that the modal is open again.\n\n`loading` first, so a frame that said `lazy` is lazy again before the src that starts the\nfetch is set - the dialog is open by now, so a deferred load resolves immediately either\nway, and the attribute is never observed holding a value its author did not write."
        },
        {
          "kind": "function",
          "name": "writesClose",
          "parameters": [
            {
              "name": "mode",
              "type": {
                "text": "\"any\"|\"closerequest\"|\"none\""
              }
            }
          ],
          "description": "Whether the element writes a close button into a dialog closing this way.\n\nA visible close button is\n[strongly recommended](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/) by the APG,\nand it is the one control a pointer has that Escape gives a keyboard for free - so it is\nwritten rather than left to be forgotten.\n\nNot under `none`, and that is the whole of the exception: `closedby=\"none\"` is a dialog\nthat has to be answered rather than dismissed, and a cross in its corner is a dismissal\nwith a different shape. A dialog that says `none` and wants a way out writes its own\nbutton, saying what taking it means.",
          "return": {
            "type": {
              "text": "boolean"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<modal-elemental>` custom element.\n\nA `<dialog>` opened as a modal, per the\n[APG Modal Dialog pattern](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/) - and\nmostly by the browser rather than by this element. `showModal()` already puts the dialog\nin the top layer, makes the rest of the page inert, moves focus in, brings it back on\nclose and closes on Escape, and it does all of that better than a focus trap in script\ncan: nesting one modal inside another is the browser stacking two entries in the top\nlayer, not two libraries arguing over which one owns the page.\n\nWhat is left over is what this element is: an exit animation that the platform cannot\ngive a dialog without the `overlay` property, which Firefox and Safari do not have; a\nclick on the backdrop, which needs `closedby` support Safari does not have either; the\npage behind not scrolling; and the pile of backdrops a stack of modals would otherwise\npaint on top of each other.\n\nLight DOM, no shadow root, nothing moved: the `<dialog>` stays exactly where it was\nwritten, and every attribute on it is still the platform's own.",
          "name": "ModalElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<time>"
              },
              "description": "How long the dialog and its backdrop take to arrive and to leave. The element waits for the animation it starts, so `0s` closes instantly.",
              "name": "--modal-elemental-duration",
              "default": "200ms"
            },
            {
              "type": {
                "text": "<easing-function>"
              },
              "description": "Easing for both ends.",
              "name": "--modal-elemental-easing",
              "default": "ease"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The sheet over the page.",
              "name": "--modal-elemental-backdrop",
              "default": "rgb(0 0 0 / 50%)"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Capped again at `100% - 2rem`, so a phone keeps its gutter.",
              "name": "--modal-elemental-max-width",
              "default": "32rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Inside the box.",
              "name": "--modal-elemental-padding",
              "default": "1.5rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Its corners.",
              "name": "--modal-elemental-radius",
              "default": "0.75rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "The close button, both axes. Its cross is sized from it.",
              "name": "--modal-elemental-close-size",
              "default": "2rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "How far the cross sits from the box's corner.",
              "name": "--modal-elemental-close-inset",
              "default": "0.75rem"
            },
            {
              "type": {
                "text": "<length> | auto"
              },
              "description": "Where it sits: `auto` centres, `5vh auto` pins it near the top.",
              "name": "--modal-elemental-margin-block",
              "default": "auto"
            }
          ],
          "slots": [
            {
              "description": "The `<dialog>`. One direct child, with the modal's content inside it.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "dialog",
              "description": "The dialog this element upgrades. Direct child, so a nested modal's dialog is not\nmistaken for this one's.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "open",
              "description": "Whether the modal is on screen. Closing counts as open until the animation is over,\nwhich is what the dialog is doing for that quarter second.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "writeClose",
              "parameters": [
                {
                  "name": "dialog"
                }
              ],
              "description": "Write the cross in the corner.\n\nFirst child rather than last, so the tab order and the reading order agree with where it\nis drawn - and so focus lands on it when the dialog opens, which is the right first stop\nfor a dialog that is read rather than filled in. Put `autofocus` on a field to move it.\n\n`command=\"request-close\"` rather than a handler of its own: it is the same door the\nEscape key uses, animation and all, and it is markup an author could have written.\n\nThe cross is text, not a background image, so a page that loaded the script but not the\nstylesheet still has a button with something in it. `aria-label` is what is announced -\na cross is a shape and reads as nothing."
            },
            {
              "kind": "method",
              "name": "adopt",
              "description": "Take over a dialog somebody else opened, so it looks like one this element opened.\n\nA modal joins the stack and is numbered with the rest, since the backdrops have to be\ncounted whoever asked for them. A non-modal `show()` gets the visible state and nothing\nelse: it is a dialog in the page, not over it, and putting it in the stack would dim\nthe page and lock its scroll for a box the reader can still click past."
            },
            {
              "kind": "field",
              "name": "closedBy",
              "description": "What `closedby` says, whether it was written here or on the dialog.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "name",
              "parameters": [
                {
                  "name": "dialog"
                }
              ],
              "description": "Give the dialog a name if it has none, from the first heading inside it.\n\nA dialog with no accessible name is announced as \"dialog\" and nothing else, which is\nthe most common failure of the pattern and the one an author is least likely to see -\ntheir own modal has a heading at the top of it, right there on screen. `aria-labelledby`\npoints at that heading rather than copying its words, so the two cannot drift apart.\n\nOnly a heading of this dialog: a nested modal written inside this one has headings too,\nand they name a different dialog."
            },
            {
              "kind": "method",
              "name": "show",
              "parameters": [
                {
                  "name": "options",
                  "description": "`fromHash` marks the URL as\nwhat opened it, so closing takes the fragment back off again. `pushed` says that\nfragment was navigated to rather than loaded with, which decides how it comes off.",
                  "optional": true,
                  "type": {
                    "text": "{fromHash?: boolean, pushed?: boolean}"
                  }
                }
              ],
              "description": "Open the modal."
            },
            {
              "kind": "method",
              "name": "close",
              "parameters": [
                {
                  "name": "returnValue",
                  "description": "What `dialog.returnValue` should say afterwards.",
                  "optional": true,
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Close the modal, once its animation has finished.\n\nEvery way out lands here - Escape, the backdrop, a close button, a form, the fragment\nchanging - so there is one close, and it is animated whichever door was used. The\nexception is an author calling `close()` on the `<dialog>` itself, which the platform\nperforms immediately and this element only tidies up after."
            },
            {
              "kind": "method",
              "name": "onNativeClose",
              "description": "Bookkeeping for a dialog that has closed, however it closed - including from script\nthat never went through this element."
            },
            {
              "kind": "method",
              "name": "onCancel",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "Escape, and every other close request the platform makes.\n\nAlways prevented, never because the dialog should stay: the close it would perform is\ninstant, and this element owns an animated one. `closedby=\"none\"` is the one case where\npreventing it is the whole answer - and a second Escape will still force the dialog\nshut, because a close watcher only lets itself be argued with once."
            },
            {
              "kind": "method",
              "name": "onPointerDown",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "Where a click started, since a selection dragged out of the dialog and released on\nthe backdrop is not a click on the backdrop."
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "A click on the backdrop, which is a click on the dialog: the backdrop is a\npseudo-element and cannot be a target of its own.\n\nTwo things have to agree for it to count. The target, so a click on a button inside is\nnever one - including the click a keyboard makes, which reports its position as the\norigin of the viewport and would otherwise read as the far corner of the backdrop. And\nthe geometry, because the dialog's own padding is part of the dialog."
            },
            {
              "kind": "method",
              "name": "onSubmit",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "`<form method=\"dialog\">`, held back long enough to animate and then performed by\nhand, `returnValue` and all."
            },
            {
              "kind": "method",
              "name": "toggled",
              "parameters": [
                {
                  "name": "open"
                }
              ]
            }
          ],
          "events": [
            {
              "name": "modal-toggle",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.open` is the new state, `detail.dialog` the dialog, `detail.depth` how deep in a stack of modals it sits."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "\"any\"|\"closerequest\"|\"none\""
              },
              "description": "What closes the dialog besides a close button: `closerequest` is Escape, `any` adds a click on the backdrop, `none` neither. The same three values HTML gives `<dialog>`, read from here so the element can animate the close instead of the browser cutting it short - written on the `<dialog>` it is moved up here on upgrade.",
              "name": "closedby",
              "default": "closerequest"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Opening this one closes every modal already open, instead of stacking on top of them.",
              "name": "close-others",
              "default": "false"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The close button's accessible name. Its label too, for anyone reading the markup - the cross itself is a shape and says nothing.",
              "name": "close-text",
              "default": "Close"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "modal-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "dismissMode",
          "declaration": {
            "name": "dismissMode",
            "module": "src/elementals/modal/index.js"
          }
        },
        {
          "kind": "js",
          "name": "dismissible",
          "declaration": {
            "name": "dismissible",
            "module": "src/elementals/modal/index.js"
          }
        },
        {
          "kind": "js",
          "name": "commandAction",
          "declaration": {
            "name": "commandAction",
            "module": "src/elementals/modal/index.js"
          }
        },
        {
          "kind": "js",
          "name": "adoption",
          "declaration": {
            "name": "adoption",
            "module": "src/elementals/modal/index.js"
          }
        },
        {
          "kind": "js",
          "name": "settleLimit",
          "declaration": {
            "name": "settleLimit",
            "module": "src/elementals/modal/index.js"
          }
        },
        {
          "kind": "js",
          "name": "outside",
          "declaration": {
            "name": "outside",
            "module": "src/elementals/modal/index.js"
          }
        },
        {
          "kind": "js",
          "name": "stopMedia",
          "declaration": {
            "name": "stopMedia",
            "module": "src/elementals/modal/index.js"
          }
        },
        {
          "kind": "js",
          "name": "restoreMedia",
          "declaration": {
            "name": "restoreMedia",
            "module": "src/elementals/modal/index.js"
          }
        },
        {
          "kind": "js",
          "name": "writesClose",
          "declaration": {
            "name": "writesClose",
            "module": "src/elementals/modal/index.js"
          }
        },
        {
          "kind": "js",
          "name": "ModalElemental",
          "declaration": {
            "name": "ModalElemental",
            "module": "src/elementals/modal/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/navbar/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "navbarMode",
          "parameters": [
            {
              "name": "matches",
              "description": "Whether the `media` query matches, or there is no query.",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "overflowed",
              "description": "How many items did not fit.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "total",
              "description": "How many items there are.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "minimum",
              "default": "1",
              "description": "How many have to fit for this to still be a bar.",
              "optional": true,
              "type": {
                "text": "number"
              }
            }
          ],
          "description": "Which of the two widgets the bar is right now.\n\nTwo inputs, because there are two ways to run out of room and only one of them is a\nwidth. `media` not matching is the author saying \"this is a phone\"; too few items still\nfitting is the bar saying \"there is nothing left on me\" - at which point the overflow\nbutton is not an overflow any more, it is the whole navigation, and a drawer is the\nhonest name for that.\n\n`minimum` is where \"too few\" is. One is the default and the old behaviour: a bar stops\nbeing one when nothing at all is left on it. Two says a single link beside an overflow\nbutton is not a navigation bar either.",
          "return": {
            "type": {
              "text": "\"bar\"|\"stack\""
            }
          }
        },
        {
          "kind": "function",
          "name": "probeState",
          "parameters": [
            {
              "name": "hasPanel",
              "description": "Whether the original opens a panel.",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "The state a copy of a control has to carry to be measured as its original.\n\n`wire()` writes `aria-expanded` on every trigger, and a theme is free to draw on that -\nthe shipped one puts a caret after it. The copy is built before any of that and with its\npanels taken out, so nothing marks its triggers as triggers: each one measures a caret\nnarrower than the button it stands for. The row is then told more fits than does, and the\nitems past the edge are not folded into the overflow - they are clipped by the rail,\nwhich is how the overflow button itself ends up half under the search field.",
          "return": {
            "type": {
              "text": "{ 'aria-expanded': string }|null"
            }
          }
        },
        {
          "kind": "function",
          "name": "ownsRow",
          "parameters": [
            {
              "name": "ancestors",
              "description": "Tag names between the list and the navbar, neither end included.",
              "type": {
                "text": "string[]"
              }
            }
          ],
          "description": "Whether the first list found inside a navbar is the navbar's own row.\n\nThe row is \"the first `<ul>` or `<menu>` in the element\", which holds right up until the bar\ncarries something that writes a list of its own - a `<suggest-elemental>` results panel, a\nnested navbar. Adopting one of those is not a cosmetic mistake: the row's parent becomes the\nrail, so the other element's box is handed `display: grid`, `overflow: clip` and a copy of\nitself to measure, and its list is laid out as a bar.\n\nA custom element between the two is the line, because that is what \"somebody else owns this\"\nlooks like in markup. A page that wraps its row in one gets no navbar rather than a hijacked\npanel - the links are still there, still a list, which is the degradation the element\npromises anyway.",
          "return": {
            "type": {
              "text": "boolean"
            }
          }
        },
        {
          "kind": "function",
          "name": "hoverIntent",
          "parameters": [
            {
              "name": "branch",
              "description": "The row item the pointer is in, or null.",
              "type": {
                "text": "*"
              }
            },
            {
              "name": "trigger",
              "description": "The control under the pointer, if it opens a panel.",
              "type": {
                "text": "*"
              }
            }
          ],
          "description": "What a pointer arriving somewhere means for the panels that are open.\n\n`branch` is the row's own item the pointer is inside, however deep - not the control under\nit. A link inside an open panel opens nothing, so reading the control alone says \"close\neverything\", which closes the panel the pointer just walked into.\n\nNothing of the row's under the pointer is not an instruction either: between a trigger and\nthe panel hanging under it lies the bar's own padding, and closing there would shut every\npanel the moment anyone reached for one. Leaving the bar is what closes them, and\n`onPointerLeave` owns that.",
          "return": {
            "type": {
              "text": "{ except: *, open: * }|null"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<navbar-elemental>` custom element.\n\nA site's navigation bar: a row of links, some of them opening a panel of more links, and\ntwo things a row of links always ends up needing - somewhere for the ones that do not fit\nto go, and a way to be a drawer instead on a narrow screen.\n\nIt is the APG's\n[Disclosure Navigation Menu](https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/examples/disclosure-navigation/)\nand not a menubar. `role=\"menuitem\"` replaces link semantics, and the APG's own menubar\nexample opens by recommending this pattern instead for exactly that reason. So there are\nno roles here at all: the items are links in a list, `Tab` reaches every one of them, and\nthe buttons are buttons with `aria-expanded`. The optional arrow keys from that example\nare implemented, because they cost twenty lines and a bar of eight items wants them.\n\nWhat it adds to the pattern is the thing CSS cannot do: noticing that the links have run\nout of room. A breakpoint cannot know how many links a site has, how long their labels\nare, or what the reader's font does to them - so the row is measured instead, and the\nitems that do not fit move into the overflow panel one at a time. When none of them fit\nthe whole bar becomes a drawer.\n\nThe measuring is an `IntersectionObserver` on a copy of the row rather than on the row\nitself, which is the one idea in here worth stealing. Hiding an overflowing item shrinks\nthe row, which makes the next item fit, which hides that one instead - an observer\nwatching the box it is also changing is an infinite loop. So the copy is what is watched\nand the row is what changes, and nothing ever invalidates its own measurement. It also\nmeans late-arriving webfonts are free: they change the copy's geometry, the observer says\nso, and the row settles again. A cached set of widths would simply be wrong from then on.\n\nLight DOM, no shadow root. Three things are generated - the copy being measured, the copies\nof the items inside the overflow panel, and the bars inside the drawer's button - and\nnothing the page wrote is moved or wrapped.",
          "name": "NavbarElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Corner radius of a floating panel.",
              "name": "--navbar-elemental-radius",
              "default": "0.375rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Padding inside a panel, and around each item.",
              "name": "--navbar-elemental-inset",
              "default": "0.35rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between the items of the row.",
              "name": "--navbar-elemental-gap",
              "default": "0.15rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "The caret on a trigger that opens a panel.",
              "name": "--navbar-elemental-caret-size",
              "default": "0.75em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Width of the generated hamburger icon.",
              "name": "--navbar-elemental-hamburger-size",
              "default": "1.25em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "How thick each of its three bars is.",
              "name": "--navbar-elemental-bar-thickness",
              "default": "2px"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "How far the outer two sit from the middle one.",
              "name": "--navbar-elemental-bar-gap",
              "default": "0.35em"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "What a floating panel is painted on. The page's own background, so re-point it on a card.",
              "name": "--navbar-elemental-surface",
              "default": "Canvas"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Item background under the pointer, and while focused.",
              "name": "--navbar-elemental-hover",
              "default": "color-mix(in srgb, currentcolor 10%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The rim around a floating panel.",
              "name": "--navbar-elemental-border",
              "default": "color-mix(in srgb, currentcolor 20%, transparent)"
            },
            {
              "type": {
                "text": "<shadow>"
              },
              "description": "What lifts a floating panel off the page.",
              "name": "--navbar-elemental-shadow",
              "default": "0 4px 20px rgb(0 0 0 / 15%)"
            }
          ],
          "slots": [
            {
              "description": "The `<ul>` of links, optionally a `[data-navbar-more]` overflow item and a `[data-navbar-toggle]` button.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "row",
              "description": "The row: the first list in the element that no other custom element between them owns. A\nnested `<navbar-elemental>` keeps its own, and so does anything else on the bar that\nwrites a list - see `ownsRow`.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "rail",
              "description": "The box the row is measured inside, which is whatever the page put the row in. The copy\ngoes in here beside it, so the two are the same width without either of them having to\nbe told what that width is.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "moreItem",
              "description": "The item holding the overflow button, if the page authored one.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "morePanel",
              "description": "The list inside it, which the element fills with copies.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "toggle",
              "description": "The button that opens the drawer in stack mode.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "items",
              "description": "The items being measured: the row's own, minus the two kinds that are not links competing\nfor room - the overflow button, and anything the page has marked as the drawer's alone.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "lists",
              "description": "Every list in this navbar, the row included.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "minBarItems",
              "description": "How many links have to fit for this to still be a bar. One - a bar that keeps going until\nnothing at all is left on it - unless the page says otherwise.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "stacked",
              "description": "Whether the bar is currently the drawer rather than the row.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "open",
              "description": "Whether the drawer is showing. Reflected, so `[open]` is a styling hook too."
            },
            {
              "kind": "field",
              "name": "hover",
              "description": "Whether a mouse opens a panel by pointing at it rather than by clicking.\n\nOpt-in, and only ever an addition. Off while stacked, where the panels are in the flow\nand a pointer crossing the drawer on its way somewhere would open every one it passed."
            },
            {
              "kind": "method",
              "name": "fillToggle",
              "description": "The bars inside the drawer's button.\n\nA hamburger that crosses into an X is three lines, and a button has two pseudo-elements -\nso one of the three has to be an element. It is written here rather than asked of the\npage: every toggle already out there is an empty `<button>`, and a look that only works\nfor markup written after it shipped is a look nobody sees. `aria-hidden`, because the\nbutton's label is its name and this is a picture of what the button does.\n\nNothing is drawn without the optional theme; the attribute is a hook for whoever wants\nto draw their own."
            },
            {
              "kind": "method",
              "name": "fillMore",
              "description": "Put a copy of every item inside the overflow panel, and hand back the copies in the\norder their originals are in.\n\nCopies rather than the items themselves, because moving an item out of the row would\nmove it out of the thing being measured - and because a link that is both on the bar and\nin the panel is one link the reader can reach two ways, which is the point of an\noverflow."
            },
            {
              "kind": "method",
              "name": "buildProbe",
              "description": "Build the copy of the row that gets measured, and put it in the rail beside the row.\n\nTwo things are done to it, and both are about width. The panels come out: an absolutely\npositioned box adds nothing to a row's width, and a copy of one would be a second box\nanswering to the same anchor name. And the overflow item moves to the front, where its\nbox reserves exactly the room it is going to take at the other end - the row has to be\nmeasured against the space that will be left once the overflow button is on it, or the\nlast link and the button would fight over the same pixels.\n\nThe stylesheet hides it, but the copy is a second, focusable, announced navigation\nuntil it does - so the neutralising is done here too, where it holds whether or not the\nstructure styles ever arrive."
            },
            {
              "kind": "method",
              "name": "itemsOf",
              "parameters": [
                {
                  "name": "list"
                }
              ],
              "description": "The items of one list: what its `<li>`s hold, and not what its panels do."
            },
            {
              "kind": "method",
              "name": "branchOf",
              "parameters": [
                {
                  "name": "node"
                }
              ],
              "description": "The row's own control whose branch this node sits in, however deep inside a panel it is.\nA pointer over a link three levels down is still pointing at the item on the bar that\nopened the panels above it."
            },
            {
              "kind": "method",
              "name": "panelOf",
              "parameters": [
                {
                  "name": "trigger"
                }
              ],
              "description": "The list a trigger opens, if it opens one."
            },
            {
              "kind": "method",
              "name": "triggerOf",
              "parameters": [
                {
                  "name": "list"
                }
              ],
              "description": "The trigger that opens a list: the button beside it in the same `<li>`."
            },
            {
              "kind": "method",
              "name": "isOpen",
              "parameters": [
                {
                  "name": "list"
                }
              ]
            },
            {
              "kind": "method",
              "name": "navigable",
              "parameters": [
                {
                  "name": "from"
                }
              ],
              "description": "The set the arrow keys walk from here.\n\nA panel is its own surface, so inside one the arrows stay inside it. On the bar they walk\nthe bar. Stacked, there are no surfaces: the open panels are on screen, in the flow, and\nstopping at the edge of a list the reader is looking straight through would be arbitrary."
            },
            {
              "kind": "method",
              "name": "visibleItems",
              "parameters": [
                {
                  "name": "list"
                }
              ],
              "description": "Every item on screen from `list` down, in the order they are rendered."
            },
            {
              "kind": "method",
              "name": "watchMedia"
            },
            {
              "kind": "method",
              "name": "onMediaChange"
            },
            {
              "kind": "method",
              "name": "wire",
              "description": "Point every trigger at what it opens. `aria-expanded` and `aria-controls` are the whole\nof the ARIA here - there is no role to write, which is the pattern's point."
            },
            {
              "kind": "method",
              "name": "observe"
            },
            {
              "kind": "method",
              "name": "onIntersect",
              "parameters": [
                {
                  "name": "entries"
                }
              ]
            },
            {
              "kind": "method",
              "name": "apply",
              "description": "Push the current mode onto the element, and the drawer's state onto the row.\n\nCrossing between the two closes whatever was open, because what was open belonged to the\nother widget: a dropdown left open on a bar that has just become a drawer is a floating\npanel in a stack."
            },
            {
              "kind": "method",
              "name": "applyDrawer",
              "description": "The drawer is the row itself, hidden behind the toggle while stacked and simply the bar\nagain while not - so there is one list in the page rather than a row and a copy of it in\na panel, and one set of links to keep in step with the site."
            },
            {
              "kind": "method",
              "name": "setPanel",
              "parameters": [
                {
                  "name": "trigger"
                },
                {
                  "name": "open"
                }
              ],
              "description": "Show or hide one panel.\n\nOn the bar the panels overlap, so opening one closes its siblings - that is what makes a\nbar readable. Stacked they are in the flow, and closing a branch the reader opened on\npurpose only loses their place."
            },
            {
              "kind": "method",
              "name": "closeSiblings",
              "parameters": [
                {
                  "name": "trigger"
                }
              ],
              "description": "Close every other panel of the same list."
            },
            {
              "kind": "method",
              "name": "closePanels",
              "parameters": [
                {
                  "name": "list"
                }
              ],
              "description": "Close every open panel below a list, deepest first."
            },
            {
              "kind": "method",
              "name": "holdsFocus",
              "parameters": [
                {
                  "name": "trigger"
                }
              ],
              "description": "Whether the panel this trigger opens has focus in it."
            },
            {
              "kind": "method",
              "name": "closeBar",
              "parameters": [
                {
                  "name": "except"
                }
              ],
              "description": "Close every panel on the bar, except one, and except any the keyboard is inside."
            },
            {
              "kind": "method",
              "name": "controlFor",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "The control this event happened on, or null for anything outside this navbar."
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onKeyDown",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "closeBranch",
              "parameters": [
                {
                  "name": "list"
                }
              ],
              "description": "Close the list focus is in and hand focus back to whatever opened it. Off the end of a\npanel, Escape goes to that panel's own trigger; on the bar itself it closes the drawer."
            },
            {
              "kind": "method",
              "name": "onPointerOver",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "Point at it and it opens - and pointing at one panel is also the instruction to close the\nothers, since they overlap and only one of them can be read at a time.\n\n`pointerover` rather than `pointerenter` because it bubbles, so one listener covers every\nitem; mouse only, because a touch \"hover\" is the tap that was about to choose something."
            },
            {
              "kind": "method",
              "name": "onPointerLeave",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "The pointer has left the whole bar, so the panels close - after a beat, because the gap\nbetween a label and its panel is a place the pointer passes through rather than a place\nit means to be."
            },
            {
              "kind": "method",
              "name": "onFocusOut",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "Tab out of a panel and it is behind you. The APG asks for this, and `1.4.13 Content on\nHover or Focus` asks for it too: `relatedTarget` is null when focus lands outside the\ndocument altogether, which counts as leaving."
            },
            {
              "kind": "method",
              "name": "onDocumentClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onBeforeMatch",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            }
          ],
          "events": [
            {
              "name": "navbar-toggle",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.panel` is the list that opened or closed, `detail.open` which way it went."
            }
          ],
          "attributes": [
            {
              "name": "media",
              "type": {
                "text": "string"
              },
              "description": "The media query the bar exists in. Outside it, the drawer. Unset means a bar at every width, until the links stop fitting."
            },
            {
              "name": "min-bar-items",
              "type": {
                "text": "number"
              },
              "description": "How many links have to fit for this to still be a bar. `2` says one link beside an overflow button is a drawer.",
              "default": "1"
            },
            {
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the drawer is showing. Reflected, so `[open]` is a styling hook.",
              "default": "false"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "A mouse also opens a panel by pointing at it. Never on touch, never stacked.",
              "name": "hover",
              "default": "false"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "navbar-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "stepIndex",
          "declaration": {
            "name": "stepIndex",
            "module": "src/elementals/navbar/index.js"
          }
        },
        {
          "kind": "js",
          "name": "navbarMode",
          "declaration": {
            "name": "navbarMode",
            "module": "src/elementals/navbar/index.js"
          }
        },
        {
          "kind": "js",
          "name": "probeState",
          "declaration": {
            "name": "probeState",
            "module": "src/elementals/navbar/index.js"
          }
        },
        {
          "kind": "js",
          "name": "ownsRow",
          "declaration": {
            "name": "ownsRow",
            "module": "src/elementals/navbar/index.js"
          }
        },
        {
          "kind": "js",
          "name": "hoverIntent",
          "declaration": {
            "name": "hoverIntent",
            "module": "src/elementals/navbar/index.js"
          }
        },
        {
          "kind": "js",
          "name": "NavbarElemental",
          "declaration": {
            "name": "NavbarElemental",
            "module": "src/elementals/navbar/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/password/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "revealAfter",
          "parameters": [
            {
              "name": "event",
              "description": "`toggle`, `submit`, `reset`, or anything else.",
              "type": {
                "text": "string"
              }
            },
            {
              "name": "shown",
              "description": "Whether the value is visible now.",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "Whether the field is showing its value after `event`, given whether it was before.\n\nTwo of these three are about security rather than about the button. A revealed field is an\n`<input type=\"text\">`, and browsers remember what has been typed into text fields - so a\nvalue submitted while revealed can be offered back in an autofill list on some other page\nlater. Masking before the value leaves costs nothing and is the whole of the fix.\n\nReset is the same argument from the other end: the page loaded masked, and putting the\nvalue back without putting the mask back would leave a password on screen that the reader\nnever asked to see again.",
          "return": {
            "type": {
              "text": "boolean"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<password-elemental>` custom element.\n\nA password field you can look at, and a button that says so out loud.\n\nThere is no APG pattern for this, because there is no widget: it is a `<button>` next to an\n`<input>`, and both are already accessible. What is missing is everything about the\n*state*. A button that swaps an eye for a crossed-out eye has told a sighted reader which\nway round it is and told everyone else nothing, and the field it controls has changed from\nmasked to plain with no announcement at all - which is the one change on the page a reader\nmost needs to know about, because it is the one with a shoulder-surfer behind it.\n\nWhat it does, and nothing more: gives the button `aria-pressed` and keeps it in step,\nflips the field between `password` and `text`, says which in a live region, and masks the\nfield again before the form is submitted or reset. No strength meter, no generator, no\nconfirmation field - see below.\n\n`aria-pressed` with a *fixed* name, which is the one place the sources disagree:\n\n| Prior art | Button name | State |\n| --- | --- | --- |\n| [GOV.UK](https://design-system.service.gov.uk/components/password-input/) | swaps, `Show` ⇄ `Hide` | implied by the name |\n| [Make Things Accessible](https://www.makethingsaccessible.com/guides/make-an-accessible-password-reveal-input/) | fixed | `aria-pressed` |\n| [hexagoncircle](https://github.com/hexagoncircle/password-input-components) | swaps | `aria-pressed` - both at once |\n| this | fixed | `aria-pressed` |\n\nThe third row is what the second explicitly warns against: with `aria-pressed` carrying the\nstate, a name that also changes says it twice and disagrees with itself half the time. Of\nthe two that are self-consistent, the toggle is the one ARIA is written for - the name is\nthe thing being switched and `pressed` is whether it is on, exactly as a Bold button in an\neditor works - and it is the one where nothing changes under a reader's focus. The live\nregion is what settles the ambiguity either way, and both sources agree on having one.\n\nLight DOM, no shadow root, nothing moved. One node is added: the live region, because a\nlive region only announces text that lands in one already in the document.\n\nDegrades honestly: the stylesheet keeps the button out of reach until the element has\nupgraded, so with no script there is no dead button - just a password field, which is what\nthe page had before and still works. Nothing is taken away before there is something to\nreplace it with.\n\nponytail: no caret to save and restore. Flipping `input.type` keeps focus and the selection\nrange in Chromium and WebKit - measured - so the obvious defensive code would have been\nfifteen lines guarding against nothing. Pressing the button still moves focus to the\nbutton, because that is what pressing a button does; what the preserved selection buys is\n`el.shown = true` from script leaving the reader's place in the field alone.",
          "name": "PasswordElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "The eye, both axes.",
              "name": "--password-elemental-icon-size",
              "default": "1em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Inside the button.",
              "name": "--password-elemental-padding",
              "default": "0.35em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "The button's corners.",
              "name": "--password-elemental-radius",
              "default": "0.35rem"
            }
          ],
          "slots": [
            {
              "description": "An `<input type=\"password\">` and the `<button>` that reveals it.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "control",
              "description": "The field being revealed: the first password or text input in here. Both, because\nonce it has been revealed the type is what this element changed it to.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "button",
              "description": "The button that reveals it. Direct child or not - a reveal button is often inside a\nwrapper holding it against the field's right edge.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "shown",
              "description": "Whether the value is visible. Reflected, so a page can reveal one from script."
            },
            {
              "kind": "field",
              "name": "label",
              "description": "The button's accessible name. Does not change with the state - `aria-pressed` does.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "shownText",
              "description": "What the live region says once the value is on screen.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "hiddenText",
              "description": "And once it is not.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "status",
              "description": "The live region. Added at upgrade, because a live region only announces text that\nlands in one already in the document.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "render",
              "description": "Put the field and the button into the state the `shown` attribute says they are in."
            },
            {
              "kind": "method",
              "name": "announce",
              "parameters": [
                {
                  "name": "message"
                }
              ],
              "description": "Say something in the live region.\n\nA live region announces a *change*, so the same message set twice in a row is silent.\nCleared first and set back in a later task, so the two writes cannot coalesce into no\nchange at all."
            },
            {
              "kind": "method",
              "name": "update",
              "parameters": [
                {
                  "name": "event"
                }
              ],
              "description": "Move to the state `event` calls for, and say so if it changed anything."
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onForm",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "A submit or a reset masks the field.\n\n`submit` fires only when the form is really being submitted - a browser that refuses\nthe submit on a constraint never dispatches it, measured in Chromium - so this lands\nexactly at the moment the value is about to leave the page and at no other. There is\nno trade to weigh: a reader whose submit was refused keeps the field the way they left\nit, and nothing was submitted for a browser to remember."
            }
          ],
          "events": [
            {
              "name": "password-reveal",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.shown` is whether the value is now visible."
            }
          ],
          "attributes": [
            {
              "name": "shown",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the value is visible. Reflected, so `[shown]` is a styling hook, and settable from script.",
              "default": "false"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "The button's accessible name. Fixed on purpose: `aria-pressed` is what carries the state.",
              "name": "label",
              "default": "Show password"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "What the live region says when the value is revealed.",
              "name": "shown-text",
              "default": "Your password is visible"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "What it says when the mask goes back on.",
              "name": "hidden-text",
              "default": "Your password is hidden"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "password-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "revealAfter",
          "declaration": {
            "name": "revealAfter",
            "module": "src/elementals/password/index.js"
          }
        },
        {
          "kind": "js",
          "name": "PasswordElemental",
          "declaration": {
            "name": "PasswordElemental",
            "module": "src/elementals/password/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/progress/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "percent",
          "parameters": [
            {
              "name": "value"
            },
            {
              "name": "max"
            }
          ],
          "description": "How far along the bar is, as a percentage of `max`, clamped to the ends.\n\n`<progress>` has no `min` - it counts from zero by definition, which is what separates it\nfrom `<meter>`. A `max` that is zero, negative or unparseable is no scale to measure\nagainst, and zero is the honest answer there: a bar drawn full because the denominator\nwas nonsense is a bar that lies about being finished."
        },
        {
          "kind": "class",
          "description": "A native `<progress>` whose fill and buffer are readable from CSS. The prose above is the\nwhole story; these tags are the same API in the form a machine can read -\n`custom-elements.json` is generated from them, and the docs tables, editor autocomplete\nand the live options panel all come out of that one file.\n\nCurated by omission: `--progress-elemental-value` and `--progress-elemental-buffer` are\nwritten by the element and are not tagged. They are the state, not a knob to turn, and\nsetting one by hand parks the bar somewhere the `<progress>` underneath it disagrees\nwith.",
          "name": "ProgressElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Bar thickness.",
              "name": "--progress-elemental-height",
              "default": "0.5rem"
            },
            {
              "type": {
                "text": "<length-percentage>"
              },
              "description": "Bar corners. A big number is a pill at any height.",
              "name": "--progress-elemental-radius",
              "default": "999px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The part behind everything.",
              "name": "--progress-elemental-track",
              "default": "color-mix(in srgb, currentcolor 15%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The value.",
              "name": "--progress-elemental-fill",
              "default": "currentcolor"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The buffer, behind the value.",
              "name": "--progress-elemental-buffer-fill",
              "default": "color-mix(in srgb, currentcolor 35%, transparent)"
            },
            {
              "type": {
                "text": "<time>"
              },
              "description": "How long the fill takes to move.",
              "name": "--progress-elemental-duration",
              "default": "250ms"
            },
            {
              "type": {
                "text": "ease | ease-in | ease-out | ease-in-out | linear"
              },
              "description": "How the fill moves.",
              "name": "--progress-elemental-easing",
              "default": "ease-out"
            },
            {
              "type": {
                "text": "<time>"
              },
              "description": "One sweep, while there is no value.",
              "name": "--progress-elemental-indeterminate-duration",
              "default": "1.4s"
            }
          ],
          "slots": [
            {
              "description": "One `<progress>`, with the `value` and `max` it would have had on its own.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "progress",
              "description": "The bar. Direct child, so a `<progress>` inside a card this element happens to wrap\nis not mistaken for the one being measured.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "value",
              "description": "The value, or `null` while there is none. Setting `null` takes the attribute off and\nputs the bar back to indeterminate, which is what\n[MDN says to do](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/progress)\nand the only way there is."
            },
            {
              "kind": "field",
              "name": "max",
              "description": "What counts as done. The `<progress>`'s own, which is `1` when it has none."
            },
            {
              "kind": "field",
              "name": "buffer",
              "description": "The second value, on the same scale. `null` is no buffer bar."
            },
            {
              "kind": "method",
              "name": "apply",
              "description": "Push the bar's state onto the element, where the CSS reads it. Public because a\n`<progress>` swapped out from under this element is not something the observer is\nwatching for: replace the child and call this.\n\n`data-indeterminate` as well as the missing percentage, because CSS cannot ask whether\na custom property was set - an unset one inside `calc()` is a bar sitting at zero, and\na bar at zero is a claim that nothing has happened yet rather than that nobody knows."
            }
          ],
          "attributes": [
            {
              "name": "buffer",
              "type": {
                "text": "number"
              },
              "description": "A second value on the same `max`, drawn behind the fill. Absent is no buffer bar at all."
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "progress-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "percent",
          "declaration": {
            "name": "percent",
            "module": "src/elementals/progress/index.js"
          }
        },
        {
          "kind": "js",
          "name": "ProgressElemental",
          "declaration": {
            "name": "ProgressElemental",
            "module": "src/elementals/progress/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/search/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "searchAction",
          "parameters": [
            {
              "name": "value",
              "description": "What is in the field.",
              "type": {
                "text": "string"
              }
            },
            {
              "name": "min",
              "description": "How many characters a query needs. `0` sends the empty one.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "last",
              "description": "The query already answered, or `null` for none.",
              "type": {
                "text": "string|null"
              }
            }
          ],
          "description": "What the field's current contents mean: send a query, do nothing, or take the results\ndown.\n\nThree answers rather than two, because \"nothing to send\" splits. A query identical to\nthe one already answered is `idle` - the results on screen are its answer, and sending\nit again would replace them with themselves. A query under the minimum is `clear` - the\nreader has deleted what those results were for, and a panel left standing is an answer\nto a question no longer being asked.\n\nThe ends are trimmed before anything is measured or compared: trailing whitespace is not\na new query, and a space is not a character worth searching for.",
          "return": {
            "type": {
              "text": "\"query\"|\"idle\"|\"clear\""
            }
          }
        },
        {
          "kind": "function",
          "name": "searchStatus",
          "parameters": [
            {
              "name": "state",
              "type": {
                "text": "\"idle\"|\"pending\"|\"results\"|\"empty\"|\"error\""
              }
            },
            {
              "name": "count",
              "description": "How many answers landed.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "texts",
              "type": {
                "text": "{results?: string|null, empty?: string|null, error?: string|null}"
              }
            }
          ],
          "description": "What a settled search says in the live region.\n\n[WCAG 2.2 4.1.3 Status Messages](https://www.w3.org/WAI/WCAG22/Understanding/status-messages.html)\nis the whole reason this string exists: results appearing in a panel is a change a\nsighted reader sees and a screen reader user is told nothing about, and a search that\nfound nothing is the one they most need to hear.\n\nA search still in flight says nothing. The loader is for the eye - a polite region\nannouncing \"Searching…\" on every keystroke is a reader listening to their own typing,\nand the answer is a second or so behind it anyway.\n\nThe default is English and handles the one plural English has, because \"1 results\" is\nthe bug this exists not to ship. Any other language sets the strings, and `{n}` is\nsubstituted wherever it appears - a language whose plural needs more than one form\nwants a string set per query from JS, which is why this takes them rather than owning\nthem.",
          "return": {
            "type": {
              "text": "string"
            }
          }
        },
        {
          "kind": "function",
          "name": "searchOpen",
          "parameters": [
            {
              "name": "state",
              "type": {
                "text": "\"idle\"|\"results\"|\"empty\"|\"error\""
              }
            },
            {
              "name": "filled",
              "description": "Whether the panel has anything in it.",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "Whether the panel should be showing, for a settled state and a panel that does or does\nnot have something in it.\n\nAnswers are the obvious half. The other half is the empty search, and it has two right\nanswers depending on what the page did: a panel emptied out has nothing to show and\nclosing it is the only honest thing to do, but a page that put \"No packages match wombat\"\nin there has written the answer and hiding it would throw the answer away. So `empty`\nfollows the panel rather than the count - which is also the rule that keeps this element\nout of writing markup it does not own.\n\n`error` closes either way, and that is the asymmetry worth naming: what is in the panel\nafter a failed request is the *last* query's results, still sitting there because nothing\nreplaced them. Leaving those up under an error is the one outcome worse than showing\nnothing.",
          "return": {
            "type": {
              "text": "boolean"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<search-elemental>` custom element.\n\nThe query half of a search field: the debounce, the abort, the loading state and the\nannouncement. [`<suggest-elemental>`](../suggest/) owns the panel those results land in\nand says outright that it has no opinion about where they came from - this is the other\nside of that seam, and it has no opinion about the results either.\n\nThere is no APG pattern here, because there is no widget. The panel is a\n[combobox](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) and belongs to the other\nelement; what is left is the part every search field re-implements and gets subtly\nwrong: a debounce, one `AbortController` per query, the sequence number that drops the\nslow answer arriving after the fast one, and\n[WCAG 2.2 4.1.3 Status Messages](https://www.w3.org/WAI/WCAG22/Understanding/status-messages.html),\nwhich a panel silently filling itself does not meet.\n\n**It does not fetch.** The page does, and hands back the promise:\n\n```js\nsearch.addEventListener('search-query', (e) => {\n  e.detail.wait(\n    fetch(url + encodeURIComponent(e.detail.query), { signal: e.detail.signal })\n      .then((response) => response.json())\n      .then((rows) => list.replaceChildren(...rows.map(toRow)))\n  );\n});\n```\n\n`wait()` is what buys the loading state: without it the element settles the moment the\nlistener returns, which is right for a page filtering an index it already has, and there\nis nothing to draw a spinner for. That is the degradation and it is honest in both\ndirections - a page that never calls it is never left with a spinner nothing will stop.\n\nLight DOM, no shadow root. One node is added, the live region, because a live region has\nto be in the document before the text lands in it. With no script the `<form>` inside\nsubmits to its `action` and the reader gets a search page - which is why the markup is a\nform and not a bare input.\n\nponytail: no `src`, no result-shape mapping, no cache, no keyboard shortcut to focus the\nfield. Owning a wire format is owning an escaping boundary and someone else's API; `⌘K`\nis a page-level binding and belongs to the page that knows what else is bound.",
          "name": "SearchElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Both axes of the spinner the theme draws while a query is out.",
              "name": "--search-elemental-spinner-size",
              "default": "1rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "How far the spinner sits from the inline end of the element.",
              "name": "--search-elemental-spinner-inset-inline",
              "default": "0.75rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "How far it sits from the block end, which is the bottom of the field.",
              "name": "--search-elemental-spinner-inset-block",
              "default": "0.75rem"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The moving part of it; the track is the same colour at a third of that.",
              "name": "--search-elemental-spinner-color",
              "default": "currentcolor at 45%"
            },
            {
              "type": {
                "text": "<time>"
              },
              "description": "One turn. `prefers-reduced-motion` replaces the turning with a fade.",
              "name": "--search-elemental-spinner-duration",
              "default": "0.7s"
            }
          ],
          "slots": [
            {
              "description": "The `<form>` holding the field, and the `<suggest-elemental>` its results go into.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "field",
              "description": "The field being typed in: the first `<input>` inside.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "panel",
              "description": "Where results land. Counting the links in it is how the element knows what to\nannounce, so this is a DOM query and not a call into the other element - a page that\nloaded this bundle and not that one still gets its states and its announcement.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "status",
              "description": "The live region. Added at upgrade, because a live region only announces text that\nlands in one already in the document.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "delay",
              "description": "Milliseconds the field has to stop changing before a query goes out.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "min",
              "description": "Characters needed before one goes out at all.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "texts",
              "description": "What the live region says, in the page's own words where it gave any.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "cancel",
              "description": "Stop whatever is in flight: the query that has not gone out yet, and the one that\nhas. Both, because a keystroke lands in one of those two windows and never says\nwhich."
            },
            {
              "kind": "method",
              "name": "mark",
              "parameters": [
                {
                  "name": "name",
                  "type": {
                    "text": "\"idle\"|\"pending\"|\"results\"|\"empty\"|\"error\""
                  }
                }
              ],
              "description": "Write the state onto the element, and onto the panel where it is the panel's to\nreport.\n\n`data-state` is the whole of the loading API: `[data-state=\"pending\"]` is what a\nspinner hangs off, and there is no second way to ask. `aria-busy` goes on the panel\nrather than here because the panel is the region whose contents are being fetched."
            },
            {
              "kind": "method",
              "name": "announce",
              "parameters": [
                {
                  "name": "message"
                }
              ],
              "description": "Say something in the live region.\n\nA live region announces a *change*, so the same message set twice in a row is silent -\nwhich would make the second search for the same number of hits say nothing. Cleared\nfirst and set back in a later task, so the two writes cannot coalesce into no change\nat all."
            },
            {
              "kind": "field",
              "name": "count",
              "description": "How many answers landed. Links only, which is the rule the panel itself counts an\noption by.\n\nThe panel where there is one, and otherwise the element's own links with the form's\nleft out - a search whose results are a list in the page rather than a popup still has\na count to announce, and the \"advanced search\" link beside the field is not one of\nthem. Results rendered outside the element are outside what it can count, and it says\nso rather than guessing.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "filled",
              "description": "Whether the panel has anything in it at all, which is a different question from how\nmany answers are in it.\n\nText rather than a selector, because an empty state is whatever the page wrote - a\n`<li>`, a paragraph, a line about what to try instead - and a list of shapes to match\nwould be this element having an opinion about markup it does not own. An empty `<ul>`\nhas no text; anything a reader could read does.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "settle",
              "parameters": [
                {
                  "name": "state",
                  "type": {
                    "text": "\"idle\"|\"results\"|\"empty\"|\"error\""
                  }
                },
                {
                  "name": "count",
                  "type": {
                    "text": "number"
                  }
                }
              ],
              "description": "A search has finished: show it, open or close the panel, say what happened."
            },
            {
              "kind": "method",
              "name": "settleFromPanel",
              "description": "Settle from whatever ended up in the panel."
            },
            {
              "kind": "method",
              "name": "run",
              "parameters": [
                {
                  "name": "query",
                  "type": {
                    "text": "string"
                  }
                }
              ],
              "description": "Ask the page for results, and take whatever it does with that.\n\nThe sequence number is the bug this element exists to fix: two requests in flight\nsettle in whatever order the network feels like, and the slow answer to the query\nbefore last is the one that ends up on screen. `signal` asks the page to abort the\nold one and the number makes sure it does not matter whether it did."
            },
            {
              "kind": "method",
              "name": "onInput"
            }
          ],
          "events": [
            {
              "name": "search-query",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.query` is what to search for, `detail.signal` an `AbortSignal` that fires when a newer query replaces this one, and `detail.wait(promise)` hands the element the work so it can show a loading state."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "number"
              },
              "description": "Milliseconds the field has to stop changing before a query goes out.",
              "name": "delay",
              "default": "200"
            },
            {
              "type": {
                "text": "number"
              },
              "description": "Characters needed before one goes out at all. `0` sends the empty query too, which is what a field cleared back to nothing sends.",
              "name": "min",
              "default": "1"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "What the live region announces on a hit. `{n}` is the count. Default: `5 results`, `1 result`.",
              "name": "results-text"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "What it announces when nothing matched.",
              "name": "empty-text",
              "default": "No results"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "What it announces when the request failed.",
              "name": "error-text",
              "default": "Search failed"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "search-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "searchAction",
          "declaration": {
            "name": "searchAction",
            "module": "src/elementals/search/index.js"
          }
        },
        {
          "kind": "js",
          "name": "searchStatus",
          "declaration": {
            "name": "searchStatus",
            "module": "src/elementals/search/index.js"
          }
        },
        {
          "kind": "js",
          "name": "searchOpen",
          "declaration": {
            "name": "searchOpen",
            "module": "src/elementals/search/index.js"
          }
        },
        {
          "kind": "js",
          "name": "SearchElemental",
          "declaration": {
            "name": "SearchElemental",
            "module": "src/elementals/search/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/segmented/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "checkedIndex",
          "parameters": [
            {
              "name": "inputs"
            }
          ],
          "description": "Which segment the knob belongs under: the index of the checked radio, or `-1` for\nnone.\n\n`-1` is a state and not a failure - a group whose markup checks nothing has no\nselection, and no knob is the honest drawing of that. The first checked one wins where\ntwo are, which is what a browser does when markup checks two radios of one name."
        },
        {
          "kind": "class",
          "description": "A row of native radio buttons drawn as one track with a knob that slides between them.\nThe prose above is the whole story; these tags are the same API in the form a machine\ncan read - `custom-elements.json` is generated from them, and the docs tables, editor\nautocomplete and the live options panel all come out of that one file.\n\nCurated by omission: `--segmented-elemental-index` and `--segmented-elemental-count`\nare written by the element and are not tagged. They are the state, not a knob to turn,\nand setting one by hand moves the knob off the segment the reader actually chose.",
          "name": "SegmentedElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between the knob and the inside of the track.",
              "name": "--segmented-elemental-gap",
              "default": "3px"
            },
            {
              "type": {
                "text": "<length-percentage>"
              },
              "description": "Track corners. A big number is a pill at any height.",
              "name": "--segmented-elemental-radius",
              "default": "999px"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Track border width.",
              "name": "--segmented-elemental-border-width",
              "default": "1px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Track border.",
              "name": "--segmented-elemental-border-color",
              "default": "transparent"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Track fill, behind every segment.",
              "name": "--segmented-elemental-track",
              "default": "color-mix(in srgb, currentcolor 10%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Knob fill. The page's own background, so re-point it on a card.",
              "name": "--segmented-elemental-knob",
              "default": "Canvas"
            },
            {
              "type": {
                "text": "<length-percentage>"
              },
              "description": "Knob corners.",
              "name": "--segmented-elemental-knob-radius",
              "default": "var(--segmented-elemental-radius)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Label colour on the segments the knob is not under.",
              "name": "--segmented-elemental-color",
              "default": "currentcolor"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Label colour on the one it is. Set this with `--segmented-elemental-knob` - the label sits on top of the knob.",
              "name": "--segmented-elemental-color-selected",
              "default": "currentcolor"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Segment padding, block axis. This is what sets the height.",
              "name": "--segmented-elemental-padding-block",
              "default": "0.375rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Segment padding, inline axis.",
              "name": "--segmented-elemental-padding-inline",
              "default": "0.875rem"
            },
            {
              "type": {
                "text": "<time>"
              },
              "description": "Knob slide and colour cross-fade.",
              "name": "--segmented-elemental-duration",
              "default": "250ms"
            },
            {
              "type": {
                "text": "ease | ease-in | ease-out | ease-in-out | linear"
              },
              "description": "Knob slide and colour cross-fade.",
              "name": "--segmented-elemental-easing",
              "default": "ease-in-out"
            }
          ],
          "slots": [
            {
              "description": "One `<label>` per segment, each wrapping an `<input type=\"radio\">` and the text or icon that names it.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "inputs",
              "description": "The segments, in document order. Direct children, so a radio group inside one\nsegment's popover is not mistaken for part of this one.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "selectedIndex",
              "description": "Index of the checked segment, or `-1` when the group has no selection.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "onReset",
              "description": "A form is only put back to its defaults once the `reset` event has been dispatched,\nso the selection is read on the next task rather than in the handler."
            },
            {
              "kind": "method",
              "name": "apply",
              "description": "Push the selection onto the element, where the CSS reads it. Public because the count\nis read here: add or remove a segment and this is the one call that catches up.\n\n`data-index` as well as the custom property, because CSS cannot ask whether a custom\nproperty was set - an unset one inside `calc()` leaves the knob at zero, which is a\nknob claiming the first segment. The attribute is what the knob's existence hangs\noff, so no script and no selection both come out as no knob."
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "segmented-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "checkedIndex",
          "declaration": {
            "name": "checkedIndex",
            "module": "src/elementals/segmented/index.js"
          }
        },
        {
          "kind": "js",
          "name": "SegmentedElemental",
          "declaration": {
            "name": "SegmentedElemental",
            "module": "src/elementals/segmented/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/slider/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "ratio",
          "parameters": [
            {
              "name": "value"
            },
            {
              "name": "min"
            },
            {
              "name": "max"
            }
          ],
          "description": "Where a value sits between the ends, as `0` to `1`.\n\nA range whose ends are the same, or the wrong way round, is no scale to place anything\non - and the start of the track is the honest answer there, because it is where a native\nrange input parks its own thumb when it is given the same markup."
        },
        {
          "kind": "function",
          "name": "clampPair",
          "parameters": [
            {
              "name": "start"
            },
            {
              "name": "end"
            },
            {
              "name": "gap"
            },
            {
              "name": "moved"
            },
            {
              "name": "min"
            },
            {
              "name": "max"
            },
            {
              "name": "step"
            }
          ],
          "description": "The two values with the thumbs kept apart, and which one gave way.\n\nThe thumb being moved is the one that gives way, so dragging the low thumb into the high\none stops it rather than pushing the high one along - a drag that shoves the other end of\nthe range ahead of it is a drag that changes a value nobody touched.\n\nExcept at the ends, where it cannot: `gap` may be wider than the room left between the\nmoved thumb and the bound behind it, and then the other one is what has to move. That is\nthe case a range pinned to its minimum is in every time the low thumb is dragged down.\n\nThe thumb gives way to the notch *past* where the gap lands, never the nearest one. A\n`gap` no whole number of steps wide - `step=\"10\"` with `gap=\"25\"` - would otherwise be\nhanded a value the input rounds back towards the other thumb, leaving the two 20 apart\nwith a gap of 25 asked for and nothing anywhere saying so. Erring outwards spends at most\none notch and cannot be silently wrong."
        },
        {
          "kind": "function",
          "name": "stackedThumb",
          "parameters": [
            {
              "name": "start"
            },
            {
              "name": "end"
            },
            {
              "name": "max"
            }
          ],
          "description": "Which thumb goes on top when the two are at the same value, or `null` while they are not.\n\nStacked, one thumb is under the other and out of reach of the pointer, so the one on top\nhas to be the one that can still move. At the maximum that is the low thumb, because the\nhigh one has nowhere left to go and every drag from there is downwards; anywhere else it\nis the high thumb, so a drag away from the pile widens the range rather than refusing to.\nEither is still reachable with <kbd>Tab</kbd>, which is why this decides the pointer only."
        },
        {
          "kind": "function",
          "name": "nearerThumb",
          "parameters": [
            {
              "name": "value"
            },
            {
              "name": "start"
            },
            {
              "name": "end"
            }
          ],
          "description": "Which thumb a press on the track belongs to: the nearer one, and on a tie the one the\npress is asking to move.\n\nThe tie is not a corner case, it is the whole of the piled-up state - with both thumbs on\none value, every point on the track is exactly as far from one as from the other, so\ndistance alone would answer the same thumb every time and half the track would be dead:\nthe clamp puts a low thumb dragged past the high one straight back where it came from."
        },
        {
          "kind": "function",
          "name": "alongTrack",
          "parameters": [
            {
              "name": "x"
            },
            {
              "name": "left"
            },
            {
              "name": "width"
            },
            {
              "name": "thumb"
            },
            {
              "name": "rtl"
            }
          ],
          "description": "Where a point on the control sits along the thumb's travel, as `0` to `1`.\n\nOn the scale's own axis, not the screen's: `0` is `min` in both writing directions, which\nis the same axis `--slider-elemental-start` and `--slider-elemental-end` are measured on\nand the same one `inset-inline-start` spends them on.\n\nThe travel is the box less one thumb, because a thumb's centre starts half a width in and\nstops half a width short - the same arithmetic the fill is placed with, and the reason a\nbare percentage of the width is off by half a thumb at both ends. A control with no room\nto travel answers `0`, for the reason `ratio` does: there is one place a thumb can be."
        },
        {
          "kind": "function",
          "name": "snapToStep",
          "parameters": [
            {
              "name": "value"
            },
            {
              "name": "min"
            },
            {
              "name": "max"
            },
            {
              "name": "step"
            },
            {
              "name": "direction"
            }
          ],
          "description": "A value put on the nearest notch, the way a range input puts its own there.\n\nThe notches are counted from `min` rather than from zero, which is the platform's rule:\n`min=\"1\" step=\"10\"` is 1, 11, 21, and a bubble reading out 10 for a press the input would\nmake 11 is a bubble that lies about what it is previewing. Ties round up, which is what\n[MDN says a user agent does](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/input/range)\nand what `Math.round` does.\n\nThe top of the scale is the last notch at or below `max`, not `max` - `max` is not always\na whole number of steps above `min`, and where it is not the end of the scale is a value\nthe input cannot hold. `min=\"0\" max=\"100\" step=\"40\"` stops at 80.\n\nRounded to the decimals the step is written with, because binary floating point cannot\nhold a tenth: three notches up a `step=\"0.1\"` scale is `0.30000000000000004` otherwise,\nand that is what the reader would see.\n\n`direction` picks which notch when the value falls between two: `-1` the one below, `1`\nthe one above, and nothing at all the nearest. Rounding to the nearest is right for a\nbubble previewing a press, and wrong for the gap between two thumbs - there, landing on\nthe near notch is landing closer to the other thumb than the gap allows, so the caller\nthat has a direction to give gives it."
        },
        {
          "kind": "function",
          "name": "stepOf",
          "parameters": [
            {
              "name": "input"
            }
          ],
          "description": "The notch spacing an input is actually on: unset means `1`, and `any` means no notches."
        },
        {
          "kind": "function",
          "name": "thumbUnder",
          "parameters": [
            {
              "name": "x"
            },
            {
              "name": "left"
            },
            {
              "name": "width"
            },
            {
              "name": "thumb"
            },
            {
              "name": "ratios"
            },
            {
              "name": "rtl"
            }
          ],
          "description": "Which thumb the pointer is over, by index, or `-1` for the track beside or between them.\n\nA pointer within half a thumb of a thumb's centre is on it, because that is the thumb's\nown box - one `--slider-elemental-thumb-size` across, centred on where it sits. The first\nmatch wins, and with two thumbs piled on one value that is the low one: they carry the\nsame value, so there is nothing to choose between what either would read out.\n\n`ratios` are on the scale's axis and this is screen pixels, which is the whole of the\n`rtl` flip - the low thumb of a range runs to the right-hand end when the page does."
        },
        {
          "kind": "function",
          "name": "draggedThumb",
          "parameters": [
            {
              "name": "under"
            },
            {
              "name": "count"
            }
          ],
          "description": "Which thumb a press holds the value bubble on until it is let go, or `-1` for a press that\ndrags no thumb at all.\n\nWhere the pointer is answers this everywhere except during a drag, and during a drag it is\nwrong twice over: a thumb snaps to notches while the pointer moves smoothly, so half a step\nout the pointer is beside the thumb it is dragging, and past either end it is off the\ncontrol entirely. Both would flip the bubble to a track reading under the reader's own\nhand. So the press decides, once.\n\nA press on the track is a drag only with one thumb, and that is the stylesheet's doing\nrather than a rule invented here: one input covers the whole control, so the native range\ntakes the press and carries on into a drag. Two are stacked with their pointer events on\nthe thumbs, so the press is this element's, moves the nearer thumb and stops - nothing is\nbeing dragged, and nothing should be pinned."
        },
        {
          "kind": "function",
          "name": "tooltipModes",
          "parameters": [
            {
              "name": "value"
            }
          ],
          "description": "Which value bubbles the `tooltip` attribute asked for.\n\nA bare `tooltip` is the thumb, because that is the half a slider is asked for and the half\nevery library that ships the feature ships. A token list rather than one value, so a page\ncan have the other half on its own or both at once without a third keyword to remember.\n\nAn unknown token is no bubble rather than an error: the attribute is a list, and refusing\nto draw anything is what tells the author the word did not land - louder than a thumb\nbubble they did not ask for and quieter than a page that throws."
        },
        {
          "kind": "class",
          "description": "One or two native range inputs sharing a track, with the fill position CSS cannot work\nout on its own. The prose above is the whole story; these tags are the same API in the\nform a machine can read - `custom-elements.json` is generated from them, and the docs\ntables, editor autocomplete and the live options panel all come out of that one file.\n\nCurated by omission: `--slider-elemental-start`, `--slider-elemental-end`,\n`--slider-elemental-at` and `data-stacked` are written by the element and are not tagged.\nThey are the state, not knobs to turn, and setting one by hand parks the fill or the\nbubble somewhere the thumbs are not.",
          "name": "SliderElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Thumb width and height. This is also the height of the control, and what the fill is inset by so its ends meet the thumb centres.",
              "name": "--slider-elemental-thumb-size",
              "default": "1rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Track thickness.",
              "name": "--slider-elemental-track-size",
              "default": "0.375rem"
            },
            {
              "type": {
                "text": "<length-percentage>"
              },
              "description": "Track corners. A big number is a pill at any height.",
              "name": "--slider-elemental-radius",
              "default": "999px"
            },
            {
              "type": {
                "text": "<length-percentage>"
              },
              "description": "Thumb shape. `50%` is a circle, `0` a square.",
              "name": "--slider-elemental-thumb-radius",
              "default": "50%"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The part of the track outside the selection.",
              "name": "--slider-elemental-track",
              "default": "color-mix(in srgb, currentcolor 20%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The part inside it.",
              "name": "--slider-elemental-fill",
              "default": "currentcolor"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Thumb fill. Follows the selection unless you set it.",
              "name": "--slider-elemental-thumb",
              "default": "var(--slider-elemental-fill)"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Ring around a focused thumb.",
              "name": "--slider-elemental-focus-width",
              "default": "3px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Ring colour.",
              "name": "--slider-elemental-focus-color",
              "default": "color-mix(in srgb, currentcolor 35%, transparent)"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between the thumb and the `tooltip` bubble above it.",
              "name": "--slider-elemental-tooltip-gap",
              "default": "0.375rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Above and below the number in the bubble.",
              "name": "--slider-elemental-tooltip-padding-block",
              "default": "0.25em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Either side of it.",
              "name": "--slider-elemental-tooltip-padding-inline",
              "default": "0.5em"
            },
            {
              "type": {
                "text": "<length-percentage>"
              },
              "description": "The bubble's corners.",
              "name": "--slider-elemental-tooltip-radius",
              "default": "6px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "What the bubble is painted in.",
              "name": "--slider-elemental-tooltip-surface",
              "default": "CanvasText"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The number on it.",
              "name": "--slider-elemental-tooltip-color",
              "default": "Canvas"
            }
          ],
          "slots": [
            {
              "description": "One `<input type=\"range\">` for a slider, two for a range, and optionally an `<output>` per input to read the value back.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "inputs",
              "description": "The thumbs, in document order. Direct children, so a range input inside a card this\nelement happens to wrap is not mistaken for one of them.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "outputs",
              "description": "The readouts, in document order, each following the input at the same index.\n\nAnywhere inside, unlike the inputs: a readout is nearly always wrapped in the\npunctuation that gives it meaning - a currency symbol, a unit, the word \"to\" between a\npair - and a rule that only saw direct children would refuse the shape everybody\nwrites. `closest` is what keeps a nested slider's readouts its own.\n\nThe `tooltip` bubble is an `<output>` too and is not one of these. Counted in, it would\ntake an index off the end of the list and the readouts would each be showing the value\nof the thumb before their own.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "gap",
              "description": "Least distance between the two thumbs, in the scale's own units."
            },
            {
              "kind": "field",
              "name": "rtl",
              "description": "Whether the control runs right to left. Computed style rather than `:dir()`, which\nthrows on the browsers that do not know it instead of quietly not matching.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "onReset",
              "description": "A form is only put back to its defaults once the `reset` event has been dispatched,\nso the values are read on the next task rather than in the handler."
            },
            {
              "kind": "method",
              "name": "onInput",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "clamp",
              "parameters": [
                {
                  "name": "moved"
                }
              ],
              "description": "Stop the two thumbs crossing, and keep `gap` between them. Public because it is what\ncatches up a pair moved by script, which fires no `input` of its own.\n\nBoth inputs keep the `min` and `max` the markup gave them, rather than the low one's\n`max` being pulled down to the high one's value as the\n[multi-thumb pattern](https://www.w3.org/WAI/ARIA/apg/patterns/slider-multithumb/)\ndescribes: these are stacked native inputs, so shrinking one's scale moves every pixel\non it and the two tracks stop agreeing about where a value is. The pattern's\n`aria-valuemin`/`aria-valuemax` go with it, and are not written for a second reason -\n[HTML-ARIA says authors should not put them on `input type=range`](https://www.w3.org/TR/html-aria/),\nwhere the browser computes them from `min`, `max` and `value` already."
            },
            {
              "kind": "method",
              "name": "apply",
              "description": "Push the thumb positions onto the element, where the CSS reads them. Public because the\ninputs are read here: swap one out, or move it from script, and this is the one call\nthat catches up."
            },
            {
              "kind": "method",
              "name": "syncTooltip",
              "description": "Put the value bubble in, or take it out, to match the `tooltip` attribute.\n\nWritten by this element rather than asked of the author: a box reading out where the\n*pointer* is has no markup anyone would have written anyway, and an `<output>` the page\nforgot would be a tooltip that silently never appeared. `aria-hidden`, because the input\nbeneath it announces its own value already."
            },
            {
              "kind": "method",
              "name": "removeTooltip",
              "description": "The bubble and the listeners that draw it, gone together. The element wrote the bubble,\nso the element is what takes it back off the page."
            },
            {
              "kind": "method",
              "name": "onPointerMove",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onTooltipDown",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "A press pins the bubble to whatever it is about to drag, for as long as it is held."
            },
            {
              "kind": "method",
              "name": "onTooltipUp",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "Let go, and where the pointer is decides again - including that it may have been let go\nsomewhere the bubble has no business still being.\n\nAnswered from the coordinates rather than from `target` or from a `pointerleave` that\nfollows, because neither survives the drag. A range input holds pointer capture until\nthe release, so `target` is the input wherever on the screen the pointer actually is;\nand the leave that would have covered it is not something every engine sends - Chromium\nfires one after the capture ends and WebKit fires none at all, which is a bubble left\non the page after every drag that ended off the control.\n\nA finger is not asked where it is: it is nowhere the moment it lifts, however far inside\nthe control it let go, so the release is the end of the bubble and not a question."
            },
            {
              "kind": "method",
              "name": "onPointerLeave"
            },
            {
              "kind": "method",
              "name": "metrics",
              "parameters": [
                {
                  "name": "x"
                }
              ],
              "description": "Everything a bubble is drawn from, measured in one go: the scale, where each thumb sits\non it, and which one a pointer at `x` is over. `null` where there is nothing to measure."
            },
            {
              "kind": "method",
              "name": "showTooltipAt",
              "parameters": [
                {
                  "name": "x"
                }
              ],
              "description": "Draw the bubble for a pointer at `x`, in viewport coordinates, or hide it where the\nattribute did not ask for a bubble at that spot.\n\nA thumb reads out its input's own `value`, which the browser has already put on a step\nand written the way it writes numbers. The track is the only one with a value to work\nout, and it is worked out the way a press on the track is - same travel, same rounding -\nso the number previewed is the number a click there would produce.\n\nA drag in progress overrules the pointer: `dragging` is the thumb the press pinned this\nto, and it holds until the release. Without it the bubble answers where the pointer is,\nwhich during a drag is beside the thumb half the time."
            },
            {
              "kind": "method",
              "name": "formatValue",
              "parameters": [
                {
                  "name": "value"
                },
                {
                  "name": "fallback"
                }
              ],
              "description": "What the bubble says for a value, once `format` has had it.\n\n`fallback` is the browser's own spelling of the same number and is what shows whenever\nthere is no formatter, so an element nobody has assigned one to reads exactly as it did\nbefore this hook existed. A formatter returning nothing falls back too - a bubble that\nwent blank because a function forgot a `return` looks like a broken element rather than\na bug in the page."
            },
            {
              "kind": "method",
              "name": "onPointerDown",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "A press on the track, which stacked inputs would otherwise eat: the one on top covers\nthe whole width, so the stylesheet takes its pointer events away and leaves them on the\nthumbs. That is what makes both thumbs grabbable, and it is also what leaves the track\ndead until this runs - the nearer thumb takes the value and the focus."
            },
            {
              "type": {
                "text": "?function"
              },
              "description": "What the `tooltip` bubble says, called as `(value, element)` and returning what lands in it. A number is all the bubble can say on its own, and a scrubber needs `01:12` where a price needs `€40` — neither is something an attribute could spell. Unset, or returning nothing, leaves the browser's own spelling of the value. Does not touch what the input announces: assistive technology reads the range, which still says the number.",
              "name": "format",
              "kind": "field"
            }
          ],
          "attributes": [
            {
              "name": "gap",
              "type": {
                "text": "number"
              },
              "description": "Least distance between the two thumbs, in the scale's own units. Ignored with one thumb.",
              "default": "0"
            },
            {
              "name": "tooltip",
              "type": {
                "text": "string"
              },
              "description": "A value bubble that follows the pointer. `thumb` over the thumb it is on, `track` for the value under it elsewhere, `thumb track` for both; a bare `tooltip` is `thumb`. Pointer only, and `aria-hidden` — the input announces its own value. Touch draws it for the length of a press, since a finger has no hover to rest in."
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "slider-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "ratio",
          "declaration": {
            "name": "ratio",
            "module": "src/elementals/slider/index.js"
          }
        },
        {
          "kind": "js",
          "name": "clampPair",
          "declaration": {
            "name": "clampPair",
            "module": "src/elementals/slider/index.js"
          }
        },
        {
          "kind": "js",
          "name": "stackedThumb",
          "declaration": {
            "name": "stackedThumb",
            "module": "src/elementals/slider/index.js"
          }
        },
        {
          "kind": "js",
          "name": "nearerThumb",
          "declaration": {
            "name": "nearerThumb",
            "module": "src/elementals/slider/index.js"
          }
        },
        {
          "kind": "js",
          "name": "alongTrack",
          "declaration": {
            "name": "alongTrack",
            "module": "src/elementals/slider/index.js"
          }
        },
        {
          "kind": "js",
          "name": "snapToStep",
          "declaration": {
            "name": "snapToStep",
            "module": "src/elementals/slider/index.js"
          }
        },
        {
          "kind": "js",
          "name": "stepOf",
          "declaration": {
            "name": "stepOf",
            "module": "src/elementals/slider/index.js"
          }
        },
        {
          "kind": "js",
          "name": "thumbUnder",
          "declaration": {
            "name": "thumbUnder",
            "module": "src/elementals/slider/index.js"
          }
        },
        {
          "kind": "js",
          "name": "draggedThumb",
          "declaration": {
            "name": "draggedThumb",
            "module": "src/elementals/slider/index.js"
          }
        },
        {
          "kind": "js",
          "name": "tooltipModes",
          "declaration": {
            "name": "tooltipModes",
            "module": "src/elementals/slider/index.js"
          }
        },
        {
          "kind": "js",
          "name": "SliderElemental",
          "declaration": {
            "name": "SliderElemental",
            "module": "src/elementals/slider/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/splitter/index.js",
      "declarations": [
        {
          "kind": "variable",
          "name": "DEFAULT_POSITION",
          "type": {
            "text": "number"
          },
          "default": "50",
          "description": "Down the middle, when nothing says otherwise."
        },
        {
          "kind": "variable",
          "name": "STEP",
          "type": {
            "text": "number"
          },
          "default": "1",
          "description": "What one arrow key is worth, as a percentage of the track. Not an attribute: a splitter\nwhose arrow key moved by a configurable amount would be a second number to keep in step\nwith `min` and `max`, and nothing in the pattern asks for one."
        },
        {
          "kind": "function",
          "name": "splitterKey",
          "parameters": [
            {
              "name": "key",
              "description": "KeyboardEvent.key value.",
              "type": {
                "text": "string"
              }
            },
            {
              "name": "vertical",
              "description": "Whether the panes are stacked down the page.",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "rtl",
              "description": "Whether the layout runs right to left.",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "What a key does to the primary pane, or `null` where the key is not this element's.\n\nThe names are the pane's rather than the screen's - `shrink` and `grow` instead of left and\nup - because which physical arrow shrinks the pane is exactly the part that moves. It\nchanges with the axis, and on the horizontal axis it changes again with the writing\ndirection: in a right-to-left page the primary pane is the one on the right, so\n<kbd>ArrowLeft</kbd> is the key that makes it bigger.\n\nOnly the axis the splitter is on answers. <kbd>ArrowUp</kbd> on a splitter between a left\nand a right pane is not a key with nothing to do - it is the page's key, for scrolling, and\nswallowing it is how a widget takes the document's keyboard away from a reader who is\ninside it.",
          "return": {
            "type": {
              "text": "'shrink'|'grow'|'min'|'max'|'collapse'|null"
            }
          }
        },
        {
          "kind": "function",
          "name": "positionFrom",
          "parameters": [
            {
              "name": "rect",
              "description": "The element's box.",
              "type": {
                "text": "DOMRect|{left: number, top: number, width: number, height: number}"
              }
            },
            {
              "name": "x",
              "description": "Viewport coordinates, as `PointerEvent.clientX` gives them.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "y",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "options",
              "default": "{}",
              "description": "`size` is the handle's\nextent along the axis, in pixels.",
              "optional": true,
              "type": {
                "text": "{vertical?: boolean, rtl?: boolean, size?: number}"
              }
            }
          ],
          "description": "Where a pointer puts the separator, as a percentage of the track.\n\n**The track is the box minus the handle, not the box.** The handle occupies a column of its\nown, so the primary pane can only ever be as wide as what is left over - and a percentage\nmeasured against the full box would mean `100` is a pane one handle wider than the space\nthere is, which overflows by that much at one end and cannot be reached at the other. The\nstylesheet sizes the first track out of the same subtraction, so the two agree by\nconstruction rather than by a constant written twice.\n\nHalf a handle comes off the pointer's own position for the same reason: the reader grabbed\nthe middle of the handle, and the track starts at its leading edge.",
          "return": {
            "type": {
              "text": "number"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<splitter-elemental>` custom element.\n\nTwo panes and a handle between them that resizes both: the\n[APG Window Splitter pattern](https://www.w3.org/WAI/ARIA/apg/patterns/windowsplitter/),\nover the two boxes you were going to write anyway.\n\n**The pattern has no reference example.** The APG page carries the roles, the states and the\nkeys, and where the example would be it says work on one is tracked by\n[issue 130](https://github.com/w3c/aria-practices/issues/130). So the keyboard here is read\noff the pattern's own prose rather than copied from a demo: arrows move the separator,\n<kbd>Home</kbd> and <kbd>End</kbd> take the primary pane to the smallest and largest size it\nis allowed, and <kbd>Enter</kbd> collapses it and puts it back.\n\nThe first element child is the primary pane - the one `aria-valuenow` is about, the one\n`aria-controls` names, and the one <kbd>Enter</kbd> collapses. The second is the other pane.\nThe handle is written between them, and it is the only node this element adds.\n\n**`vertical` is the panes' word, not the separator's, and the two are opposites.** Stacked\npanes are split by a separator lying across the page, so `vertical` writes\n`aria-orientation=\"horizontal\"` - which is also the role's own default, and therefore the\nvalue that is left off. The APG spends the word the other way round and calls the splitter\nbetween a left and a right pane a vertical one; ARIA is describing the separator, this\nelement's attribute is describing the layout, and both are right about different things.\n\n`min` and `max` are the floor and the ceiling for everything - the pointer, the keys, and\n<kbd>Enter</kbd> too. They are `aria-valuemin` and `aria-valuemax` verbatim, which is what\nthe pattern says those two properties are: the positions where the primary pane is at its\nsmallest and its largest. So a `min` above zero is an author saying the pane may not\ndisappear, and <kbd>Enter</kbd> takes it down to that floor rather than through it. The\npattern's collapse is what the default `min=\"0\"` gives you.\n\nPercentages and nothing else. A `min-width` in pixels on a pane is a size this element\ncannot see, and grid honours it while `aria-valuenow` goes on reporting the position that\nwas asked for - two numbers disagreeing about the same pane. The bound belongs on `min`.\n\nLight DOM, no shadow root. With no script it is two boxes in normal flow, in the order you\nwrote them, and no handle - there is nothing to resize when there is nothing listening.\n\nponytail: no persistence, no `localStorage`, no session key. `position` is reflected, so\nsaving it is `splitter.addEventListener('splitter-change', e => store(e.detail.position))`\nand restoring it is an attribute in the markup - an element that owned a storage key would\nbe an element with an opinion about which one.\n\nponytail: two panes, not n. Three panes is two splitters, and a splitter that shared its\nneighbour's space would need a layout model of the whole row rather than of itself.",
          "name": "SplitterElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "The handle's thickness, and therefore the size of the target a pointer has to hit. `24px` is [WCAG 2.2 2.5.8](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html)'s minimum rather than a taste; the theme draws a thinner line inside it.",
              "name": "--splitter-elemental-size",
              "default": "24px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Theme. The line drawn down the middle of the handle.",
              "name": "--splitter-elemental-color",
              "default": "currentcolor at 20%"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Theme. Its colour while the pointer is on the handle or the handle has focus.",
              "name": "--splitter-elemental-active-color",
              "default": "currentcolor at 45%"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Theme. How thick that line is. Not the target: the handle stays `--splitter-elemental-size` whatever this says.",
              "name": "--splitter-elemental-line-size",
              "default": "1px"
            }
          ],
          "slots": [
            {
              "description": "Two boxes. The first is the primary pane, the second is the other one; a third lands on a second grid row, under the primary pane and the width of it.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "handle",
              "type": {
                "text": "null"
              },
              "default": "null",
              "description": "The handle, `null` until there are two panes to put one between."
            },
            {
              "kind": "field",
              "name": "restorePosition",
              "default": "50",
              "description": "Where the primary pane was before <kbd>Enter</kbd> collapsed it, so the second press has\nsomewhere to go. The middle until something else has been seen, rather than `min` - a\nrestore that put the pane back where it already was would read as a key that did nothing.",
              "type": {
                "text": "number"
              }
            },
            {
              "kind": "field",
              "name": "pointerId",
              "type": {
                "text": "null"
              },
              "default": "null",
              "description": "The pointer id of the drag in progress, `null` when there is none. Held so a second\nfinger arriving mid-drag is ignored rather than fighting the first."
            },
            {
              "kind": "field",
              "name": "dragRtl",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "The writing direction as it was when the drag started. Read once per gesture: `direction`\nis inherited, and re-reading it per frame is a style resolution inside a pointer handler."
            },
            {
              "kind": "field",
              "name": "dragMoved",
              "type": {
                "text": "boolean"
              },
              "default": "false",
              "description": "Whether the drag in progress has moved the separator at all. A press that let go where it\nlanded changed nothing, and an event saying it did is one a listener would save to storage."
            },
            {
              "kind": "field",
              "name": "panes",
              "description": "The panes: every element child that is not the handle. The handle is written between\nthem, so it has to come out of the count that decides where it goes.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "position",
              "description": "Where the separator sits, clamped into `min`-`max`. The attribute is the state, so a\ndrag, a key and a page setting `position=\"30\"` all arrive the same way."
            },
            {
              "kind": "field",
              "name": "min",
              "description": "How far the primary pane may shrink."
            },
            {
              "kind": "field",
              "name": "max",
              "description": "How far it may grow. Floored at `min`, so a pair given the wrong way round is a splitter\nthat will not move rather than one whose clamp inverts."
            },
            {
              "kind": "field",
              "name": "vertical",
              "description": "Whether the panes are stacked down the page. Reflected, so `[vertical]` is a styling\nhook and the stylesheet needs nothing written for it."
            },
            {
              "kind": "field",
              "name": "labelText",
              "description": "The handle's accessible name."
            },
            {
              "kind": "method",
              "name": "build",
              "description": "Write the handle between the panes and wire it up.\n\nA `<div>` and not a `<button>`, because `role=\"separator\"` would replace the button role\nand leave a control announcing itself as one thing while behaving as another - and the\nonly thing the button element brings that is wanted here, the tab stop, is one attribute.\n\nThe pattern needs the primary pane to be identifiable, so it is given an id if it has\nnone. Everything written here comes off again in `disconnectedCallback`, except that id:\nsomething else may be pointing at it by then."
            },
            {
              "kind": "method",
              "name": "render",
              "description": "Put the position where the stylesheet and a screen reader can both read it.\n\nThe custom property is unitless and the stylesheet spends it, which is what keeps the one\nsubtraction that matters - the track is the box minus the handle - in the one place that\ncan do it in the browser's units rather than in measured pixels.\n\n`aria-valuenow` is rounded and `aria-valuetext` says the per cent out loud, because a\nseparator announcing \"37.482\" is a number with no unit attached to it and a pane size is\nnever anything but a proportion.\n\n`aria-orientation` is written only for side-by-side panes: `horizontal` is the separator\nrole's own default, and writing a default is a second place for it to be wrong."
            },
            {
              "kind": "method",
              "name": "moveTo",
              "parameters": [
                {
                  "name": "position"
                },
                {
                  "name": "commit"
                }
              ],
              "description": "Move the separator, and say so once the gesture that moved it is over."
            },
            {
              "kind": "method",
              "name": "onKeyDown",
              "parameters": [
                {
                  "name": "event"
                }
              ]
            },
            {
              "kind": "method",
              "name": "rtl",
              "description": "Whether the layout runs right to left. Read per gesture rather than held: `direction` is\ninherited, so an ancestor flipping it is a change no attribute on this element reports."
            },
            {
              "kind": "method",
              "name": "onPointerDown",
              "parameters": [
                {
                  "name": "event"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onPointerMove",
              "parameters": [
                {
                  "name": "event"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onPointerUp",
              "parameters": [
                {
                  "name": "event"
                }
              ]
            },
            {
              "kind": "method",
              "name": "endDrag",
              "description": "Let go of the pointer and stop listening for it. Safe to call twice, which is what a\n`pointercancel` arriving after a `pointerup` needs it to be."
            },
            {
              "kind": "method",
              "name": "positionFromEvent",
              "parameters": [
                {
                  "name": "event"
                }
              ],
              "description": "Where the pointer puts the separator. The handle's own extent comes out of the sum, and\nit is measured rather than assumed: `--splitter-elemental-size` is the page's to set."
            }
          ],
          "events": [
            {
              "name": "splitter-change",
              "type": {
                "text": "CustomEvent"
              },
              "description": "The separator has been moved and the gesture is over - a key press, or a pointer released. `detail.position` is where it landed. Deliberately not fired per frame of a drag: a pane that has to keep up with one is a pane with a `ResizeObserver` on it, which is the platform's own answer and does not go stale."
            }
          ],
          "attributes": [
            {
              "name": "position",
              "type": {
                "text": "number"
              },
              "description": "Where the separator sits, as a percentage of the track. Reflected, and written back as the handle is dragged.",
              "default": "50"
            },
            {
              "name": "min",
              "type": {
                "text": "number"
              },
              "description": "How far the primary pane may shrink, as a percentage. The floor for the pointer, the arrows and <kbd>Enter</kbd> alike, and `aria-valuemin`.",
              "default": "0"
            },
            {
              "name": "max",
              "type": {
                "text": "number"
              },
              "description": "How far it may grow. `aria-valuemax`.",
              "default": "100"
            },
            {
              "name": "vertical",
              "type": {
                "text": "boolean"
              },
              "description": "The panes are stacked down the page rather than side by side. Swaps the arrow keys with them.",
              "default": "false"
            },
            {
              "name": "label-text",
              "type": {
                "text": "string"
              },
              "description": "The handle's accessible name. The pattern asks for the separator to be named after the primary pane, so a page with a sidebar behind it says `label-text=\"Sidebar\"`. `--splitter-elemental-position` is deliberately not tagged below. The element writes it into its own `style` attribute, where an inline declaration beats any stylesheet - a knob for it would be a control that cannot move anything. It is an output, and `position` is the input.",
              "default": "Resize"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "splitter-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "DEFAULT_POSITION",
          "declaration": {
            "name": "DEFAULT_POSITION",
            "module": "src/elementals/splitter/index.js"
          }
        },
        {
          "kind": "js",
          "name": "STEP",
          "declaration": {
            "name": "STEP",
            "module": "src/elementals/splitter/index.js"
          }
        },
        {
          "kind": "js",
          "name": "splitterKey",
          "declaration": {
            "name": "splitterKey",
            "module": "src/elementals/splitter/index.js"
          }
        },
        {
          "kind": "js",
          "name": "positionFrom",
          "declaration": {
            "name": "positionFrom",
            "module": "src/elementals/splitter/index.js"
          }
        },
        {
          "kind": "js",
          "name": "SplitterElemental",
          "declaration": {
            "name": "SplitterElemental",
            "module": "src/elementals/splitter/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/suggest/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "suggestAction",
          "parameters": [
            {
              "name": "key",
              "description": "`KeyboardEvent.key`",
              "type": {
                "text": "string"
              }
            },
            {
              "name": "altKey",
              "description": "Whether Alt was held",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "open",
              "description": "Whether the popup is showing",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "cursor",
              "description": "Whether a row is already under the cursor",
              "optional": true,
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "tabCompletes",
              "description": "Whether Tab may take the row under the cursor",
              "optional": true,
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "What a key means to a listbox popup, given whether it is showing.\n\nThe [APG combobox pattern](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) keyboard\nmap for a textbox with a listbox popup, as a table rather than a branch tree, so it can\nbe read and tested without a DOM.\n\n`Home` and `End` are the pattern's one genuinely two-sided key, and it calls them\noptional: either they jump the list, or - \"if the combobox is editable\" - they return the\ncaret to the first character of what was typed. This field is always editable, so both\nanswers are right at different moments, and `cursor` is which moment it is.\n\nBefore an arrow key has put a cursor on a row the reader is still writing a query, and a\n`Home` that jumped the list rather than reaching the start of `install` would be the\nwrong answer on nearly every press. Once there is a cursor they are reading results\ninstead of writing, and the ends of the list are the only thing those keys can mean.\n\n`close` and `leave` both shut the popup and differ in what the key still has to do after.\nEscape is finished once the popup is gone, so its own default - emptying a search field -\nis the page's to keep. Tab was on its way out of the field, and swallowing it would cost\na second press to do what the first one already said.\n\n`tabCompletes` is the one exception, and it is opt-in because the rows here are links: a\nTab that took the one under the cursor would navigate away on a keystroke that means\n\"move along\". Where the rows are text about to be typed - a mention, an emoji - it is what\nevery editor does, so the markup asks for it rather than the element guessing.",
          "return": {
            "type": {
              "text": "\"open\"|\"open-first\"|\"open-last\"|\"move\"|\"first\"|\"last\"|\"activate\"|\"close\"|\"leave\"|null"
            }
          }
        },
        {
          "kind": "function",
          "name": "suggestState",
          "parameters": [
            {
              "name": "open",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "activeId",
              "description": "`id` of the option the cursor is on, or null for none",
              "type": {
                "text": "string|null"
              }
            }
          ],
          "description": "The state a listbox and its control carry, for a given open state and cursor.\n\n`activedescendant: null` means the attribute comes off rather than being written empty:\nan `aria-activedescendant` pointing at nothing is a control claiming a cursor it does\nnot have, and screen readers announce the stale option.",
          "return": {
            "type": {
              "text": "{expanded: string, hidden: boolean, activedescendant: string|null}"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<suggest-elemental>` custom element.\n\nA list of links a text field can drive with the arrow keys, per the\n[APG Combobox pattern](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/) with a\nlistbox popup - the results panel every search field, filter and \"jump to\" box ends up\nneeding, and the half of it that has nothing to do with where the results came from.\n\nIt owns the keyboard and the ARIA and nothing else: no fetching, no filtering, no\nopinion about what put the links there. Give it an `<ul>` of `<a>` and point it at an\ninput, and typing, arrowing and Enter behave the way the pattern says.\n\n**Not `<combobox-elemental>`.** That one is a view of a `<select>`: it holds a value, it\nsubmits under a name, its options carry `aria-selected`. These options are links - they\nare destinations, not values, and there is nothing to select. The two share the cursor\nmechanics and nothing else.\n\nFocus never enters the list. The cursor is `aria-activedescendant` on the control, which\nis what lets someone keep typing while walking the results - a roving `tabindex` would\ntake the caret out of the field on the first arrow key.\n\nLight DOM, no shadow root. Degrades to what it is: with no script, a list of links,\nvisible and reachable. Nothing is authored `hidden`, so nothing is lost when the script\nis not there.",
          "name": "SuggestElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Corners of the popup.",
              "name": "--suggest-elemental-radius",
              "default": "0.375rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "The one padding unit: down the side of every option, and three quarters of it above and below the text.",
              "name": "--suggest-elemental-inset",
              "default": "0.5rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "How tall the popup gets before it scrolls.",
              "name": "--suggest-elemental-max-height",
              "default": "20rem"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "What the popup is painted on.",
              "name": "--suggest-elemental-surface",
              "default": "Canvas"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The option the cursor is on - where Enter would land, and what the pointer moves.",
              "name": "--suggest-elemental-active",
              "default": "color-mix(in srgb, currentcolor 12%, transparent)"
            }
          ],
          "slots": [
            {
              "description": "The `<ul>` of `<a>` the popup shows. Replace its contents whenever you like; the element re-marks them.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "control",
              "description": "The text field driving this popup - what `for` names.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "options",
              "description": "The options, in document order. Links only: an `<a>` with no `href` is not a\ndestination, and a listbox option that goes nowhere is a dead row on the list.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "open",
              "description": "Whether the popup is showing. Reflected, so `[open]` is a styling hook too."
            },
            {
              "kind": "field",
              "name": "tabCompletes",
              "description": "Whether Tab takes the row under the cursor rather than leaving the field."
            },
            {
              "kind": "method",
              "name": "mark",
              "description": "Give the current children the roles and `id`s the pattern needs.\n\nA `listbox` may only own `option`s, and the markup between them here is a `<ul>` and\nan `<li>` per row - both of which carry list semantics of their own. `presentation`\ntakes those off without taking the boxes away, so the options are owned by the listbox\ndirectly and the CSS still has its list to lay out."
            },
            {
              "kind": "method",
              "name": "apply",
              "description": "Push `open` onto the popup and its control."
            },
            {
              "kind": "method",
              "name": "applyCursor",
              "description": "Write the cursor - `aria-activedescendant` on the control, a marker on the option."
            },
            {
              "kind": "method",
              "name": "moveTo",
              "parameters": [
                {
                  "name": "index",
                  "type": {
                    "text": "number|null"
                  }
                }
              ],
              "description": "Move the cursor to an index, and scroll it into view.\n\n`nearest` rather than `center`: the cursor usually moves one row at a time, and a\npopup that re-centres on every arrow key slides the whole list under the reader when\nonly one line needed to come into view."
            },
            {
              "kind": "method",
              "name": "place",
              "description": "Put the popup where there is room for it, as two attributes for the CSS to key off.\n\nThe element does not write coordinates: a light-DOM popup lives in the page's own\nstacking and layout, and an element setting `top` and `left` on it is an element\nfighting whatever the page already decided. `data-side` and `data-align` say which\ncorner won, and the stylesheet spends them."
            },
            {
              "kind": "method",
              "name": "onKeyDown",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onFocusOut",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onPointerMove",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onMouseDown",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            }
          ],
          "events": [
            {
              "name": "suggest-toggle",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.open` is the new state."
            }
          ],
          "attributes": [
            {
              "name": "open",
              "type": {
                "text": "boolean"
              },
              "description": "Whether the popup is showing. Reflected, so `[open]` is a styling hook, and settable so whatever fills the list can show it.",
              "default": "false"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "`id` of the text field that drives it. Also read as `data-for`. Without it the element does nothing.",
              "name": "for"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Tab takes the row under the cursor instead of leaving. For a completer whose rows are text about to be typed; wrong for a list of links, which is why it is opt-in.",
              "name": "tab-completes",
              "default": "false"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "suggest-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "suggestAction",
          "declaration": {
            "name": "suggestAction",
            "module": "src/elementals/suggest/index.js"
          }
        },
        {
          "kind": "js",
          "name": "suggestState",
          "declaration": {
            "name": "suggestState",
            "module": "src/elementals/suggest/index.js"
          }
        },
        {
          "kind": "js",
          "name": "SuggestElemental",
          "declaration": {
            "name": "SuggestElemental",
            "module": "src/elementals/suggest/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/switch/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "formValue",
          "parameters": [
            {
              "name": "checked"
            },
            {
              "name": "disabled"
            },
            {
              "name": "value"
            }
          ],
          "description": "What a form takes from the switch: the value when it is on, and nothing at all when it\nis off or disabled. `null` rather than an empty string, because an unchecked checkbox\ndoes not submit an empty value - it does not appear in the form data at all, and that\nabsence is what every server-side \"was this box ticked\" check is already written\nagainst. A disabled control submits nothing either, which is the platform's rule for\nevery other form control and has to be honoured by hand here."
        },
        {
          "kind": "function",
          "name": "validityState",
          "parameters": [
            {
              "name": "required"
            },
            {
              "name": "checked"
            },
            {
              "name": "customMessage"
            },
            {
              "name": "missingMessage"
            }
          ],
          "description": "The validity a switch can have, which is one thing: a required switch has to be on.\nA custom message wins, as it does on a native control - it is the author saying the\nvalue is wrong for a reason the browser cannot know."
        },
        {
          "kind": "function",
          "name": "seedChecked",
          "parameters": [
            {
              "name": "selector"
            },
            {
              "name": "root"
            },
            {
              "name": "checked"
            },
            {
              "name": "report"
            }
          ],
          "description": "What a `checked-if` switch starts in: whether the root element matches the selector the\nauthor wrote. Asked once, at upgrade, and only of the root - `<html>` is the one element\ncertain to be parsed whenever a switch upgrades, and a condition that silently never\nmatches because it named something further down the page is worse than no condition.\n\nThe point of asking this early is that the button is `display: none` until the element is\ndefined, so a state decided here is the first one painted. Anything setting `checked`\nafterwards - a bundle at `DOMContentLoaded`, a framework - has already lost that race.\n\nA selector the browser cannot parse leaves the markup's own state standing and hands the\nerror to `report` rather than throwing it here: by upgrade there is a visible button, and an\nexception out of `connectedCallback` leaves it sitting there with no `role` on it - a switch\nthat stopped being a switch because of a typo. Reported and not swallowed, though, because a\ncondition that quietly never matches is the kind of bug you look for everywhere else first."
        },
        {
          "kind": "function",
          "name": "borrowedValueMissingMessage",
          "description": "What a required switch says while it is off, when nobody has said otherwise.\n\n`setValidity` throws on an empty message, and a form-associated custom element gets no\nplatform default to ask for - so the nearest true thing is borrowed: the browser's own\nmessage for a required checkbox, which arrives already translated into the reader's\nlanguage. It says \"box\" rather than \"switch\", and a reader given the right language\nwith the wrong noun is better served than one given English. Read once and kept."
        },
        {
          "kind": "class",
          "description": "A real `<button>` turned into an on/off switch, per the APG Switch pattern. Submits\nwith its form when given a `name`. The prose above is the whole story; these tags are\nthe same API in the form a machine can read - `custom-elements.json` is generated from\nthem, and the docs tables, editor autocomplete and the live options panel in the docs\nall come out of that one file rather than out of four hand-kept copies.\n\nCurated by omission: `--switch-elemental-inset`, `--switch-elemental-knob-size` and\n`--switch-elemental-travel` are `calc()`-derived from the four geometry properties and\nare deliberately not tagged. Setting them by hand is how a knob ends up overshooting\nits own track, and anything tagged here is something a reader is being invited to turn.",
          "name": "SwitchElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Track width.",
              "name": "--switch-elemental-width",
              "default": "3.625rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Track height.",
              "name": "--switch-elemental-height",
              "default": "2rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Track corners. The height is a pill.",
              "name": "--switch-elemental-radius",
              "default": "var(--switch-elemental-height)"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Track border width.",
              "name": "--switch-elemental-border-width",
              "default": "2px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Track border, off.",
              "name": "--switch-elemental-border-color",
              "default": "currentcolor"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Track border, on.",
              "name": "--switch-elemental-border-color-checked",
              "default": "var(--switch-elemental-border-color)"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between the knob and the inside of the track.",
              "name": "--switch-elemental-gap",
              "default": "2px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Track fill, off.",
              "name": "--switch-elemental-track",
              "default": "transparent"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Track fill, on.",
              "name": "--switch-elemental-track-checked",
              "default": "currentcolor"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Knob fill, off.",
              "name": "--switch-elemental-knob",
              "default": "currentcolor"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Knob fill, on. The page's own background, so re-point it on a card.",
              "name": "--switch-elemental-knob-checked",
              "default": "Canvas"
            },
            {
              "type": {
                "text": "<length-percentage>"
              },
              "description": "Knob shape. `50%` is a circle, `0` a square.",
              "name": "--switch-elemental-knob-radius",
              "default": "50%"
            },
            {
              "type": {
                "text": "<time>"
              },
              "description": "Slide and cross-fade.",
              "name": "--switch-elemental-duration",
              "default": "250ms"
            },
            {
              "type": {
                "text": "ease | ease-in | ease-out | ease-in-out | linear"
              },
              "description": "Slide and cross-fade.",
              "name": "--switch-elemental-easing",
              "default": "ease-in-out"
            }
          ],
          "slots": [
            {
              "description": "The `<button>` that flips, and optionally a `.switch-elemental-on` and a `.switch-elemental-off` icon inside it.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "formAssociated",
              "static": true,
              "readonly": true
            },
            {
              "kind": "field",
              "name": "requiredMessage",
              "type": {
                "text": "null"
              },
              "static": true,
              "default": "null",
              "description": "The page-wide default for what a required switch says while it is off. `null` means\nthe browser's own translated message is used, which is the right answer until a page\nhas a reason of its own - one line at boot changes every switch on it."
            },
            {
              "kind": "field",
              "name": "button",
              "description": "The `<button>` that flips. Direct child, so a button in a label beside it - or\nin a second switch nested somewhere below - is not mistaken for the control.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "checked",
              "description": "Whether the switch is on. Reflected, so `[checked]` is a styling hook too."
            },
            {
              "kind": "field",
              "name": "disabled",
              "description": "Disabled by its own attribute, or by a `<fieldset disabled>` somewhere above it -\nwhich the button already answers for, since `:disabled` matches a button inside a\ndisabled fieldset whether or not it carries the attribute itself."
            },
            {
              "kind": "field",
              "name": "required",
              "description": "Whether the form refuses to submit while this is off."
            },
            {
              "kind": "field",
              "name": "requiredMessage",
              "description": "What this switch says while it is required and off, in three steps: its own\n`required-message`, then whatever the page put on `SwitchElemental.requiredMessage`,\nthen the browser's own translated one. One switch, one page, or every language."
            },
            {
              "kind": "field",
              "name": "validity",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "validationMessage",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "willValidate",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "checkValidity"
            },
            {
              "kind": "method",
              "name": "reportValidity"
            },
            {
              "kind": "method",
              "name": "setCustomValidity",
              "parameters": [
                {
                  "name": "message"
                }
              ],
              "description": "Your own message, for the constraint the browser cannot know about. `''` clears it."
            },
            {
              "kind": "method",
              "name": "validate",
              "description": "Push the current constraint onto the form. The button is the anchor, so the\nbrowser's own bubble points at the control the reader has to flip - and not at an\nelement that is `display: contents` and has no box to point at."
            },
            {
              "kind": "field",
              "name": "value",
              "description": "What the form submits when the switch is on. `on`, as a checkbox's is."
            },
            {
              "kind": "method",
              "name": "apply",
              "description": "Push the current state onto the button, and onto the form if there is one."
            },
            {
              "kind": "method",
              "name": "formDisabledCallback",
              "parameters": [
                {
                  "name": "disabled"
                }
              ],
              "description": "The element's own `disabled` attribute, or a `<fieldset disabled>` above it. The\nbutton is disabled with it, because a switch that takes focus and then does nothing\nis worse than one that is plainly out of reach - and the form value goes with it."
            },
            {
              "kind": "method",
              "name": "formResetCallback",
              "description": "The form is putting its controls back to the state the markup arrived in."
            },
            {
              "kind": "method",
              "name": "formStateRestoreCallback",
              "parameters": [
                {
                  "name": "state"
                }
              ],
              "description": "The browser is restoring this control after a back-navigation or a session restore,\nwith whatever `setFormValue` last put in. Off submitted nothing, so nothing coming\nback is off."
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            }
          ],
          "events": [
            {
              "name": "switch-toggle",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.checked` is the new state. Fires on flip, not on `value` or `required` changing."
            }
          ],
          "attributes": [
            {
              "name": "checked",
              "type": {
                "text": "boolean"
              },
              "description": "Whether it is on. Reflected, so markup, script and CSS read the same thing.",
              "default": "false"
            },
            {
              "name": "value",
              "type": {
                "text": "string"
              },
              "description": "What it submits while on.",
              "default": "on"
            },
            {
              "name": "disabled",
              "type": {
                "text": "boolean"
              },
              "description": "Disables the button and submits nothing. A `<fieldset disabled>` does the same.",
              "default": "false"
            },
            {
              "name": "required",
              "type": {
                "text": "boolean"
              },
              "description": "The form will not submit while it is off.",
              "default": "false"
            },
            {
              "name": "required-message",
              "type": {
                "text": "string"
              },
              "description": "What this one says while it is required and off."
            },
            {
              "type": {
                "text": "string"
              },
              "description": "A selector. The switch starts on when `<html>` matches it, asked once at upgrade — for a setting the document already knows, like a theme toggle.",
              "name": "checked-if"
            },
            {
              "type": {
                "text": "string"
              },
              "description": "Submits under this name. No name, no form data.",
              "name": "name"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "switch-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "formValue",
          "declaration": {
            "name": "formValue",
            "module": "src/elementals/switch/index.js"
          }
        },
        {
          "kind": "js",
          "name": "validityState",
          "declaration": {
            "name": "validityState",
            "module": "src/elementals/switch/index.js"
          }
        },
        {
          "kind": "js",
          "name": "seedChecked",
          "declaration": {
            "name": "seedChecked",
            "module": "src/elementals/switch/index.js"
          }
        },
        {
          "kind": "js",
          "name": "borrowedValueMissingMessage",
          "declaration": {
            "name": "borrowedValueMissingMessage",
            "module": "src/elementals/switch/index.js"
          }
        },
        {
          "kind": "js",
          "name": "SwitchElemental",
          "declaration": {
            "name": "SwitchElemental",
            "module": "src/elementals/switch/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/tabs/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "tabKey",
          "parameters": [
            {
              "name": "key",
              "description": "KeyboardEvent.key value.",
              "type": {
                "text": "string"
              }
            },
            {
              "name": "vertical",
              "description": "Whether the tablist runs down the page.",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "Which of `nextIndex`'s keys an arrow means on this tablist's axis.\n\nA tablist answers to one axis and ignores the other - Left/Right on a horizontal one,\nUp/Down on a vertical one - which is the whole of what `aria-orientation` promises. So\nrather than a second key map, the axis is translated into the vertical vocabulary\n`nextIndex` already speaks, and the wrapping and the ends stay in one place for every\npattern in the book.",
          "return": {
            "type": {
              "text": "string|null"
            }
          }
        },
        {
          "kind": "function",
          "name": "selectedIndex",
          "parameters": [
            {
              "name": "value",
              "description": "The attribute, as authored.",
              "type": {
                "text": "string|null"
              }
            },
            {
              "name": "length",
              "description": "How many tabs there are.",
              "type": {
                "text": "number"
              }
            }
          ],
          "description": "Which tab the `selected` attribute is naming, given how many there are.\n\nEvery value is answered with a tab, because a tab set with nothing selected is not a\nstate this pattern has: one tab is always current. A missing attribute, a typo and a\nnegative index are the first tab - there is no earlier one to mean. An index past the\nend, which is what a tab removed from under a `selected` leaves behind, is clamped to\nthe last tab rather than sent back to the start: the reader was at the far end of the\nset, and that is the nearest tab still there.",
          "return": {
            "type": {
              "text": "number"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<tabs-elemental>` custom element.\n\nA list of tabs and the panels they show, per the\n[APG Tabs pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/), in both\norientations.\n\nThe markup it is written on is the markup the page would have had anyway: a list of\nin-page links, and the sections they point at. With no script that is exactly what it\nstill is - every panel on screen, every link jumping to one - which is why the panels\nare not authored `hidden`. The element is what turns the list into a tablist, and it is\nthe only thing that ever hides a panel.\n\nponytail: no `<dialog>`-style platform primitive to build on here, and no sliding\nindicator either. An indicator that follows the selected tab has to be measured every\ntime the strip reflows; a border on the selected tab needs no script at all and says\nthe same thing.\n\nLight DOM, no shadow root. Nothing is moved and nothing is wrapped - the panels stay\nwhere the markup put them, which is what lets a page lay them out with the element's\nown grid or with one of its own.",
          "name": "TabsElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between the tabs.",
              "name": "--tabs-elemental-gap",
              "default": "0.25rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Padding inside a tab.",
              "name": "--tabs-elemental-inset",
              "default": "0.5rem 0.75rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between the tablist and the panel.",
              "name": "--tabs-elemental-panel-inset",
              "default": "1rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Corner radius of a tab.",
              "name": "--tabs-elemental-radius",
              "default": "0.375rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Thickness of the rule under the strip, and of the mark on the selected tab.",
              "name": "--tabs-elemental-indicator-size",
              "default": "2px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "What marks the selected tab.",
              "name": "--tabs-elemental-indicator",
              "default": "currentcolor"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The rule the tabs sit on.",
              "name": "--tabs-elemental-border",
              "default": "color-mix(in srgb, currentcolor 20%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Tab background under the pointer.",
              "name": "--tabs-elemental-hover",
              "default": "color-mix(in srgb, currentcolor 10%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Text of a tab that is not selected.",
              "name": "--tabs-elemental-muted",
              "default": "color-mix(in srgb, currentcolor 65%, transparent)"
            }
          ],
          "slots": [
            {
              "description": "The `<ul>` of tabs, and the panels - one per tab, in the same order.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "tablist",
              "description": "The tablist: the first list in the element. A nested `<tabs-elemental>` keeps its own.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "tabs",
              "description": "The tabs, in order. What the `<li>`s hold, so a link inside a panel is not one.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "panels",
              "description": "The panels, in tab order. A tab with nothing to show keeps its place as `null`.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "panelOf",
              "parameters": [
                {
                  "name": "tab"
                }
              ],
              "description": "The panel a tab shows: what its `aria-controls` or its own `#fragment` names, and\nfailing both, the child sitting in the same position.\n\nThe fragment is the one worth writing markup for. A tab authored as a link to its own\npanel is a working in-page link before this element exists and after it fails to\nupgrade, and it means the relationship is stated once rather than in an `id` and an\n`aria-controls` that can drift apart."
            },
            {
              "kind": "field",
              "name": "selected",
              "description": "Index of the selected tab. Reflected, so `[selected]` is a styling hook too."
            },
            {
              "kind": "field",
              "name": "vertical",
              "description": "Whether the tablist runs down the page. The arrow keys go with it."
            },
            {
              "kind": "field",
              "name": "manual",
              "description": "Whether moving focus along the tablist also selects.\n\nAutomatic is the default because it is what the APG recommends wherever showing a\npanel is instant, which it is when the panel is already in the page. `manual` is for\nthe case that is not: a panel whose content arrives over the network, where arrowing\npast four tabs would start four requests nobody asked for."
            },
            {
              "kind": "method",
              "name": "release",
              "parameters": [
                {
                  "name": "panel"
                }
              ],
              "description": "Take everything this element wrote back off a panel, and hand it to the page as it\nwas found."
            },
            {
              "kind": "method",
              "name": "wire",
              "description": "Read the markup and put the pattern on it - the roles, the pairings, the ids either\nside of them - then push the current state through `apply`.\n\nPublic and idempotent, because the tabs are the page's to change: add one, remove one,\nswap the labels, and this is the one call that says so. Nothing observes the markup on\nthe element's behalf, which would be a `MutationObserver` running on every page that\nnever touches its tabs to save this one line on the pages that do."
            },
            {
              "kind": "method",
              "name": "apply",
              "description": "Push the selection onto the tabs and their panels.\n\nThe roving tabindex is the half of this that is easy to miss: a tab strip is one stop\non the way through the page, not one stop per tab, so the selected tab is the only one\n`Tab` can land on and the arrows do the rest."
            },
            {
              "kind": "method",
              "name": "tabFor",
              "parameters": [
                {
                  "name": "e"
                }
              ],
              "description": "The tab this event happened on, or null for anything else - a link inside a panel,\nor a nested tab set's."
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onKeyDown",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onHashChange"
            },
            {
              "kind": "method",
              "name": "onBeforeMatch",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "selectFromHash",
              "description": "Select the tab whose panel holds the element the URL fragment points at, so a link\ninto a panel lands on it showing. Which is also the no-script story arriving: the tabs\nare in-page links, and following one before this element upgrades leaves exactly this\nfragment in the URL."
            }
          ],
          "events": [
            {
              "name": "tabs-select",
              "type": {
                "text": "CustomEvent"
              },
              "description": "`detail.tab` is the tab now selected, `detail.panel` what it shows, `detail.index` where it is."
            }
          ],
          "attributes": [
            {
              "name": "selected",
              "type": {
                "text": "number"
              },
              "description": "Index of the selected tab. Reflected, so `[selected]` is a styling hook.",
              "default": "0"
            },
            {
              "name": "vertical",
              "type": {
                "text": "boolean"
              },
              "description": "The tablist runs down the page rather than across it. Swaps the arrow keys with it.",
              "default": "false"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Arrows move focus without selecting; Enter or Space selects. Automatic otherwise.",
              "name": "manual",
              "default": "false"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "tabs-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "tabKey",
          "declaration": {
            "name": "tabKey",
            "module": "src/elementals/tabs/index.js"
          }
        },
        {
          "kind": "js",
          "name": "selectedIndex",
          "declaration": {
            "name": "selectedIndex",
            "module": "src/elementals/tabs/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TabsElemental",
          "declaration": {
            "name": "TabsElemental",
            "module": "src/elementals/tabs/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/tilt/index.js",
      "declarations": [
        {
          "kind": "variable",
          "name": "DEFAULT_MAX",
          "type": {
            "text": "number"
          },
          "default": "10",
          "description": "Degrees at the edge, when `max` says nothing usable. Chosen rather than derived: the card\nthis was extracted from leans 2, which is slight enough that most readers never see it."
        },
        {
          "kind": "function",
          "name": "tiltFrom",
          "parameters": [
            {
              "name": "rect",
              "description": "The box.",
              "type": {
                "text": "DOMRect|{left: number, top: number, width: number, height: number}"
              }
            },
            {
              "name": "pointerX",
              "description": "Viewport coordinates, as `PointerEvent.clientX` gives them.",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "pointerY",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "options",
              "default": "{}",
              "optional": true,
              "type": {
                "text": "{max?: number, axis?: string|null, reverse?: boolean}"
              }
            }
          ],
          "description": "Where the pointer is inside the box, as the two angles the card is drawn at and the point\nthe glare is centred on.\n\n**The sign convention is the whole of it, and nothing about `rotateX` announces which way\nit goes.** A positive `rotateX` sends the top edge away from the reader and a positive\n`rotateY` sends the right edge away, so the card leans *away* from the pointer - the feel\nof pressing a corner rather than of the card following a magnet. `reverse` is the other\none, and it is an attribute rather than a default because both look deliberate and only\none of them can be first.\n\nThe glare does not flip with it. Reverse changes which way the card leans; the light is\nstill where the reader is pointing.\n\nAngles are unitless and so is the glare position - the stylesheet multiplies by `1deg` and\n`1%`. That is what lets the theme's shadow read the same two numbers as an offset without\ndividing a `deg` back out of them.",
          "return": {
            "type": {
              "text": "{x: number, y: number, glareX: number, glareY: number}"
            }
          }
        },
        {
          "kind": "function",
          "name": "layerDepth",
          "parameters": [
            {
              "name": "value"
            }
          ],
          "description": "A layer's `data-tilt-depth` as a number, or `null` when it is not one.\n\n`Number` rather than `parseFloat`, so `data-tilt-depth=\"40px\"` is refused instead of\nhalf-honoured: the value is multiplied by `--tilt-elemental-depth-step`, and a length\ntimes a length is a `calc()` that does not resolve - which invalidates the whole\n`transform` and takes the card's rotation down with the layer's rise."
        },
        {
          "kind": "class",
          "description": "`<tilt-elemental>` custom element.\n\nA card that leans under the pointer: the 3D tilt every product page has, wrapped round the\nmarkup you already wrote rather than poured into a structure of its own.\n\nThere is no APG pattern for this, because there is no widget - nothing is operated, and the\ncontent is the same content lying flat. What there is instead is an obligation nobody in\nthis corner of the ecosystem meets.\n[WCAG 2.2 2.3.3 Animation from Interactions](https://www.w3.org/WAI/WCAG22/Understanding/animation-from-interactions.html)\nsays motion animation triggered by interaction has to be able to be turned off unless it is\nessential - and a decorative tilt is never essential, so there is no exemption to claim.\n`prefers-reduced-motion` is the technique the criterion names, and **this element reads it\nand then does not listen at all**: no pointer handlers, no transform, no glare. It follows\nthe setting live, because unlike a marquee there is no button here for a reader to have\npressed, so nothing is being overridden by keeping up with them.\n\nThe pointer is a mouse or it is nothing. A finger dragging across a card is a page trying\nto scroll, and a card that tilts under it is a card fighting the gesture it intercepted.\nThere is no keyboard equivalent either, and that is deliberate rather than missing: motion\nthat a reader cannot avoid triggering is the failure the criterion is about.\n\nLight DOM, no shadow root. Nothing you wrote is moved or wrapped - the element writes four\nnumbers into its own `style` and the stylesheet spends them. With no script it is a `<div>`\nthat does nothing, with your content inside it in reading order.\n\n**The tilt is a CSS transition, not a loop.** Every library on this shelf runs a `lerp`\ninside `requestAnimationFrame` to smooth the movement; a transition on `transform` is the\nsame easing done by the compositor, off the main thread, and it stops on its own. All the\nframe callback is left with is collapsing a burst of pointer events into one write.\n\nponytail: no gyroscope. `DeviceOrientationEvent.requestPermission()` needs transient\nactivation and a permission prompt on iOS, and a card that asks for sensor access to wobble\nis a worse trade than a card that does not wobble on a phone. If it is ever wanted it is an\nattribute plus a method the author calls from their own gesture, never an automatic prompt.\n\nponytail: no scale on hover, because `data-tilt-active` is public and\n`tilt-elemental[data-tilt-active] { scale: 1.03 }` is the whole feature.",
          "name": "TiltElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "How near the reader is. Smaller is a wider lens and a stronger lean for the same angle.",
              "name": "--tilt-elemental-perspective",
              "default": "1000px"
            },
            {
              "type": {
                "text": "<time>"
              },
              "description": "How long the card takes to catch up with the pointer. The trailing feel every tilt library calls speed.",
              "name": "--tilt-elemental-duration",
              "default": "120ms"
            },
            {
              "type": {
                "text": "<time>"
              },
              "description": "How long it takes to settle flat once the pointer has gone. Longer than the chase on purpose.",
              "name": "--tilt-elemental-return-duration",
              "default": "400ms"
            },
            {
              "type": {
                "text": "ease | ease-in | ease-out | ease-in-out | linear"
              },
              "description": "How both of those move.",
              "name": "--tilt-elemental-easing",
              "default": "ease-out"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "What one unit of `data-tilt-depth` is worth, so `data-tilt-depth=\"40\"` rises 40px.",
              "name": "--tilt-elemental-depth-step",
              "default": "1px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The highlight, alpha included. One property rather than a colour and an opacity, because a glare is the two together. Two weights because the surface decides how much of a white light shows - a light card needs all of it where a dark one needs a tenth.",
              "name": "--tilt-elemental-glare-color",
              "default": "light-dark(rgb(255 255 255 / 100%), rgb(255 255 255 / 10%))"
            },
            {
              "type": {
                "text": "<length-percentage>"
              },
              "description": "How far it spreads before it is gone. Small is a hotspot, large is a wash.",
              "name": "--tilt-elemental-glare-size",
              "default": "60%"
            },
            {
              "type": {
                "text": "<length-percentage>"
              },
              "description": "Theme. The card's corners, which the glare follows. Rounded with `border-radius` alone, because `overflow: hidden` over it would flatten every layer.",
              "name": "--tilt-elemental-radius",
              "default": "0.75rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Theme. How soft the shadow under the card is. Spent as half of itself, because the shadow is a blurred fill and a blur takes the deviation where a `box-shadow` takes twice it.",
              "name": "--tilt-elemental-shadow-size",
              "default": "1.5rem"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "Theme. Its colour, and the fill of the layer it is drawn on - so a card with a see-through background wants this at `transparent`. Dimmed to a quarter while the card lies flat, full while it leans. The offset is not a knob: it is the lean, a pixel per degree, moving against it.",
              "name": "--tilt-elemental-shadow-color",
              "default": "currentcolor at 30%"
            }
          ],
          "slots": [
            {
              "description": "The card. Anything at all; mark a descendant `data-tilt-depth=\"40\"` to have it rise out of the surface while the card is leaning.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "frame",
              "type": {
                "text": "number"
              },
              "default": "0",
              "description": "The frame callback's handle, `0` when none is queued."
            },
            {
              "kind": "field",
              "name": "pointer",
              "type": {
                "text": "null"
              },
              "default": "null",
              "description": "The last position the pointer reported, `null` when it is not over the card."
            },
            {
              "kind": "field",
              "name": "box",
              "type": {
                "text": "null"
              },
              "default": "null",
              "description": "The card's box as it is with nothing leaning it, held for the length of one hover and\n`null` at rest.\n\n**Every decision is made against this and not against the box the card is currently\ndrawn in**, which is the whole of why this element does not flicker at its own border.\nThe card leans *away* from the pointer, so the edge the pointer is nearest is always the\nedge that swings back - and a receding edge projects inwards. Hit-test against the\nleaning card and a pointer a pixel inside that edge falls outside it: `pointerleave`\nfires, the card straightens, the edge lands back under the pointer, and it leans again.\n\nA lift towards the reader looks like it would cover the gap and does not: what the\nrotation gives away grows with the square of the card's size, where a lift only scales\nit, so any fixed lift is a card size away from flickering again. Measured on a\n`640x400` card at ten degrees, `2.5rem` of lift was still nine pixels short.\n\nIt can only be read at the one moment the card is flat, which is the first pointer event\nof a hover - so it is read there and kept. Stale after a scroll, which is what the\nscroll listener is for."
            },
            {
              "kind": "field",
              "name": "max",
              "description": "Degrees at the edge. The trust boundary for `max`: past here it is a number at or above\nzero, and `0` is a value an author means rather than one to fall back from."
            },
            {
              "kind": "field",
              "name": "axis",
              "description": "`'x'`, `'y'`, or `null` for both. Anything else is both, rather than a card that has\nsilently stopped moving because of a typo."
            },
            {
              "kind": "method",
              "name": "update",
              "description": "Copy every `data-tilt-depth` in the card onto a custom property the stylesheet can\nmultiply.\n\nCSS cannot read an attribute as a number - `attr()` with a type is not in the browsers\nthis book targets - so the number has to be carried across, and this is the one place it\nhappens.\n\nponytail: read at upgrade and on demand, with no observer behind it. A card whose layers\narrive from a render after that calls `.update()`; a `MutationObserver` over the whole\nsubtree is the upgrade if that turns out to be the common case rather than the rare one."
            },
            {
              "kind": "method",
              "name": "onMotionChange"
            },
            {
              "kind": "method",
              "name": "start",
              "description": "Listen. Nothing is drawn until the pointer arrives, so this is safe to call twice."
            },
            {
              "kind": "method",
              "name": "stop",
              "description": "Stop listening and lie flat. The card animates back rather than snapping, because the\ntransition is on the transform and the transform is what changed."
            },
            {
              "kind": "method",
              "name": "onPointerMove",
              "parameters": [
                {
                  "name": "event"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onDocumentMove",
              "parameters": [
                {
                  "name": "event"
                }
              ],
              "description": "The pointer, once the card is leaning: the card's own `pointermove` stops being the\nauthority the moment there is a transform in the way of it.\n\nCapture, so nothing on the page can swallow the event that decides the card is done, and\nthe containment test is against the flat box rather than against whether the event\nreached the element at all."
            },
            {
              "kind": "method",
              "name": "track",
              "parameters": [
                {
                  "name": "x"
                },
                {
                  "name": "y"
                }
              ],
              "description": "Note where the pointer is and ask for a frame.\n\nThe first call of a hover is the one that matters: the card is still flat, so this is the\nonly moment `getBoundingClientRect` answers with the untransformed box, and it is kept\nfor the rest of the hover. Everything after it - the angles, and whether the pointer has\nleft - is measured against that.\n\nOne write a frame. `pointermove` outruns the display, and every extra pass is a style\ninvalidation for a frame nobody will see."
            },
            {
              "kind": "method",
              "name": "apply",
              "description": "Draw the card at the last position the pointer reported."
            },
            {
              "kind": "method",
              "name": "rest",
              "description": "Flat, and nothing queued.\n\nThe two angles are removed rather than set to zero, so the fallbacks in the stylesheet\nare the only place the resting angle is written down. Removing one still animates: what\ntransitions is `transform`, which is recomputed from whatever the `var()` resolves to.\n\n**The glare's position is deliberately left where it was.** Removing it too would send\nthe highlight sliding back to the middle of the card as it fades - a light travelling to\na place the pointer never was, and the one movement in this element that nobody asked\nfor. Fading out where it stood is what a highlight does. The next hover writes over it\nbefore anything is visible again.\n\nThe attribute comes off in the same task, which is what makes the way back slower than\nthe way out - the longer duration is the rule that applies once it is gone."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "number"
              },
              "description": "Degrees at the edge of the box. `0` is a card that does not move, for a glare on its own. Anything that is not a number at or above zero is the default.",
              "name": "max",
              "default": "10"
            },
            {
              "type": {
                "text": "x | y"
              },
              "description": "Keep one rotation and drop the other. `x` is the card nodding as the pointer moves up and down, `y` is it turning as the pointer moves across. Absent is both.",
              "name": "axis"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Lean towards the pointer instead of away from it. The glare does not move.",
              "name": "reverse",
              "default": "false"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Draw a highlight that follows the pointer across the card. `--tilt-elemental-x`, `--tilt-elemental-y`, `--tilt-elemental-glare-x` and `--tilt-elemental-glare-y` are deliberately not tagged below. The element writes them into its own `style` attribute, and an inline declaration beats any stylesheet - so a knob for one would be a control that cannot move anything, and the docs panel is built out of these tags. They are outputs, and the page describes them as such.",
              "name": "glare",
              "default": "false"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "tilt-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "DEFAULT_MAX",
          "declaration": {
            "name": "DEFAULT_MAX",
            "module": "src/elementals/tilt/index.js"
          }
        },
        {
          "kind": "js",
          "name": "tiltFrom",
          "declaration": {
            "name": "tiltFrom",
            "module": "src/elementals/tilt/index.js"
          }
        },
        {
          "kind": "js",
          "name": "layerDepth",
          "declaration": {
            "name": "layerDepth",
            "module": "src/elementals/tilt/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TiltElemental",
          "declaration": {
            "name": "TiltElemental",
            "module": "src/elementals/tilt/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/toolbar/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "toolbarKey",
          "parameters": [
            {
              "name": "key",
              "description": "`KeyboardEvent.key`",
              "type": {
                "text": "string"
              }
            },
            {
              "name": "vertical",
              "description": "Whether the bar runs down the page",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "Which of `stepIndex`'s keys an arrow means on this bar's axis, if any.\n\nA toolbar answers to one axis and leaves the other where it was pressed - Left/Right on\na bar across the page, Up/Down on one down it - which is the whole of what\n`aria-orientation` promises. The other axis is not merely unused: a Down arrow on a\nhorizontal bar is the page scrolling, and swallowing it would pin the page under a reader\nwho is only passing through.",
          "return": {
            "type": {
              "text": "string|null"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<toolbar-elemental>` custom element.\n\nA row of buttons the arrow keys walk and Tab passes in one step, per the\n[APG Toolbar pattern](https://www.w3.org/WAI/ARIA/apg/patterns/toolbar/).\n\nA bar of six buttons is six tab stops between the reader and whatever is after it. The\npattern's answer is a roving `tabindex`: one stop for the bar, arrows between the\ncontrols inside it. That is all this element is - the role, the axis, and the one\n`tabindex=\"0\"` that moves.\n\nThe ends do not wrap. Running off one is not how you get anywhere here: Tab is, and a bar\nthat looped would be a bar a reader can walk forever without noticing they had.\n\n**Name it.** A toolbar takes its name from `aria-label`, or `aria-labelledby` if something\non the page already says it - and where there is more than one bar, naming them is not\noptional. The element cannot invent one, and does not pretend to: an unnamed toolbar is\nannounced as a toolbar and nothing else.\n\nLight DOM, no shadow root. Degrades to what it is: with no script, the buttons are\nbuttons, each its own tab stop. That is the state the pattern improves on, not a broken\none - nothing is lost when the script never arrives.",
          "name": "ToolbarElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between the controls.",
              "name": "--toolbar-elemental-gap",
              "default": "0.25rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Padding inside the bar.",
              "name": "--toolbar-elemental-inset",
              "default": "0.25rem"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Corners of the bar and of a control inside it.",
              "name": "--toolbar-elemental-radius",
              "default": "0.375rem"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The bar's outline.",
              "name": "--toolbar-elemental-border",
              "default": "color-mix(in srgb, currentcolor 20%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "A control under the pointer.",
              "name": "--toolbar-elemental-hover",
              "default": "color-mix(in srgb, currentcolor 10%, transparent)"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "A control whose `aria-pressed` is true.",
              "name": "--toolbar-elemental-pressed",
              "default": "color-mix(in srgb, currentcolor 18%, transparent)"
            }
          ],
          "slots": [
            {
              "description": "The controls: `<button>`s and links, in the order the arrows should walk them.",
              "name": ""
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "vertical",
              "description": "Whether the bar runs down the page. Reflected, so `[vertical]` is a styling hook."
            },
            {
              "kind": "field",
              "name": "controls",
              "description": "The controls the arrows walk, in document order.\n\nA `disabled` button is left out because the platform will not focus one, and a cursor\nthat lands where focus cannot follow is a bar that stops moving. Keeping such a control\nreachable is `aria-disabled` on it instead - still focusable, still announced, and this\nlist still has it.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "wire",
              "description": "Put the axis on the bar and the single tab stop inside it.\n\nThe stop follows focus where there is any, so a bar entered by clicking its last button\nis a bar the arrows carry on from there rather than one that jumps back to the start."
            },
            {
              "kind": "method",
              "name": "onFocusIn"
            },
            {
              "kind": "method",
              "name": "onKeyDown",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            }
          ],
          "attributes": [
            {
              "name": "vertical",
              "type": {
                "text": "boolean"
              },
              "description": "The bar runs down the page rather than across it. Swaps the arrow keys with it, and says so with `aria-orientation`.",
              "default": "false"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "toolbar-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "toolbarKey",
          "declaration": {
            "name": "toolbarKey",
            "module": "src/elementals/toolbar/index.js"
          }
        },
        {
          "kind": "js",
          "name": "ToolbarElemental",
          "declaration": {
            "name": "ToolbarElemental",
            "module": "src/elementals/toolbar/index.js"
          }
        }
      ]
    },
    {
      "kind": "javascript-module",
      "path": "src/elementals/tooltip/index.js",
      "declarations": [
        {
          "kind": "function",
          "name": "titleRole",
          "parameters": [
            {
              "name": "trigger",
              "type": {
                "text": "{text: string, ariaLabel: ?string, ariaLabelledby: ?string}"
              }
            }
          ],
          "description": "Whether the words in a `title` are the trigger's *name* or a *description* of it.\n\nA `title` is the accessible name of a control that has no other one - an icon-only\nbutton is called \"Save\" by nothing else. Moving those words into a description and\ndropping the attribute would leave that button nameless, which is a worse page than the\none that started with a native tooltip. So the words go back where they were already\nworking: as a name when the trigger had none, as a description when it did.",
          "return": {
            "type": {
              "text": "'name'|'description'"
            }
          }
        },
        {
          "kind": "function",
          "name": "nextTooltipState",
          "parameters": [
            {
              "name": "state",
              "type": {
                "text": "{hovering: boolean, focused: boolean, dismissed: boolean, open: boolean}"
              }
            },
            {
              "name": "event",
              "type": {
                "text": "'pointerenter'|'pointerleave'|'focus'|'blur'|'escape'|'activate'"
              }
            }
          ],
          "description": "Where hover, focus and Escape leave a tooltip.\n\nA tooltip has no state to a screen reader - the description is on the trigger the whole\ntime, and this is only about what is drawn. Which is why it is a reducer over four\nfacts rather than an `open` flag: hover and focus can each hold it open, so neither\nleaving is on its own a reason to close.\n\nEscape is the part worth writing down. [WCAG 2.2 SC 1.4.13](https://www.w3.org/WAI/WCAG22/Understanding/content-on-hover-or-focus.html)\nasks for dismissible, and a dismissal that is undone by the next `pointermove` over the\nsame button is no dismissal at all - so `dismissed` outlives the press and is cleared\nonly once the reader has actually left, by pointer and by focus both.\n\nActivating the trigger is deliberately not Escape's dismissal. The click has already\ngiven the button focus, and a `dismissed` waiting for that focus to leave would kill\nhover on the control for as long as it stays - a mouse never blurs what it clicked. So\n`activate` forgets both holds instead: the bubble hides now, and whichever of hover or\nfocus arrives next opens it anew.",
          "return": {
            "type": {
              "text": "{hovering: boolean, focused: boolean, dismissed: boolean, open: boolean}"
            }
          }
        },
        {
          "kind": "function",
          "name": "arrowOffset",
          "parameters": [
            {
              "name": "trigger",
              "description": "Rect of the trigger, in viewport coordinates",
              "type": {
                "text": "DOMRect|object"
              }
            },
            {
              "name": "bubble",
              "description": "Where the bubble landed",
              "type": {
                "text": "{left: number, top: number, width: number, height: number}"
              }
            },
            {
              "name": "horizontal",
              "description": "Whether the bubble sits beside the trigger rather than over or under it",
              "type": {
                "text": "boolean"
              }
            },
            {
              "name": "rtl",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "How far along the bubble the caret has to sit to point at the middle of its trigger,\nmeasured from the bubble's own start edge on whichever axis the caret runs along.\n\nAligning the bubble to an edge of the trigger is not the same as pointing at it: a\nbutton wider than its tooltip is aligned at the left and centred nowhere near it. So the\ntrigger's middle is measured and handed over as one number, and where the caret may\nactually go - off the rounded corner, past the end - is the stylesheet's to clamp, since\nthe corner radius and the caret's own size are its numbers and not this file's.\n\nMeasured from the *inline start*, which in RTL is the right-hand edge - so a stylesheet\ncan spend it on `inset-inline-start` without knowing the direction.",
          "return": {
            "type": {
              "text": "number"
            }
          }
        },
        {
          "kind": "function",
          "name": "alignOnAxis",
          "parameters": [
            {
              "name": "start",
              "description": "Trigger's near edge on this axis, in viewport coordinates",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "end",
              "description": "Trigger's far edge",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "size",
              "description": "The bubble's extent on the same axis",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "limit",
              "description": "The viewport's, on that axis",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "margin",
              "default": "0",
              "description": "Breathing room kept from either edge, so a slid bubble stops\nshort of the glass rather than on it. A bubble too big for the margined viewport sits\nat the near margin.",
              "optional": true,
              "type": {
                "text": "number"
              }
            }
          ],
          "description": "Where the bubble starts on the axis it runs *along* - the one the caret slides on.\n\nCentred on the trigger, always: a tooltip is a bubble with a caret, and a caret coming\nout of a corner points at whatever happens to be beside the control. The viewport is the\none thing allowed to move it off the middle - the clamp slides the bubble just far enough\nin to fit, so a trigger near an edge keeps the most centred bubble there is room for. It\ndoes not spoil the caret, which is measured against wherever the bubble actually landed.\n\n`placeFlyout`'s own alignment used to decide this: it declines to centre where a fully\ncentred bubble would not fit and hands back the trigger's edge instead - a position that\nfits the viewport on its own, so the clamp never fired and the bubble jumped from centred\nto edge-aligned with nothing in between.",
          "return": {
            "type": {
              "text": "number"
            }
          }
        },
        {
          "kind": "function",
          "name": "landedAlign",
          "parameters": [
            {
              "name": "at",
              "description": "Where the bubble's near edge landed on this axis, in viewport coordinates",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "size",
              "description": "The bubble's extent on the same axis",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "start",
              "description": "Trigger's near edge on it",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "end",
              "description": "Trigger's far edge",
              "type": {
                "text": "number"
              }
            },
            {
              "name": "rtl",
              "description": "Whether this axis runs right to left - the inline one, in RTL",
              "type": {
                "text": "boolean"
              }
            }
          ],
          "description": "Where the bubble ended up relative to its trigger's middle, as the `data-align` a\nstylesheet reads.\n\nMeasured from where the bubble landed rather than taken from what was asked for, because\nthe centre is asked for every time now: the viewport's clamp is the only thing that can\ntake a bubble off the middle, and the attribute is worth nothing if it says `center` for\na bubble the edge of the screen has slid halfway off its trigger. `center` is the answer\non a page with room, which is most of them.\n\nThe token names the end of the *bubble* the caret comes out near, which is the corner a\nstylesheet has to care about - and it is logical, so RTL names the other one.",
          "return": {
            "type": {
              "text": "string"
            }
          }
        },
        {
          "kind": "function",
          "name": "withoutToken",
          "parameters": [
            {
              "name": "list",
              "type": {
                "text": "string|null"
              }
            },
            {
              "name": "token",
              "type": {
                "text": "string"
              }
            }
          ],
          "description": "A token list with one token taken back out, or `null` for a list left empty - the shape\n`removeAttribute` wants to hear.\n\nFor `aria-describedby` on teardown: the element only ever appended its bubble's `id`, so\nonly that one comes off, and every token the page had written there is kept - order,\nunknown ids and all.",
          "return": {
            "type": {
              "text": "string|null"
            }
          }
        },
        {
          "kind": "class",
          "description": "`<tooltip-elemental>` custom element.\n\nA description that shows on hover and on focus, wired to the control it describes - and\nstill on the page when the script is not.\n\nThe [APG's tooltip pattern](https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/) says of\nitself that it \"is work in progress; it does not yet have task force consensus\", so this\nimplements the half every source does agree on - `aria-describedby` from the trigger to\nthe words, Escape to dismiss, hover and focus to show - and refuses the half they do\nnot. `role=\"tooltip\"` is set because the pattern says so, and\n[buys nothing on its own](https://sarahmhigley.com/writing/tooltips-in-wcag-21/):\n`aria-describedby` is what any screen reader actually reads.\n\nTwo ways to write it, and they are the same element:\n\n```html\n<tooltip-elemental>\n  <button type=\"button\">Save</button>\n  <span>Saves to your drafts, without publishing</span>\n</tooltip-elemental>\n\n<button type=\"button\" id=\"save\">Save</button>\n<tooltip-elemental for=\"save\">Saves to your drafts, without publishing</tooltip-elemental>\n```\n\nWrapping is the one to reach for. `for` exists because a bubble is sometimes written far\nfrom the button it belongs to - a table of controls, a template that emits its help text\nin one block - and it costs nothing at the markup level, since the element works out\nwhich shape it is in by whether it contains something focusable.\n\nA `title` on the trigger is read as the words when there is no other source for them,\nand the attribute is removed so the native tooltip does not double up. Where those words\nend up depends on what the trigger already had: see `titleRole`.\n\n**It is all but unreachable by touch, and that is not a bug to work around.** There is no\nhover on a touch screen and a tap is activation rather than hover, so pointer events from\na touch are ignored outright rather than half-handled. Focus is the one way in that is\nleft, and whether a tap moves it is the engine's: Chromium focuses a `<button>` on tap and\nthe bubble opens, WebKit does not focus buttons on tap and nothing appears - measured, not\nassumed. Focus is deliberately not filtered by how it arrived, because that would take the\nwords away from the readers who do get them; what it means is that nothing essential\nbelongs in one. A control that needs its description read on every device wants visible\ntext, and one that reveals content on a press wants `<disclosure-elemental>`.\n\nActivating the trigger - click, Enter, Space - hides the bubble: a used control's tooltip\nhas said its piece, and the focus the click leaves on the button would otherwise hold it\nopen over the neighbour's bubble the pointer goes to next. It is not Escape's held\ndismissal - hovering away and back, or Tab out and in, shows it again. A tap's click is\nthe one exemption, since on Chromium the tap is how touch opens the bubble at all.\n\nLight DOM, no shadow root. Nothing is moved and nothing is wrapped: the bubble is the\nelement the author wrote, given a `role`, an `id` if it had none, and a `hidden` while\nit is not showing - which still leaves it readable, because\n[`aria-describedby` reaches hidden content](https://www.w3.org/TR/accname-1.2/).\n\nDegrades honestly: with no script the words are a sentence beside the control, which is\nwhat the markup says on its own. Nothing is hidden until there is something able to show\nit again.\n\nponytail: fixed positioning rather than the popover API, so a bubble is not clipped by a\nscrolling ancestor. Popover would put it in the top layer and take the Escape handling\nwith it; it is worth revisiting once CSS anchor positioning is in Firefox and Safari,\nwhich is the half that would then be missing.",
          "name": "TooltipElemental",
          "cssProperties": [
            {
              "type": {
                "text": "<length>"
              },
              "description": "Between the trigger and the bubble.",
              "name": "--tooltip-elemental-gap",
              "default": "6px"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "The least the bubble keeps between itself and the edge of the viewport, sliding along its trigger and flipping sides to honour it. `0` lets it kiss the edge again.",
              "name": "--tooltip-elemental-viewport-margin",
              "default": "6px"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Half the caret, since it is drawn as a border.",
              "name": "--tooltip-elemental-caret",
              "default": "5px"
            },
            {
              "type": {
                "text": "<time>"
              },
              "description": "The fade, in and out. Off by default; `prefers-reduced-motion` keeps it off however high it is turned.",
              "name": "--tooltip-elemental-duration",
              "default": "0s"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Above and below the words.",
              "name": "--tooltip-elemental-padding-block",
              "default": "0.5em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Either side of them.",
              "name": "--tooltip-elemental-padding-inline",
              "default": "0.75em"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "The bubble's corners.",
              "name": "--tooltip-elemental-radius",
              "default": "6px"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "The rim, which the caret takes too.",
              "name": "--tooltip-elemental-border-width",
              "default": "1px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "That rim's colour.",
              "name": "--tooltip-elemental-border-color",
              "default": "Canvas 28% into the surface"
            },
            {
              "type": {
                "text": "<length>"
              },
              "description": "Where the words wrap. A tooltip is a sentence, not a paragraph.",
              "name": "--tooltip-elemental-max-width",
              "default": "250px"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "What the bubble is painted in.",
              "name": "--tooltip-elemental-surface",
              "default": "CanvasText"
            },
            {
              "type": {
                "text": "<color>"
              },
              "description": "The words on it.",
              "name": "--tooltip-elemental-color",
              "default": "Canvas"
            }
          ],
          "members": [
            {
              "kind": "field",
              "name": "trigger",
              "description": "The control being described: what the element wraps, or what `for` names.",
              "readonly": true
            },
            {
              "kind": "field",
              "name": "bubble",
              "description": "The words. A direct child that is not the trigger, or - when `for` named the trigger\nfrom somewhere else on the page - this element itself.",
              "readonly": true
            },
            {
              "kind": "method",
              "name": "onPointer",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onFocus"
            },
            {
              "kind": "method",
              "name": "onBlur"
            },
            {
              "kind": "method",
              "name": "onKeydown",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "onClick",
              "parameters": [
                {
                  "name": "e"
                }
              ]
            },
            {
              "kind": "method",
              "name": "apply",
              "parameters": [
                {
                  "name": "event"
                }
              ],
              "description": "Runs one event through the state machine and draws whatever came out of it."
            },
            {
              "kind": "method",
              "name": "close",
              "parameters": [
                {
                  "name": "delay"
                }
              ],
              "description": "Leaving with a pointer waits, so the strip between the trigger and the bubble can be\ncrossed - the bubble has to be reachable to satisfy \"hoverable\"."
            },
            {
              "kind": "method",
              "name": "show"
            },
            {
              "kind": "method",
              "name": "hide"
            },
            {
              "kind": "method",
              "name": "stopWatching"
            },
            {
              "kind": "method",
              "name": "reposition"
            },
            {
              "kind": "method",
              "name": "place",
              "description": "Puts the bubble beside the trigger, in viewport coordinates.\n\n`position: fixed` rather than an offset parent, because the two are not always in the\nsame one - and because a tooltip inside anything scrolling would otherwise be clipped\nby it. The side and the alignment are written out as attributes as well, since a caret\nhas to point back the way the bubble came from and nothing in CSS can read a number\nthis file computed.\n\nThe axis is the author's, the side is the viewport's: `horizontal` says beside rather\nthan over or under, and which of the two sides that turns out to be is measured. Which\nis the whole reason there is no `placement=\"e\"` here - a fixed side is a tooltip off\nthe edge of the screen on the one page where it did not fit."
            }
          ],
          "attributes": [
            {
              "type": {
                "text": "string"
              },
              "description": "`id` of the control this describes. Only read when the element does not wrap one.",
              "name": "for"
            },
            {
              "type": {
                "text": "boolean"
              },
              "description": "Beside the control rather than over or under it. Which of the two sides is still the viewport's call.",
              "name": "horizontal",
              "default": "false"
            }
          ],
          "superclass": {
            "name": "ElementBase",
            "module": "/src/core.js"
          },
          "tagName": "tooltip-elemental",
          "customElement": true
        }
      ],
      "exports": [
        {
          "kind": "js",
          "name": "titleRole",
          "declaration": {
            "name": "titleRole",
            "module": "src/elementals/tooltip/index.js"
          }
        },
        {
          "kind": "js",
          "name": "nextTooltipState",
          "declaration": {
            "name": "nextTooltipState",
            "module": "src/elementals/tooltip/index.js"
          }
        },
        {
          "kind": "js",
          "name": "arrowOffset",
          "declaration": {
            "name": "arrowOffset",
            "module": "src/elementals/tooltip/index.js"
          }
        },
        {
          "kind": "js",
          "name": "alignOnAxis",
          "declaration": {
            "name": "alignOnAxis",
            "module": "src/elementals/tooltip/index.js"
          }
        },
        {
          "kind": "js",
          "name": "landedAlign",
          "declaration": {
            "name": "landedAlign",
            "module": "src/elementals/tooltip/index.js"
          }
        },
        {
          "kind": "js",
          "name": "withoutToken",
          "declaration": {
            "name": "withoutToken",
            "module": "src/elementals/tooltip/index.js"
          }
        },
        {
          "kind": "js",
          "name": "TooltipElemental",
          "declaration": {
            "name": "TooltipElemental",
            "module": "src/elementals/tooltip/index.js"
          }
        }
      ]
    }
  ]
}
